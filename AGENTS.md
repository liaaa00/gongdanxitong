# AGENTS.md — 强制入口

> 本文件是每个新会话唯一会被自动加载的规则入口。
> 详细规范在下方引用的文档里,动手前必须先读。

## 0. 改代码前,先读这些(按顺序)

任何代码改动、修复、重构、迁移之前,必须先阅读:

1. `docs/AI修改前必读.md` — 新会话第一步、冲突处理、影响面判断
2. `docs/业务规则回归清单.md` — 已确认且本次未被要求改变的旧规则
3. `docs/project-rules/AGENTS.md` — 项目最高操作规范(命名、目录、权限、业务边界)
4. 本次涉及页面/接口的现有测试

不要只依赖聊天上下文。长对话被压缩后,上下文里的规则会丢失,但这些文档不会。

## 1. 动手原则:读完三文档直接做,不必逐步请示

1. 读完第 0 节三个文档 + 相关测试后,**直接动手实现用户需求,不必先回报落点/影响面再等批准**。
2. 修改前先查看现有实现,禁止凭空重写或"觉得更优雅"而改。
3. 小步、可回滚、可验证;**绝不破坏现有可用代码与既有业务规则**。
4. 遇到超出本次需求的情况,**自行判断处理,无需停下来逐条确认**;判断标准是:不伤害现有功能、不改动无关业务口径。把握不准是否会伤到现有代码时,可**自己补充对应测试来验证**,用测试兜底而不是来回追问。

## 2. 唯一的硬性确认边界

仅当遇到**不可逆/高风险**操作时才必须先问用户,其余一律自主推进:

- 删除多个文件/目录、删库删表、`git reset --hard`、强推、改动生产环境或 CI。
- 需要改变用户已确认的旧业务规则口径(与 `docs/业务规则回归清单.md` 冲突)时,先指出冲突点再决定。

## 3. 改后留痕(自动完成,不再逐项汇报过程)

完成修改后:

1. **自动**把本次改动追加一条到 `docs/AI修改记录.md`(改了什么、为什么、如何验证)。
2. 若覆盖了 `docs/业务规则回归清单.md` 里的旧规则,同步更新该清单。
3. 跑固定回归:根目录 `回归测试.ps1`(可加 `-SkipBuild` 快速跑)。
4. 对用户的最终回复**只需一句话**:需求是否完成、是否通过回归。无需复述每个文件、每步过程,除非用户追问。

## 4. 优先级

1. 用户当前最新明确要求。
2. `docs/业务规则回归清单.md` 中未被本次要求改变的旧规则。
3. Git commit、测试、历史文档。
4. AI 的对话记忆或推断。

用户最新要求与旧规则冲突时:先指出冲突点 → 确认是否覆盖 → 同步改代码+测试+清单+记录。
