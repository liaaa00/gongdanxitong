# 工单系统 - 局域网单机服务器部署架构设计

## 📐 架构概述

### 部署模式
**局域网单机服务器模式**：将用户的本地电脑作为服务器，所有同事通过局域网访问同一套系统。

### 核心原则
1. **单一数据源**：所有数据保存在服务器电脑的 PostgreSQL 数据库
2. **数据互通**：所有同事访问同一个数据库，数据实时同步
3. **集中管理**：只有服务器电脑运行后端和数据库
4. **零客户端**：同事无需安装任何软件，仅需浏览器

---

## 🏗️ 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        服务器电脑                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  PostgreSQL 数据库                                        │   │
│  │  监听地址: 127.0.0.1:5432                                 │   │
│  │  - 仅本机访问（安全）                                      │   │
│  │  - 所有工单、用户、权限数据                                │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            ↑                                     │
│                            │ 本机连接                             │
│                            │                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  后端 API (NestJS)                                        │   │
│  │  监听地址: 0.0.0.0:3000                                   │   │
│  │  - 监听所有网络接口（局域网可访问）                         │   │
│  │  - 处理业务逻辑、权限控制                                  │   │
│  │  - CORS 已启用                                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            ↑                                     │
│                            │ HTTP API 调用                        │
│                            │                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  前端页面 (Vite + React)                                  │   │
│  │  监听地址: 0.0.0.0:5173                                   │   │
│  │  - 监听所有网络接口（局域网可访问）                         │   │
│  │  - 提供用户界面                                            │   │
│  │  - API 代理到后端                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  服务器 IP: 192.168.26.66                                        │
│  防火墙: 已开放端口 3000, 5173                                   │
└─────────────────────────────────────────────────────────────────┘
                            ↑
                            │ 局域网 HTTP 访问
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌─────────┐         ┌─────────┐        ┌─────────┐
   │ 同事A   │         │ 同事B   │        │ 同事C   │
   │ 浏览器  │         │ 浏览器  │        │ 浏览器  │
   └─────────┘         └─────────┘        └─────────┘
   
   访问地址: http://192.168.26.66:5173
   API 请求: http://192.168.26.66:3000/api
```

---

## 🔧 技术实现细节

### 1. 网络监听配置

#### PostgreSQL
```yaml
监听地址: 127.0.0.1:5432
访问控制: 仅本机
原因: 数据库不需要暴露到局域网，提高安全性
```

#### 后端 (NestJS)
```typescript
// src/main.ts
await app.listen(port); // 默认监听 0.0.0.0

// 启动命令
$env:HOST='0.0.0.0'
node dist/main.js
```

配置要点：
- 监听 `0.0.0.0:3000`，允许局域网访问
- 启用 CORS，允许跨域请求
- 连接本机 PostgreSQL (`127.0.0.1:5432`)

#### 前端 (Vite)
```bash
vite --host 0.0.0.0 --port 5173
```

配置要点：
- 监听 `0.0.0.0:5173`，允许局域网访问
- API 代理配置指向服务器 IP

### 2. API 通信流程

```
同事浏览器
  ↓ 访问
http://192.168.26.66:5173 (前端页面)
  ↓ 加载 JavaScript
前端代码执行
  ↓ API 请求
http://192.168.26.66:3000/api/* (后端 API)
  ↓ 数据库查询
PostgreSQL (127.0.0.1:5432)
  ↓ 返回数据
后端 API
  ↓ JSON 响应
前端页面
  ↓ 渲染
同事浏览器显示
```

### 3. 前端 API 配置

```env
# frontend/.env
VITE_API_BASE_URL=http://192.168.26.66:3000
```

```typescript
// frontend/src/services/request.ts
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL?.replace(/\/$/, '');

const request = axios.create({
  baseURL: apiBaseUrl ? `${apiBaseUrl}/api` : '/api',
  timeout: 30000,
});
```

**关键点**：
- 开发模式：使用 `VITE_API_BASE_URL` 直接指向服务器 IP
- 生产模式：可以使用相对路径 `/api`（通过 Nginx 代理）

### 4. 防火墙配置

```powershell
# 开放端口 3000 (后端 API)
New-NetFirewallRule -DisplayName "工单系统-后端API" `
    -Direction Inbound `
    -Protocol TCP `
    -LocalPort 3000 `
    -Action Allow `
    -Profile Domain,Private

# 开放端口 5173 (前端页面)
New-NetFirewallRule -DisplayName "工单系统-前端页面" `
    -Direction Inbound `
    -Protocol TCP `
    -LocalPort 5173 `
    -Action Allow `
    -Profile Domain,Private
```

**安全策略**：
- 仅允许域网络和专用网络访问
- 不允许公共网络访问
- 数据库端口 5432 不对外开放

---

## 🚀 启动流程设计

### 自动化启动脚本 (`局域网启动.ps1`)

```
1. 清理残留进程
   ├─ 停止所有 Node 进程
   └─ 强制释放端口 3000, 5173

2. 启动 PostgreSQL
   ├─ 检查是否已运行
   └─ 未运行则启动

3. 重新编译后端
   ├─ cd backend
   └─ npm run build

4. 启动后端
   ├─ 设置 HOST=0.0.0.0
   ├─ node dist/main.js
   └─ 健康检查 (http://127.0.0.1:3000/api/health)

5. 启动前端
   ├─ vite --host 0.0.0.0 --port 5173
   └─ 健康检查 (http://127.0.0.1:5173)

6. 显示访问信息
   ├─ 本机访问地址
   ├─ 局域网访问地址
   └─ 测试账号
```

### 关键设计决策

#### 为什么每次都重新编译？
- ✅ 确保运行的是最新代码
- ✅ 避免"修改了代码但没生效"的问题
- ✅ 编译时间短（约 5-10 秒），可接受

#### 为什么强制清理进程？
- ✅ 避免端口被占用
- ✅ 避免运行多个实例
- ✅ 确保干净的启动环境

#### 为什么监听 0.0.0.0？
- ✅ 允许局域网内所有设备访问
- ✅ 不仅限于 localhost
- ✅ 支持多网卡环境

---

## 🔒 安全设计

### 网络层安全

1. **数据库隔离**
   - PostgreSQL 仅监听 127.0.0.1
   - 不暴露到局域网
   - 只有本机后端可以访问

2. **防火墙保护**
   - 仅开放必要端口（3000, 5173）
   - 仅允许域网络和专用网络
   - 拒绝公共网络访问

3. **CORS 配置**
   - 后端启用 CORS
   - 允许跨域请求
   - 生产环境可配置白名单

### 应用层安全

1. **身份认证**
   - JWT Token 认证
   - Token 存储在 localStorage
   - 过期自动跳转登录

2. **权限控制**
   - 基于角色的访问控制（RBAC）
   - 字段级权限控制
   - 数据行级权限控制

3. **密码安全**
   - bcrypt 加密存储
   - 默认密码强制修改（可选）
   - 密码复杂度要求（可选）

---

## 📊 数据流设计

### 用户登录流程

```
1. 用户访问 http://192.168.26.66:5173
   ↓
2. 输入账号密码
   ↓
3. POST http://192.168.26.66:3000/api/auth/login
   ↓
4. 后端验证密码（bcrypt）
   ↓
5. 查询用户角色和权限
   ↓
6. 生成 JWT Token
   ↓
7. 返回 Token 和用户信息
   ↓
8. 前端存储 Token 到 localStorage
   ↓
9. 跳转到首页
```

### 工单创建流程

```
1. 用户填写工单表单
   ↓
2. POST http://192.168.26.66:3000/api/work-orders
   ↓
3. 后端验证 Token
   ↓
4. 检查用户权限（是否可创建工单）
   ↓
5. 验证表单数据
   ↓
6. 保存到 PostgreSQL
   ↓
7. 触发自动派单规则
   ↓
8. 发送通知给相关人员
   ↓
9. 返回工单 ID
   ↓
10. 前端显示成功消息
```

### 数据同步机制

**实时性保证**：
- 所有同事访问同一个数据库
- 数据修改立即生效
- 刷新页面即可看到最新数据

**未来优化**：
- WebSocket 实时推送
- SSE (Server-Sent Events) 通知
- 前端轮询更新

---

## 🎯 部署检查清单

### 首次部署

- [ ] 安装 Node.js v20.20.2
- [ ] 安装 PostgreSQL 16
- [ ] 克隆代码到 `D:\AI\SpeceAppDate\工单系统`
- [ ] 后端安装依赖：`cd backend && npm install`
- [ ] 前端安装依赖：`cd frontend && npm install`
- [ ] 配置后端 `.env` 文件
- [ ] 配置前端 `.env` 文件（设置服务器 IP）
- [ ] 初始化数据库：`npm run seed`
- [ ] 配置防火墙：运行 `配置防火墙.ps1`
- [ ] 测试启动：运行 `局域网启动.ps1`
- [ ] 验证访问：本机访问 `http://localhost:5173`
- [ ] 验证访问：同事访问 `http://192.168.26.66:5173`

### 日常启动

- [ ] 运行 `局域网启动.ps1`
- [ ] 等待启动完成（约 30 秒）
- [ ] 检查后端健康：`http://127.0.0.1:3000/api/health`
- [ ] 检查前端可访问：`http://127.0.0.1:5173`
- [ ] 通知同事系统已启动

### 代码更新

- [ ] 停止服务：运行 `停止系统.ps1`
- [ ] 拉取最新代码：`git pull`
- [ ] 更新依赖（如有）：`npm install`
- [ ] 重新启动：运行 `局域网启动.ps1`
- [ ] 验证功能正常

---

## 📈 性能考虑

### 预期负载

- **并发用户**：10-50 人
- **数据量**：10,000+ 工单
- **响应时间**：< 500ms

### 性能优化

1. **数据库优化**
   - 索引优化（主键、外键、常用查询字段）
   - 连接池配置
   - 查询优化

2. **后端优化**
   - 分页查询
   - 缓存常用数据
   - 异步处理

3. **前端优化**
   - 代码分割
   - 懒加载
   - 虚拟滚动

### 资源需求

**服务器电脑最低配置**：
- CPU: 4 核
- 内存: 8GB
- 硬盘: 100GB 可用空间
- 网络: 100Mbps 局域网

---

## 🔄 扩展性设计

### 当前架构的限制

- ❌ 单点故障：服务器电脑宕机则系统不可用
- ❌ 性能瓶颈：所有请求由单台电脑处理
- ❌ 局域网限制：无法远程访问

### 未来扩展方案

#### 方案 1：Docker 容器化部署
```yaml
services:
  postgres:
    image: postgres:16
  backend:
    build: ./backend
  frontend:
    build: ./frontend
  nginx:
    image: nginx
```

#### 方案 2：云服务器部署
- 部署到阿里云/腾讯云
- 支持远程访问
- 自动备份和容灾

#### 方案 3：负载均衡
- 多台后端服务器
- Nginx 负载均衡
- Redis 会话共享

---

## 📝 维护建议

### 日常维护

1. **每日**
   - 检查系统是否正常运行
   - 查看错误日志

2. **每周**
   - 备份数据库
   - 清理临时文件
   - 检查磁盘空间

3. **每月**
   - 更新依赖包
   - 安全补丁
   - 性能分析

### 备份策略

```powershell
# 数据库备份
$env:PGPASSWORD='ticket123'
& 'D:\pgsql16portable\pgsql\bin\pg_dump.exe' `
  -h 127.0.0.1 -U ticket -d ticket_system `
  -F c -f "backup_$(Get-Date -Format 'yyyyMMdd').dump"
```

### 监控指标

- 系统可用性
- 响应时间
- 错误率
- 并发用户数
- 数据库连接数
- 磁盘使用率

---

## 🎓 总结

### 架构优势

✅ **简单易用**：一键启动，无需复杂配置  
✅ **数据互通**：所有同事访问同一数据库  
✅ **零客户端**：仅需浏览器，无需安装软件  
✅ **成本低廉**：利用现有电脑，无需额外硬件  
✅ **易于维护**：集中管理，统一更新  

### 适用场景

✅ 小型团队（10-50 人）  
✅ 局域网环境  
✅ 对可用性要求不高  
✅ 预算有限  

### 不适用场景

❌ 大型团队（100+ 人）  
❌ 需要远程访问  
❌ 高可用性要求（99.9%+）  
❌ 高并发场景  

---

**文档版本**：v1.0  
**最后更新**：2026-05-14  
**架构师**：Claude (Architect)
