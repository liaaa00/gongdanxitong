# 入职期限字段修复发布结果

2026-09-14 用户明确要求删除入职“合同期限”和“试用期（月）”输入，并授权验证后同步 `192.168.26.195:8080`；本次已完成部署及正式浏览器验收。

## 范围

- 入职新建、动态编辑、子工单业务员修改弹窗、内部导入模板、门户表单和门户标准模板不再收集 `contract_term` / `probation_months`，模板配置也不能重新加入这两项。保留全局字段定义和历史值，不改变续签合同期限规则。
- 不再按旧月数自动生成试用期结束日期；页面选择范围与后台提交/导入校验按合同起止日期判断1/2/6个月上限，试用期可空，包含月末、非法日期、缺固定合同终止日期和试用期起止倒置检查。
- 内部模板实际63列减为61列，说明行、标色分区、列宽和下拉引用同步移动。门户模板使用其自身客户字段子集，不把内部61列数量套用到门户。
- 两项定向 migration：停用入职模板旧输入列；电子签合同输出移除系统自带的旧月数结束日期公式，直接输出已填写结束日期。兼容数据库 `field_code` 和种子 `fieldCode`，不改自定义公式或表头顺序。

## 发布证据

- 白名单824文件，包 `.release/duration-20260914/runtime-source.tar.gz`；SHA256 `699a68e45f50314eb53a95dec0164ecb18f217fb003a45d7f518d11612d84a57`。上传前对上次生产文件哈希复核无漂移，最终本地候选无漂移。未提交Git，其他窗口工作保留。
- 服务器独立构建前后端Docker镜像，恢复生产备份到隔离数据库演练；最终136项migration，新增2项。导出公式迁移在真实克隆配置上重复执行验证幂等。
- `回归测试.ps1 -SkipBuild`两轮成功，最后一轮721项（前端272、后端394、门户55）；补充动态表单19项、输入规则8项，以及迁移/合同导出/导入模板/日期边界5套41项通过。前后端构建及服务器最终镜像构建通过。
- 正式浏览器通过实际“下载当前字段模板”按钮获得Excel，61列，两个旧表头不存在、期限形式和起止日期存在；新建入职页旧控件不存在，填写合同日期不会自动开启试用期。证据 `.tmp_server_compare/duration-production.xlsx`、`duration-production-browser.json`、`duration-production-form.png`。
- 隔离门户使用真实密码登录、真实网关/连接器/后端，无请求mock；不填试用期提交成功、一个月边界成功、超出一天拒绝；真实下载客户Excel，上传预览与确认均两行成功、一行超期失败。桌面/手机截图及结果 `.tmp_server_compare/duration-portal-browser.json`、`duration-portal-desktop.png`、`duration-portal-mobile.png`。
- 正式浏览器使用既有管理员短期签名会话，不重置生产密码、不写测试工单。生产10张业务表原字段摘要前后一致，主单282/子单1012/客户222；测试账户、工单、门户记录仅在隔离库。生产环境配置哈希及上传挂载保留，后端healthy。

## 备份与边界

服务器备份及发布证据：`/data/apps/work-order-system/.releases/duration-20260914`；包含最新停写备份`cutover-server.dump`、源码/配置备份、前后数据摘要和镜像构建日志。回滚镜像为`work-order-system-backend:rollback-duration-20260914`、`work-order-system-frontend:rollback-duration-20260914`；禁止整库恢复覆盖上线后的业务。

仅更新内部生产应用及门户源码；独立外网门户和SMTP仍未正式上线。门户写入测试仅在隔离副本执行，不代表完整增减员派单办结流程重测。
