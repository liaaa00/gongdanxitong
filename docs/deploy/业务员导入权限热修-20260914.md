# 业务员导入权限热修

- 目标：192.168.26.195:8080，用户明确要求立即修复生产。
- 原因：AddPortalBusinessRoutes 为缺省角色创建仅含门户动作的覆盖数组，默认导入动作丢失。后端导入校验仍允许业务员访问。
- 生产修复：备份并锁定 roleActionPermissions.v1.beilun，在事务内仅为 business_group_member、business_group_leader、biz_member、biz_leader、salesperson 已有配置追加 work_order.import、route.work_order_import。
- 不改变用户、角色绑定、主子工单、客户、上传文件、密码或省外配置，不导入本地测试数据，不停机，不放开业务负责人或后道角色。
- 备份：服务器 /data/apps/work-order-system/.releases/duration-20260914/import-permissions-backup.private.json。该备份含原权限配置，恢复前应检查是否有后续管理员变更，不能盲目覆盖。
- 持久修复：旧门户迁移不再创建缺省角色覆盖数组；新增 RestoreBusinessMainOrderImport20260914220000 补偿迁移。本次热修直接执行该迁移的 up，未伪造 TypeORM 执行记录，下次标准发布会再次执行，集合去重保证不重复授权。
- 真实浏览器：以既有业务员和组长短期签名会话，各验证入职/离职按钮、跳转、实际 Excel 下载，共4条路径成功；不宣称验证了密码登录或生产写入导入。入职61列，合同期限/试用期（月）不再出现。
- 证据：.tmp_server_compare/import-browser-result.json，四份实际下载模板；权限迁移测试与 import-permissions 测试通过，后端构建通过。固定回归日志：.tmp_server_compare/import-regression.log。
- 最终固定回归725项通过，另跑权限相关11项通过并加入后续固定回归；生产后端healthy。两份迁移源码与服务器SHA256一致，镜像未重建，配置修复已持久写入生产数据库。
