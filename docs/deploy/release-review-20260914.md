# 2026-09-14 本地与服务器合并发布审核

> 最新状态：用户后续授权后已完成程序同步；本页以下为历史审核快照，最终包、生产数据保护与验收结果以 [发布结果](发布结果-20260914.md) 为准。

> **发布暂停（多窗口复核）**：本文件中的包与验证是历史候选快照，不是最终放行结论。银行卡条件、旧省外写入口、容器脚本及历史 stash 审核仍有待办，以 [统一功能发布清单](统一功能发布清单-20260914.md) 的阻塞项及放行标准为准，未获当前候选的生产发布确认。

本文件记录发布准备，不代表已上线；服务器本轮仅执行只读查询和 pg_dump 输出到本机。

## 目标与基线

- 唯一目标：192.168.26.195:22，admin，/data/apps/work-order-system。
- 当前标记：194b460953dbb7ca506c390cb066642844b74d0d + resignation/import sync 20260903_142200。
- 容器：ticket_backend、ticket_frontend、ticket_nginx、ticket_postgres；仅后端/前端属于后续代码替换范围。
- 后端容器内 src 与部署目录 src 文件哈希一致；后端创建于 9 月 8 日，前端创建于 9 月 3 日。
- 本地基线：4c51702 加当前工作区修改，不能只部署 HEAD。发布包以逐文件 SHA256 为准。

## 补丁保留

服务器补丁包括离职材料指定人员编辑、按字段权限补充、离职原因固定选项与历史值展示、导入按实际业务类型识别，以及大请求正文解析。
相对 194b460 完整核对发现 9 个服务器补丁文件，其中 2 个与本地完全一致，7 个由本地保留或扩展。本地保留上述行为，并增加离职证明补充范围。请求体上限从服务器 20MB 扩展为门户附件需要的 50MB；不覆盖生产 Nginx 和上传目录配置。
逐文件证据在本机 .tmp_server_compare/current-comparison.json、server-patches.txt。

## 本轮修复

- 试用期结束日期按实际日历日期比较，拒绝超期一天；月末截到目标月最后一天，验证闰年；非法日期不再自动滚动成下个月。
- 合同一周年、三周年阈值也按实际日期判断，避免完整月数截断。
- 新增定向配置迁移 AlignOptionalProbationAndBankFields20260914100000，替代全量 seed：仅清理旧默认试用期条件，补齐入职材料银行卡字段既定权限；保留自定义条件及非 hidden 权限。

## 数据库预演

- 服务器快照在本机独立数据库 ticket_release_20260914 恢复，未覆盖本地 ticket_system，也未对服务器执行迁移。
- 初始执行记录 117 项；原差异 16 项迁移通过，重复运行无待执行项；随后新增定向配置迁移通过，总记录 134 项。
- 业务基线：主单 281、子单 1008、客户 221。原 16 项迁移后子单、客户等未涉及表完整行哈希一致。
- 主单仅新增 completion_version 列；排除新增列后，原字段完整行哈希一致。
- 预期配置变更涉及 field_configs、field_permissions、field_supplement_rules、detail_view_templates、import_template_fields、system_settings；新增门户和邮件共 8 张表。
- 新定向迁移验证重复执行无变化，masked 权限和管理员自定义条件保留。
- 完整预演日志和快照仅保存在本机 .tmp_server_compare/release-20260914，不进入发布包。

## 发布包与切换步骤

发布包：.release/reviewed-20260914/runtime-source.tar.gz；manifest.json 记录来源、完整文件清单和 SHA256。包不含 .env、上传文件、数据库快照、node_modules、临时工具或生产配置。

正式执行前必须取得用户对生产变更的确认，并再次核对生产文件哈希，防止审核后新增修改被覆盖。

1. 在生产创建带时间戳备份目录，备份源码、现有 compose/override/nginx/.env（敏感文件仅留生产），记录镜像 ID 并建立回滚标签；重新备份数据库和受影响配置表。
2. 验证发布包 SHA256，在独立目录构建；不得将生产上传目录和 .env 拷入构建上下文。
3. 沿用生产 compose、override、Nginx、/data/uploads 挂载；AUTO_SEED=false。后端入口会自动迁移，切换前必须明确迁移已通过预演。
4. 维护窗口替换审核范围源码、构建并切换 backend/frontend，禁止全栈 down，禁止替换生产数据库。
5. 验证健康检查、真实登录、业务数据计数、角色菜单、离职编辑及门户配置入口；记录新镜像 ID、文件清单和发布来源。

## 回滚

- 应用失败时停止新版本，恢复先前 backend/frontend 镜像与源码；数据库新增表和列先保留，不自动执行破坏性 down。
- 配置需回退时，只恢复发布前受影响配置记录，禁止覆盖期间新增业务记录。
- 如数据库确需整体恢复，先将发布前快照恢复到新的隔离数据库验证，再另行确认维护窗口切换；不得对运行库执行 pg_restore --clean。

## 门户上线边界

内部代码包包含门户功能，但独立门户的正式入口、网关、连接器、共享认证及 SMTP 尚未在生产配置。
服务器未发现 Node 宿主机运行时；存在 node:20-alpine 镜像，可沿用容器部署。不能把本机 127.0.0.1:5183/18160 地址写入面向客户的生产页面。
邮件默认关闭，正式发信及外部门户开放需在域名、网络入口、认证配置落实后另行验收；不能把内部系统更新宣称为门户正式上线。

## 验证状态

试用期单测 31/31 通过；迁移预演及定向配置保护验证通过。完整固定回归通过：前端 272 项、后端 390 项、门户 55 项，共 717 项；前后端生产构建通过。

17 项迁移后 10 张受保护业务表原有字段哈希一致。隔离库生产构建启动成功，健康、auth/me、审核列表、薪酬列表 4 项真实 HTTP 查询均为 200/code=0；使用本机临时签名和快照中的有效管理员验证鉴权读取，不属于账号密码登录测试或浏览器端到端测试。首次改密账号被业务接口正确拦截，改用快照中已完成改密的管理员后查询通过。

发布包 818 个文件已解包逐项校验，文本换行统一 LF，shell 入口保留可执行权限；SHA256：709cce2f58c09f6a72846c854927dcedb5a3a5617826465b5cbe01293ac1d6a7。未在生产上传、构建、重启、运行迁移或发送邮件，生产容器切换及独立门户正式部署仍待确认和执行。
