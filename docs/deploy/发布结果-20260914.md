# 2026-09-14 程序同步发布结果

> 最新热修：业务员/组长入职、离职主工单导入权限已恢复，无停机或业务数据写入；见 [业务员导入权限热修-20260914.md](./业务员导入权限热修-20260914.md)。

> 后续更新：同日已完成入职期限/月数输入遗漏修复并再次上线，最新包、验证与回滚信息见 [入职期限字段修复发布-20260914.md](./入职期限字段修复发布-20260914.md)。下文是此前整包同步记录。

用户已明确授权执行程序同步，禁止同步本地测试数据。本次更新固定目标 `192.168.26.195` 的 `/data/apps/work-order-system`，不涉及同机其他系统。

## 发布内容

- 采用主目录已审核工作区，而非仅 HEAD；821 个白名单运行文件，包含各窗口已经承接的修改和本次省外写入口、银行卡条件、试用期、门户及性能修复。
- 最终代码包 `.release/sync-20260914-reviewed/runtime-source.tar.gz`，SHA256 `0094708afcf4c0505dd4fd19544cae62835c7463f9afa05aa40a97a1da6ac947`；文件清单同目录 manifest.json。
- 构建中修复 bcrypt 外网二进制/头文件下载失败：仅 deps 阶段安装 python3/make/g++，使用 `/usr/local` 已带 Node 头文件源码编译。runtime 不复制编译工具，只复制获准的运维修复脚本。
- 前后端均从独立候选目录实际构建 Docker 镜像，未使用开发机 node_modules 或 dist。

## 数据保护

- 生产原始业务数据不导入、不替换：主单 282、子单 1012、客户 222，以本次服务器备份为准，不使用早期快照覆盖新增记录。
- 服务器备份目录 `.releases/reviewed-20260914` 保留 preflight-server.dump、停写后的 cutover-server.dump、源码及配置备份、旧镜像回滚标签；敏感备份仅留服务器受限目录。
- 原有数据库卷、`/data/uploads` 挂载、账号密码、`.env`、compose/override、外层 nginx 配置保留，配置哈希及上传挂载一致；只重建 backend/frontend，nginx 仅重新加载以解析新容器地址，没有全栈 down。
- 迁移 117→134，定向新增结构/必要配置，不运行全量 seed。主单、子单、客户、用户、角色绑定、部门、商社、在职直单、附件元数据、模块负责人共 10 张表原字段摘要前后一致；主单仅排除新增 completion_version 列比较。
- 测试账号、客户、薪资文本及附件只存在独立 ticket_release_* 容器；正式库没有本次测试客户，正式门户受理/账号表未导入本地记录。

## 验证

- 固定回归 `回归测试.ps1 -SkipBuild` 通过 721 项；部署资产定向 5/5；此前前后端本机构建及本次服务器镜像构建通过。
- 服务器最新备份恢复到独立 PostgreSQL 容器演练，17 项迁移成功、10 张表原字段摘要保持，镜像内脚本只读盘点及白名单检查通过。
- 候选镜像真实密码登录、门户浏览器经真实网关/连接器提交文字和 Excel 附件、内部薪酬展示、内部办结与门户结果查询通过，无请求 mock；邮件关闭，未向外部发信。
- 正式后端 healthy；auth/me、审核列表、薪酬列表、客户规则四项读取 200；正式浏览器仪表盘、入职主单、门户审核、薪酬回传、客户配置五页通过。生产验收使用短期签名的既有管理员只读会话，不重置正式密码，不声称验证了正式密码登录。
- 本地证据在 `.tmp_server_compare/release-20260914/`：publish-regression.log、container-browser.json、production-browser.json、production-preservation.json、container-preservation.json。隔离验收容器停止保留，不清除备份。

## 边界与回滚

内部工单系统代码已同步，不等同独立外网客户门户或 SMTP 正式上线；域名、外网网络、网关/连接器及正式邮箱仍需单独配置验收。增减员完整业务办理链没有在本轮重新穷举。

回滚优先使用服务器 `work-order-system-backend:rollback-reviewed-20260914` 和 `work-order-system-frontend:rollback-reviewed-20260914` 标签，仅恢复应用，新增兼容表列保留。配置回退需按备份定向恢复；禁止直接恢复整库覆盖上线后新业务。原始 inspect 含敏感环境，仅保存在服务器 rollback-inspect.private.json。
