# 工单列表详情修复后自检与 QA 复测交接 - 0603

> 负责人：工单列表与详情交互工程师  
> 范围：我的工单、我的待办/已办、团队工单、历史工单、负责子工单列表、子工单详情、社保公积金减员入口的只读自检与 QA 复测交接。  
> 检查工作区：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998`  
> 日期：2026-06-03  
> 性质：**只读交接报告**。本任务不修改业务实现；业务代码修复由前置任务 commit `e4c3338c fix workorder readonly phase1 interactions` 提供。

## 0. 结论

- **工单列表/详情前端交互范围：Targeted GO，可进入 QA 复测。**
- 本报告覆盖 0603 **P0-6 / P0-8 / P0-10** 与 QA NO-GO **B2 / B7**。
- 前端已完成：子工单级展示、团队/历史/后道只读入口、社保公积金未接单/已接单/已完成按钮态、一期模块过滤、社保减员别名入口。
- 仍需后端/QA 端到端确认：真实 `status` / `accepted_at` / `action_permissions` / `module scope` / handler seed 是否与前端按钮态一致；后端必须拒绝越权 API，不能只靠前端隐藏按钮。

---

## 1. 对照 0603 P0-6 / P0-8 / P0-10 的自检清单

| 0603 项 | 已核对页面/入口 | 前端当前结论 | 测试/证据 | 后端依赖 |
|---|---|---|---|---|
| **P0-6 后道只显示自己负责的子工单模块** | `/my-work/pending`、`/my-work/done`、负责子工单列表、`/onboarding/social_insurance`、`/onboarding/social_insurance_resign` | 前端按角色过滤模块：社保专员仅社保增/减；合同专员仅合同；入离职专员仅入职联系/离职材料；数据录入仅增/减报岗；共享负责人为合同+入离职联系 | `MyDispatched/index.test.tsx`、`moduleAccess.test.ts`、`OnboardingModule/index.test.tsx` | 后端 `/dispatched-orders` 仍需按用户 scope 过滤并拒绝无关模块直访 |
| **P0-8 社保公积金未接单可自由改/撤/废** | `/my-dispatched/:id` 子工单详情 | `module=social_insurance/social_insurance_resign/resignation_social_insurance` 且 `status=pending` 且 `accepted_at` 为空时，业务员可见“修改 / 撤回 / 作废” | `MyDispatched/Detail/index.test.tsx`：未接单社保可修改、撤回、作废 | 后端必须允许未接单前业务员直接改/撤/废，并正确返回状态 |
| **P0-8 社保公积金接单/已受理后锁定** | `/my-dispatched/:id` 子工单详情 | `accepted_at` 有值，或 `status=accepted/processing` 时，前端显示告警“社保公积金子工单已接单/已受理”，只显示禁用按钮“修改需后道同意 / 撤回需审批 / 作废需审批” | `MyDispatched/Detail/index.test.tsx`：processing 锁定、`resignation_social_insurance + accepted` 锁定 | 后端必须把接单/受理后状态和 `accepted_at` 真实落库，并拒绝业务员直接调用改/撤/废 API |
| **P0-8 已完成不可直接操作** | `/my-dispatched/:id` 子工单详情、历史工单详情 | 终态 `completed/withdraw_pending/void_pending/void` 不展示业务员直接修改/撤回/作废；已完成节点仅特定后端权限可退回 | `MyDispatched/Detail/index.test.tsx` 覆盖终态不展示直接操作；QA 需补真实 completed 场景 | 后端 action permissions 决定是否显示“退回已完成节点”，并必须兜底 |
| **P0-8 社保反馈不复杂化** | 子工单详情完成弹窗 | 社保增员/减员只用“处理中 / 退回 / 完成”三态，不新增医保/社保/公积金细分状态 | `MyDispatched/Detail/index.tsx` 代码核对；QA 需点开完成弹窗确认 | 后端完成接口需接受并解释同一三态口径 |
| **P0-10 我的工单保持子工单级展示** | `/my-work/initiated`、`/my-work/returned`、`/my-work/pending`、`/my-work/done` | ProTable 使用 `getDispatchedOrdersSafe`，列表按子工单行展示，不回退到主工单行 | `MyDispatched/index.test.tsx`：我发起的子工单、退回子工单、待办/已办子工单 | 后端分页和 scope 需按子工单返回 |
| **P0-10 团队工单只读详情不可操作** | `/my-work/team` → `/my-dispatched/:id?readonly=1&from=team` | 团队工单详情展示“团队工单只读详情”，隐藏接单/完成/退回/修改/撤回/作废 | `TeamDispatched/index.test.tsx`、`MyDispatched/Detail/index.test.tsx` | 后端仍需拒绝业务负责人/组长从 API 直接操作非授权子单 |
| **P0-10 后道我的工单/我的待办只读，操作回负责列表** | `/my-work/pending`、`/my-work/done`、`/my-work/history` → `/my-dispatched/:id?readonly=1&from=my-work` | 我的待办/已办/历史详情显示只读提示，隐藏接单/完成/退回；批量操作栏在只读视图隐藏；真实操作需回左侧负责子工单列表 | `MyDispatched/index.test.tsx`、`HistoryWorkOrders` 代码核对、`MyDispatched/Detail/index.test.tsx` | 后端负责列表仍需返回可操作权限，个人视图不能成为操作入口 |

---

## 2. QA NO-GO B2 / B7 对照

### B2：前端 test 失败 / 工单交互断言不一致

| 子项 | 前端已完成/已核对 | QA 复测点 |
|---|---|---|
| `/work-orders` 动态模块加载异常 | 主工单列表不再默认强制 `orderType=onboarding`；默认可加载入职+离职；一期过滤隐藏 in-service | 业务员/组长打开 `/work-orders`，验证入职、离职均可检索；不出现续签/待遇/社保变更 |
| 业务员/组长入口 | 业务员/组长工具栏保留“新建工单 / 入职导入”，即使动态权限未显式返回 create/import | `yaoyiping`、`shenwenjun` 登录检查 `/work-orders` 工具栏 |
| 旧名清理 | 主工单列表/详情、子工单标签使用 `constants/modules.ts` 新名：劳动合同新签、增员报岗录入、社保公积金增员/减员、离职材料收集等 | UI 搜索不得显示“数据录入 / 社保公积金办理 / 劳动合同签订 / 续签合同 / 社保停保”等旧口径 |
| 一期隐藏在职 | 前台模块组选项只暴露入职/离职；我的工单和团队/历史列表过滤 in-service | 直输或筛选中不应看到 renewal_contract / benefit_apply / social_insurance_change |
| 只读详情断言 | 团队、历史、后道个人视图均带 `readonly=1` 并隐藏按钮 | 按第 4 节复测 |

### B7：社保公积金状态流未端到端验证

| 状态 | 前端按钮态已完成 | 仍依赖后端/API |
|---|---|---|
| 未接单：`pending + accepted_at=null` | 业务员可见并可点击“修改 / 撤回 / 作废”；不显示锁定告警 | 后端需允许直接修改/撤回/作废并返回正确新状态 |
| 已接单/已受理：`accepted_at` 有值或 `status=accepted/processing` | 业务员只见禁用提示按钮：修改需后道同意、撤回需审批、作废需审批；隐藏直接操作按钮 | 后端需拒绝直接调用改/撤/废 API，并提供审批/同意链路状态 |
| 审批中：`withdraw_pending/void_pending` | 业务员不再直接操作；后道/管理员根据权限可见同意/拒绝按钮 | 后端需返回审批态和 `action_permissions`，并完成通过/拒绝后的状态回滚或终结 |
| 已完成：`completed` | 不展示业务员直接修改/撤回/作废/催办；只读查看 | 后端需拒绝终态直接改/撤/废；如需退回已完成节点，必须通过后端授权 |
| 减员别名 | `social_insurance_resign` 与 `resignation_social_insurance` 统一按社保类规则处理 | 后端路由和 moduleCode 返回需统一或兼容别名 |

---

## 3. 页面/入口自检范围

| 页面/入口 | 当前核对结论 | QA 重点 |
|---|---|---|
| `/work-orders` | 业务员/组长可操作入口；后道纯角色会跳转 `/my-dispatched`；默认不强制入职 | 业务员/组长看到入职导入；离职数据可检索；无在职筛选 |
| `/work-orders/:id` | 主工单详情只展示汇总；操作提示去子工单；退回子工单标签使用新名；离职别名显示“离职” | 不再出现旧名“劳动合同签约/数据录入/社保公积金办理” |
| `/my-work/initiated` | 我发起的按子工单行展示 | 同一主工单多个子工单应为多行 |
| `/my-work/returned` | 退回待处理按子工单行展示 | 仅 returned 子工单 |
| `/my-work/pending` | 后道个人待办只读视图；详情带 `readonly=1&from=my-work` | 不显示批量操作栏，不显示接单/完成/退回按钮 |
| `/my-work/done` | 后道个人已办只读视图 | 详情只读，返回个人列表 |
| `/my-work/team` | 团队工单一子单一行；详情带 `readonly=1&from=team` | 业务组长/业务负责人可看不可操作 |
| `/my-work/history` | 历史工单详情带 `readonly=1&from=my-work` | 历史只读，不出现业务操作 |
| `/onboarding/social_insurance` | 傅倩雯负责社保公积金增员的实际操作入口 | 可接单/办理；非社保角色不可见或后端拒绝 |
| `/onboarding/social_insurance_resign` | 前端映射后端 `resignation_social_insurance`，用于社保公积金减员 | 减员数据可见；不因别名查不到 |
| `/my-dispatched/:id` | 子工单详情根据 readonly、角色、状态显示按钮 | 按第 4 节验证按钮态 |

---

## 4. QA 复测步骤

### 4.1 业务员：社保公积金未接单 / 已接单 / 已完成

账号建议：`yaoyiping` 或等价业务员。

1. **未接单社保子工单**
   - 前置：创建或找到社保公积金增员/减员子工单，`status=pending`，`accepted_at=null`。
   - 打开 `/my-work/initiated` 或通知进入对应 `/my-dispatched/:id`。
   - 期望：看到“修改 / 撤回 / 作废”；不出现“社保公积金子工单已接单/已受理”告警；点击按钮应调用对应业务员操作 API。

2. **已接单/已受理社保子工单**
   - 前置：傅倩雯在负责列表 `/onboarding/social_insurance` 或 `/onboarding/social_insurance_resign` 接单。
   - 业务员刷新同一详情。
   - 期望：出现“社保公积金子工单已接单/已受理”；只显示禁用按钮“修改需后道同意 / 撤回需审批 / 作废需审批”；不显示可点击的“修改 / 撤回 / 作废”。

3. **已完成社保子工单**
   - 前置：傅倩雯完成子工单。
   - 业务员再次打开详情。
   - 期望：不出现直接“修改 / 撤回 / 作废 / 催办”；详情仅查看。若需要退回已完成节点，只能走后端授权的主管/管理员流程。

### 4.2 团队工单只读

账号建议：`shenwenjun` 业务组长、`aolei` 业务负责人。

1. 打开 `/my-work/team`。
2. 确认列表按子工单一行一条展示，发起人/员工/模块信息不为空。
3. 点击“只读详情”。
4. 期望 URL 为 `/my-dispatched/:id?readonly=1&from=team`；详情页显示“团队工单只读详情”；不出现接单、完成、退回、修改、撤回、作废。

### 4.3 后道我的工单/待办只读，操作回负责列表

账号建议：傅倩雯、杨纯、毛雅妮、安娜祯、江璐。

1. 打开 `/my-work/pending`。
2. 点击任一详情。
3. 期望 URL 带 `readonly=1&from=my-work`；显示“我的工单只读详情”；不出现接单/完成/退回。
4. 回到左侧负责列表执行操作：
   - 傅倩雯：`/onboarding/social_insurance`、`/onboarding/social_insurance_resign`
   - 杨纯：`/onboarding/contract`
   - 毛雅妮：`/onboarding/onboarding_contact`、`/onboarding/resignation_contact`
   - 安娜祯：`/onboarding/data_entry`、`/onboarding/data_entry_resign`
   - 江璐：合同 + 入职联系 + 离职材料收集合集

### 4.4 历史工单只读

1. 打开 `/my-work/history`。
2. 点击详情。
3. 期望 URL 带 `readonly=1&from=my-work`；不出现接单/完成/退回/修改/撤回/作废。

### 4.5 角色模块过滤

| 角色/账号 | 应可见模块 | 不应可见模块 |
|---|---|---|
| 傅倩雯 / 社保专员 | 社保公积金增员、社保公积金减员 | 入职联系、劳动合同新签、增员报岗录入、减员报岗录入、在职/待遇 |
| 杨纯 / 合同专员 | 劳动合同新签 | 入职联系、报岗录入、社保增减、在职/待遇 |
| 毛雅妮 / 入离职联系 | 入职联系、离职材料收集 | 合同、报岗、社保增减、在职/待遇 |
| 江璐 / 共享负责人 | 劳动合同新签 + 入职联系 + 离职材料收集 | 社保增减、报岗增减、在职/待遇 |
| 安娜祯 / 报岗录入 | 增员报岗录入、减员报岗录入 | 合同、入职联系、社保增减、在职/待遇 |
| 业务负责人 | 仪表盘、团队工单、历史工单 | `/work-orders` 发起/导入、后道负责列表、在职入口 |

---

## 5. 已执行命令与结果

执行目录：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998\frontend`

| 命令 | 结果 |
|---|---|
| `npm test -- --run src/pages/WorkOrders/index.test.tsx src/pages/WorkOrders/Detail/index.test.tsx src/pages/MyDispatched/Detail/index.test.tsx src/utils/moduleAccess.test.ts` | PASS：4 files / 20 tests |
| `npm test -- --run src/pages/MyDispatched/index.test.tsx src/pages/TeamDispatched/index.test.tsx src/pages/OnboardingModule/index.test.tsx` | PASS：3 files / 15 tests |
| `npm run build` | PASS：`tsc -b && vite build` completed |

> 说明：本交接任务未重复跑全量前端 test；最终 integration 阶段仍建议运行 `cd frontend && npm test`。

---

## 6. 前端已闭环 vs 后端依赖

### 前端已闭环

1. 列表默认不强制入职，支持入职+离职一期数据。
2. 前台模块组选项隐藏在职，只暴露入职/离职。
3. 我的工单、团队工单、历史工单、后道个人视图均按只读/可操作入口拆分。
4. 社保公积金未接单/已接单/已完成按钮态已按前端状态完成。
5. 社保增员/减员别名使用同一锁定与三态反馈逻辑。
6. 相关 Vitest 与 build 已通过。

### 后端/API 仍必须验证

1. `/dispatched-orders` 必须按角色 scope 返回模块，不能只依赖前端过滤。
2. 已接单后，后端必须拒绝业务员直接修改/撤回/作废 API。
3. 已完成后，后端必须拒绝直接修改/撤回/作废。
4. 后端必须稳定返回 `status`、`accepted_at`、`action_permissions`。
5. 傅倩雯/杨纯/毛雅妮/江璐/安娜祯的 handler seed 与模块权限需真实库验证。
6. dashboard/菜单全量口径和在职隐藏仍需菜单仪表盘任务与后端任务共同验收。

---

## 7. 剩余风险与 GO/NO-GO

- **本任务交互范围：Targeted GO。** 工单列表/详情、只读态、按钮态、模块过滤已有结构化前端测试与 build 证据。
- **整体验收仍需条件 GO/待 QA 复测。** 需要后端 build/test、前端全量 test、真实账号 E2E，以及社保状态流 API 拒绝越权验证。
- **最大风险**：真实后端数据未返回 `accepted_at` 或 module scope 配置错误时，前端按钮态和角色模块过滤会被数据层破坏；QA 应优先按第 4 节账号矩阵复测。
