# 工单列表详情修复后自检与 QA 复测交接（0603）

> 角色：工单列表与详情交互工程师
> 性质：**只读交接报告**，不修改业务实现，不回滚他人改动。
> 文档路径：`docs/工单列表详情修复后自检与QA复测交接-0603-20260603.md`（本次 readiness / QA 复测的可追踪交付物）
> 检查工作区：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998`
> 当前可见 integration HEAD：`10e84bde`
> 对照范围：0603 **P0-6 / P0-8 / P0-10** + QA NO-GO **B2 / B7**

## 0. 结论

- **工单列表/详情前端交互范围：Targeted GO，可进入 QA 复测。**
- 已核对：我的工单、我的待办/已办、团队工单、历史工单、负责子工单列表、子工单详情、`/onboarding/social_insurance_resign` 减员入口。
- 前端已完成：子工单级展示、团队/历史/后道只读、社保公积金未接单/已接单/已完成按钮态、一期模块过滤、社保减员别名入口。
- **整体验收仍需条件 NO-GO / 待联调**：后端 `scope / status / accepted_at / action_permissions`、API 越权拒绝、以及全量前端/后端测试与真实账号 E2E 继续确认。

## 1. 0603 P0-6 / P0-8 / P0-10 自检对照

| 0603 项 | 已核对页面/入口 | 前端当前结论 | 证据/测试 | 后端依赖 |
|---|---|---|---|---|
| **P0-6 后道账号只显示自己负责的子工单模块** | `/my-work/pending`、`/my-work/done`、负责子工单列表、`/onboarding/social_insurance`、`/onboarding/social_insurance_resign` | 前端按角色过滤模块：社保专员仅社保增/减；合同专员仅合同；入离职专员仅入职联系/离职材料；数据录入仅增/减报岗；共享负责人为合同 + 入离职联系 | `src/utils/moduleAccess.test.ts`、`src/pages/MyDispatched/index.test.tsx`、`src/pages/OnboardingModule/index.test.tsx` | 后端 `/dispatched-orders` 必须按用户 scope 裁剪并拒绝无关模块直访 |
| **P0-8 社保公积金未接单可自由改/撤/废** | `/my-dispatched/:id` 子工单详情 | `module=social_insurance / social_insurance_resign / resignation_social_insurance` 且 `status=pending`、`accepted_at=null` 时，业务员可见“修改 / 撤回 / 作废” | `src/pages/MyDispatched/Detail/index.test.tsx` 覆盖未接单社保可改/撤/废 | 后端需允许未接单前直接改/撤/废并返回正确状态 |
| **P0-8 接单/已受理后锁定** | `/my-dispatched/:id` 子工单详情 | `accepted_at` 有值，或 `status=accepted/processing` 时，业务员只见禁用提示按钮：`修改需后道同意 / 撤回需审批 / 作废需审批` | `src/pages/MyDispatched/Detail/index.test.tsx` 覆盖锁定态 | 后端需真实落库 `accepted_at` / `status`，并拒绝越权改/撤/废 API |
| **P0-8 已完成不可直接操作** | `/my-dispatched/:id` 子工单详情、历史工单详情 | `completed / withdraw_pending / void_pending / void` 等终态不展示业务员直接操作 | `src/pages/MyDispatched/Detail/index.test.tsx` 覆盖终态不展示直接操作 | 后端必须拒绝终态直接改/撤/废；如需退回已完成节点只能走授权流程 |
| **P0-8 社保反馈保持简单** | 子工单完成弹窗 | 社保增员/减员只保留“处理中 / 退回 / 完成”三态，不新增医保/社保/公积金细分反馈状态 | `src/pages/MyDispatched/Detail/index.tsx` 代码核对 | 后端完成接口需接受同一三态口径 |
| **P0-10 我的工单继续按子工单级展示** | `/my-work/initiated`、`/my-work/returned`、`/my-work/pending`、`/my-work/done` | 列表按子工单行展示，不回退到主工单级 | `src/pages/MyDispatched/index.test.tsx` | 后端分页和 scope 需按子工单返回 |
| **P0-10 团队工单可打开只读详情但不可操作** | `/my-work/team` → `/my-dispatched/:id?readonly=1&from=team` | 团队工单详情显示只读提示，隐藏接单/完成/退回/修改/撤回/作废 | `src/pages/TeamDispatched/index.test.tsx`、`src/pages/MyDispatched/Detail/index.test.tsx` | 后端仍需拒绝业务负责人/组长从 API 直接操作非授权子单 |
| **P0-10 后道在我的工单/我的待办中只读，操作回负责列表** | `/my-work/pending`、`/my-work/done`、`/my-work/history` | 个人视图详情统一加 `readonly=1&from=my-work`，只读提示展示，返回个人列表 | `src/pages/MyDispatched/index.test.tsx`、`src/pages/MyDispatched/Detail/index.test.tsx` | 个人视图不能成为操作入口，后端负责列表仍需返回可操作权限 |

## 2. QA NO-GO B2 / B7 对照

### B2：业务员/业务组长 `/work-orders` UI 动态模块加载异常

| 子项 | 前端已完成/已核对 | QA 复测点 |
|---|---|---|
| 主列表默认口径 | `/work-orders` 不再默认强制只查入职，业务员/组长可看到入职 + 离职一期模块 | 业务员/组长打开 `/work-orders`，确认入职导入存在，离职可正常加载 |
| 一期隐藏在职 | 在职模块不进入一期前台选项、看板卡片、后道列表和仪表盘统计 | 不应看到在职菜单/卡片/统计/后道列表 |
| 旧名清理 | 子工单与详情统一使用新名称：入职联系、劳动合同新签、增员报岗录入、社保公积金增员、劳动合同续签、待遇申报、社保公积金变更、离职材料收集、减员报岗录入、社保公积金减员 | UI 搜索与标签不得再显示“数据录入 / 社保公积金办理 / 劳动合同签订 / 续签合同”等旧口径 |
| 主/子工单入口 | 业务员/业务组长可见“新建工单 / 入职导入”兜底入口 | 检查 `/work-orders` 工具栏入口是否仍可用 |

### B7：社保公积金状态流 E2E 未完成放行验证

| 状态 | 前端按钮态已完成 | 仍依赖后端/API |
|---|---|---|
| 未接单：`pending + accepted_at=null` | 业务员可见并可点击“修改 / 撤回 / 作废” | 后端必须允许直接改/撤/废并返回正确新状态 |
| 已接单/已受理：`accepted_at` 有值或 `status=accepted/processing` | 只显示禁用提示按钮：`修改需后道同意 / 撤回需审批 / 作废需审批` | 后端必须拒绝业务员直接调用改/撤/废 API，并提供审批链路 |
| 审批中：`withdraw_pending / void_pending` | 只读，不再提供业务员直接操作 | 后端需返回审批态与后续通过/拒绝结果 |
| 已完成：`completed` | 不展示业务员直接修改/撤回/作废 | 后端必须拒绝终态直接操作 |
| 减员别名 | `social_insurance_resign` 与 `resignation_social_insurance` 前端按同一社保规则处理 | 后端路由/模块码必须兼容或统一 |

## 3. 已核对页面与入口

| 页面 / 入口 | 当前结论 | QA 重点 |
|---|---|---|
| `/work-orders` | 业务员/组长入口保留；后道纯角色应转到个人视图 | 入口与模块加载是否符合角色 |
| `/work-orders/:id` | 主工单详情只展示汇总信息，旧名已清理 | 不再出现旧模块名与在职入口 |
| `/my-work/initiated` | 我的工单按子工单级展示 | 一条子工单一条数据 |
| `/my-work/returned` | 退回列表按子工单级展示 | 仅展示退回子工单 |
| `/my-work/pending` | 后道我的待办只读 | 点击详情应回负责子工单列表操作 |
| `/my-work/done` | 后道我的已办只读 | 不允许直接业务操作 |
| `/my-work/team` | 团队工单只读详情 | 业务组长/负责人只看不可操作 |
| `/my-work/history` | 历史工单只读详情 | 仅查看，不可操作 |
| `/my-dispatched/:id` | 子工单详情按 readonly / status / accepted_at 控制按钮 | 重点核对按钮态 |
| `/onboarding/social_insurance` | 社保公积金增员入口 | 傅倩雯应只看到自己负责模块 |
| `/onboarding/social_insurance_resign` | 社保公积金减员入口，已对齐后端别名 | 需确认减员可见且不空白 |

## 4. QA 复测步骤

### 4.1 业务员：社保公积金未接单 / 已接单 / 已完成

账号建议：`yaoyiping` 或同权限业务员。

1. 打开 `/my-work/initiated` 或通知进入社保公积金子工单详情。
2. **未接单**：确认 `status=pending` 且 `accepted_at=null`，应看到“修改 / 撤回 / 作废”。
3. **已接单/已受理**：让傅倩雯在负责列表接单后刷新详情，应出现锁定提示，只剩禁用按钮“修改需后道同意 / 撤回需审批 / 作废需审批”。
4. **已完成**：完成后再次打开，应只保留查看，不出现直接操作按钮。

### 4.2 团队工单：只读不可操作

账号建议：`shenwenjun` 业务组长、`aolei` 业务负责人。

1. 打开 `/my-work/team`。
2. 确认列表按子工单一行一条展示。
3. 点击“只读详情”。
4. 期望 URL 为 `/my-dispatched/:id?readonly=1&from=team`，页面显示团队只读提示，不出现接单/完成/退回/修改/撤回/作废。

### 4.3 后道：我的工单 / 我的待办只读，操作回负责列表

账号建议：`fuqianwen`、`yangchun`、`maoyani`、`annazhen`、`jianglu`。

1. 打开 `/my-work/pending`。
2. 点击任一详情。
3. 期望 URL 带 `readonly=1&from=my-work`，显示“我的工单只读详情”，不出现接单/完成/退回。
4. 需要实际操作时，返回左侧负责列表：
   - 傅倩雯：`/onboarding/social_insurance`、`/onboarding/social_insurance_resign`
   - 杨纯：劳动合同新签/续签
   - 毛雅妮：入职联系/离职材料收集
   - 安娜祯：增员报岗录入/减员报岗录入
   - 江璐：杨纯 + 毛雅妮合集

### 4.4 角色模块过滤

| 角色 / 账号 | 应可见模块 | 不应可见模块 |
|---|---|---|
| 傅倩雯 / 社保专员 | 社保公积金增员、社保公积金减员 | 入职联系、劳动合同新签、增员报岗录入、减员报岗录入、在职、待遇、离职联系等无关模块 |
| 杨纯 / 合同专员 | 劳动合同新签、劳动合同续签 | 入职联系、报岗录入、社保增减、在职、待遇 |
| 毛雅妮 / 入离职联系 | 入职联系、离职材料收集 | 合同、报岗、社保增减、在职、待遇 |
| 江璐 / 共享负责人 | 劳动合同新签 + 劳动合同续签 + 入职联系 + 离职材料收集 | 社保增减、报岗、在职、待遇 |
| 安娜祯 / 报岗录入 | 增员报岗录入、减员报岗录入 | 合同、入职联系、社保增减、在职、待遇 |
| 业务负责人 | 仪表盘、团队工单、历史工单 | `/work-orders` 发起/导入、后道负责列表、在职入口 |

## 5. 前端已闭环 vs 后端依赖

### 前端已闭环

1. `/work-orders` 一期仅暴露入职/离职口径，保留业务员/组长的“新建工单 / 入职导入”。
2. 我的工单继续按子工单级展示，不回退主工单级。
3. 团队工单、历史工单、后道个人视图均为只读。
4. 社保公积金未接单 / 已接单 / 已完成按钮态已按前端状态完成。
5. `social_insurance_resign` 与 `resignation_social_insurance` 按同一锁定规则处理。
6. 相关 Vitest 与 build 已通过（见下一节）。

### 仍依赖后端/API

1. `/dispatched-orders` 必须按角色 scope 返回模块，不能只靠前端隐藏。
2. 已接单后后端必须拒绝业务员直接修改/撤回/作废 API。
3. 已完成后后端必须拒绝直接修改/撤回/作废。
4. 后端必须稳定返回 `status`、`accepted_at`、`action_permissions`。
5. 角色 handler seed / module scope / route alias 需真实库验证。

## 6. 已执行命令与结果

> 说明：以下为前序已执行的 targeted 自检与 build 结果；本轮只做了只读复核，不重复跑长耗时命令，避免 300 秒中断。

| 命令 | 结果 |
|---|---|
| `npm test -- --run src/pages/WorkOrders/index.test.tsx src/pages/WorkOrders/Detail/index.test.tsx src/pages/MyDispatched/Detail/index.test.tsx src/utils/moduleAccess.test.ts` | PASS：4 files / 20 tests |
| `npm test -- --run src/pages/MyDispatched/index.test.tsx src/pages/TeamDispatched/index.test.tsx src/pages/OnboardingModule/index.test.tsx` | PASS：3 files / 15 tests |
| `npm run build` | PASS：`tsc -b && vite build` completed |

## 7. 剩余风险与 GO / NO-GO

- **本任务交互范围：Targeted GO。** 工单列表/详情交互、只读态、按钮态、模块过滤已具备交接证据。
- **整体验收：条件 NO-GO。** 仍需后端 build/test、前端全量 test、真实账号 E2E、社保状态流 API 越权拒绝确认。
- **最大风险**：后端若未正确返回 `accepted_at` 或 module scope，前端按钮态和角色过滤会被数据层破坏；QA 应优先按上面的角色矩阵复测。
