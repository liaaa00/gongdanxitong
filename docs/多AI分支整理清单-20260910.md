# 多 AI 分支与工作区整理清单

盘点日期：2026-09-10。最终验收目录按用户指定为 `D:/AI/SpeceAppDate/工单系统`。

## 当前情况

多 AI 改动既存在于独立分支、独立工作树，也存在于尚未提交的文件中。分支是提交历史，工作树是实际编辑目录；未提交的文件不会自动进入分支提交，也不会自动进入其他目录。

- 主目录当前分支为 `main`，提交为 `194b460`（2026-09-02）。9 月 2 日之后的工作区改动不由此提交完整代表。
- 共 41 个本地分支，9 个分支的提交已被 main 包含，32 个未被完整包含；后者不能直接解释为 32 份遗漏功能，人工移植和等价提交可能未保留合并关系。
- 共 22 个已注册工作树，均能访问；盘点时 12 个有未提交修改。
- 另有 4 份 Git stash 暂存备份，需要保留并纳入后续排查，不能直接批量恢复。
- 分支和工作树只隔离代码，不自动隔离数据库：已核实主项目和 `work-order-feature` 的数据库配置均指向 `127.0.0.1:5433/ticket_system`。旧分支不能未经核对直接对同一数据库运行迁移或种子。
- 仅统计 `backend/src`、`backend/test`、`frontend/src`、`customer-portal` 和 `docs`，D 盘有 63 个已跟踪文件修改和 94 个新增文件；依赖、构建、缓存、日志和上传文件不计入。数字是盘点时快照，后续编辑会改变。

## 重点目录

| 目录 | 分支 / 提交 | 处理意见 |
|---|---|---|
| `D:/AI/SpeceAppDate/工单系统` | `main` / `194b460` | 当前统一验收版本，先保护未提交源码，再按功能整理提交 |
| `C:/Users/Asus/.ao/dev/data/worktrees/project-b21f2d21/project-b21f2d21-8` | `ao/dev/project-b21f2d21-8/portal-boundary` / `3156e98` | 门户历史工作树，6 个独有提交；逐功能核对，不覆盖 D 盘 |
| `C:/Users/Asus/.ao/dev/data/worktrees/project-b21f2d21/project-b21f2d21-6` | `ao/dev/project-b21f2d21-6/root` / `84c6255` | 干净，提交已包含于 main，暂保留 |
| `C:/Users/Asus/.ao/dev/data/worktrees/project-b21f2d21/orchestrator/project-b21f-orchestrator` | `ao/dev/project-b21f-orchestrator` / `84c6255` | 干净，提交已包含于 main，暂保留 |
| `D:/AI/SpeceAppDate/work-order-feature` | `feat/resignation-cert` / `0c36418` | 旧权限工作，存在未提交修改；与当前权限中心比对后再判断 |
| 主目录内 `.spectrai-worktrees` | 多个历史 agent/integration 分支 | 派单、在职、省外、测试和设计稿残留，分模块核对 |
| 主目录内 `.release`、`.tmp_server_sync` | 历史发布及快照分支 | 保留作为部署来源证据，不作为最新开发版本 |

## 门户已经承接到什么程度

C 盘 `-8` 的独有提交和未提交变化涉及的 74 个业务源码、测试、门户及文档文件，在 D 盘均存在；盘点时 24 个内容完全一致、50 个内容不同。D 盘还有后续的 `employee_changes` 权限合并、规则完成度和 9 月 10 日数据库迁移。

这证明门户文件已承接并继续修改，但文件存在不等于每个功能语义都已完整合并。对内容不同的文件，应按当前业务规则和测试验证，不能用 C 盘整文件覆盖 D 盘。

`work-order-feature` 中有 15 个 D 盘没有同名文件的源码，涉及路由权限、字段权限元数据/校验/模板和权限帮助组件；可能是已被新方案替代的旧实现，不能仅凭“主目录没有同名文件”直接移植。

## 建议的整理顺序

1. 为 Git 提交历史及有改动的工作树建立本机快照，包含已暂存、未暂存和新增源码，排除依赖、构建、日志、缓存、上传和环境密钥。
2. 固定 D 盘为验收目录；同一个目录避免多个 AI 同时修改同一文件。
3. 按“已承接、确实缺失、旧方案重复、待核对”列功能清单，优先核对门户 `-8`，其次核对旧权限目录，再核对其他业务工作树及 stash。
4. 只补入确实缺失且仍符合当前业务规则的代码，小批修改并执行相关测试与根目录回归。
5. 在 D 盘形成按功能区分的正式提交，记录来源分支和验证结果。提交时只纳入已核对内容，不能直接 `git add .`。
6. 旧分支和目录继续保留；清理是单独操作，待确认全部成果已保护后再决定。

初次盘点完成了数据库恢复和权限迁移；后续按下方章节完成门户、旧权限方案及历史工作树的语义抽查，没有批量重放旧提交、切换或清理分支。

## 门户与权限归拢结果（2026-09-10）

- C 盘 `-8` 的 `a04c5b3/cb7aec9/878f991/4a45206/155771b/3156e98` 六项门户功能由 D 盘承接，包括门户交互、连接器、会议业务口径、办结邮件队列、投递重试、结果字段与回写监控；D 盘另有 UUID 批量规则、历史带入、真实账号认证与两组授权的后续实现，不再整文件覆盖。
- 本次修复了按全局完成度拦截单业务账号、规则仓储为测试跳过校验、停用/收权被缺失规则阻断、薪资受理依赖不存在适配器、连接器要求员工 JWT 和覆盖失败状态等真实问题；历史带入严格要求有效提交时间。
- `work-order-feature` 的旧路由权限表、过期权限模板和未引用帮助组件不移植。仅补入仍适用的批量字段权限冲突预校验，事务写入前整体拒绝冲突；原业务范围隔离保留。
- 本机真实数据库与浏览器已验证 Excel UUID 模板、成功/失败明细、历史规则白名单和人工优先、账号登录/首次改密/会话失效/授权隔离、薪资受理与内部办结回传。原有客户、主工单、门户账号和规则内容哈希保持不变，只增加标记为“本机门户演示”的验证资料。
- 最终运行位置为 D 盘：内部系统 `http://127.0.0.1:5182/`，客户门户 `http://127.0.0.1:5183/`，后端 `3010`，网关 `18082`，数据库 `5433`。原 `3001` 为另一应用的 daemon，未停止或占用。
- 固定回归含前后端构建通过：前端 238、后端 187、门户 32 项。历史业务另有 6 套、101 项定向验证。SMTP 按用户要求后配，当前邮件只持久化排队，不宣称真实投递；自动薪资提醒调度仍不属于本轮已验证功能。
- 本清单对旧工作树按功能归类，不能解读为 41 个分支的每一行都已合并。下方残留事项继续留档，原 refs、工作树和 stash 全部保留。

## 已完成的安全快照

快照目录：`D:/AI/SpeceAppDate/work-order-backups/ai-branch-audit-20260910-144207`。

- Git bundle 保存 122 个现有 refs（含 stash），并单独保存无分支引用的发布 HEAD；两个 bundle 校验通过。
- 12 个有修改的工作树共保存 402 个正式源码、测试、文档、配置及启动文件，另有已暂存/未暂存 patch、相对路径、排除原因和 SHA-256 清单；449 个备份产物哈希验证通过。
- 文件快照排除真实环境密钥、数据库、依赖、构建、日志、缓存、上传及临时内容。完整 Git bundle 原样保留原有提交历史，包括历史上已提交的旧数据库备份对象；只保存在本机。
- 源目录的 refs、HEAD、索引未改变；捕获期间所选文件无内容漂移。快照不包含所有被忽略/被排除的本地文件，也不包含快照完成后的编辑。

## 本机 PostgreSQL

本项目使用便携版 PostgreSQL 16：程序为 `D:/pgsql16portable/pgsql/bin/pg_ctl.exe`，原数据目录为 `D:/pgsql16portable/data`，连接为 `127.0.0.1:5433/ticket_system`，不依赖 Docker Desktop。

2026-09-10 已启动原实例；备份后执行唯一待执行迁移 `MergePortalEmployeePermissions20260910100000`。启动和迁移均未运行 seed。

当前数据库备份：`D:/pgsql16portable/backups/ticket_system_before_portal_permissions_2026-09-10T06-34-08-620Z.dump`，已通过 `pg_restore --list` 检查。迁移前后客户 58、主工单 24、门户账号 1、客户规则 11；账号权限已变为 `employee_changes`/`salary`，待执行迁移为零。固定回归 `回归测试.ps1 -SkipBuild` 通过：前端 230、后端 138、门户 30 项。

## 历史工作树语义审计（2026-09-10，独立审计章节）

本节覆盖 `.spectrai-worktrees`、`.release`、`.tmp_server_sync` 及相关旧 refs；门户 `-8` 和 `work-order-feature` 的专项结论由其他章节负责。本次只追加审计记录，没有改动业务代码、迁移、Git 分支或索引，也没有启动旧工作树、执行旧 seed、恢复 stash 或删除文件。

审计依据为前述快照中的 9 份历史脏工作树清单、当前 D 盘实际文件、代表性提交 diff，以及当前回归规则第 13.1、18、19、21、23、26、34 节。这 9 份工作树的 111 个已备份正式文件中，34 个在 D 盘没有同名路径，7 个内容相同，70 个内容不同；“没有同名路径”逐项区分为旧架构、测试草稿和文档，不等于缺失功能。其余 refs 做了正式源码/测试路径比对，并对下表变化核对实现；本节不声称证明每一个旧 commit、每一行旧差异都已合并。

### 发布与部署分支

| 来源 | 证据 | 处置结论 |
|---|---|---|
| `deploy/payroll-bank-fix-20260817` 的 `41025f5` | 该提交给 `backend/Dockerfile` 增加 `COPY --from=build /app/scripts ./scripts`；审计时主项目 runtime 阶段缺少这一行，而 `backend/package.json` 仍提供 `repair:onboarding-contact`，指向现有 `scripts/repair-onboarding-contact-bank-orders.ts`。构建阶段的 `COPY . .` 不会自动把 scripts 带进 runtime 阶段。 | **确认存在仍适用的打包遗漏**：需要单独补入 scripts 的 runtime 拷贝，并检查镜像内容；不需要恢复整个部署分支。本审计没有运行修复脚本或修改 Dockerfile。 |
| 同分支 `e3a76df` 的薪酬银行卡校验 | 当前 `payroll-bank-card.ts` 有四项完整性检查；`onboarding-dispatch.helper.ts` 仅在 `need_onboarding_contact` 为“是”时派入职联系，始终生成工资卡记录；单条/导入均调用创建者银行卡缺失检查。`work-order-validation.service.spec.ts`、`import.service.spec.ts`已有“集约收集否则四项必填、是则可空”的断言。 | 后端业务已经承接。旧 `conditionalRules.test.ts` 使用“需要工资单且不集约收集”的 AND 条件，第 34 节已不以是否需要工资单作为资格条件，不能原样恢复旧测试；**当前 `frontend/src/pages/WorkOrders/New/index.tsx` 的同一 AND 条件也仍残留，应按现行规则修正前端提示和校验**，见下面核查项。 |
| `.release/province-handler-fix` 的 `f2d3f643` | 当前 `province-handler.seed.ts` 已包含 `provinceHandlerConfigScope`、`provinceHandlerAccountScope`，分别按配置范围保存、按省外账号范围查人；`province-handler-seed.spec.ts` 保留对应四个参数化断言。 | 该提交的配置范围修复已承接；不重复移植或再执行旧 seed。该 detached HEAD 已另有 bundle 保护。 |
| `deploy/renewal-legacy-stock-20260819` 的 `6c58f58` | 当前 `in-service-orders.service.ts` 的 `resolveRenewalContext` 校验启用部门；历史命中继承部门，无历史要求明确部门并写 `legacy_stock`；`batchCreateRenewals` 预校验同客户证件号重复；对应服务测试保留历史/存量/缺部门断言。 | 续签存量导入功能已承接。保留当前前端和后来增加的流程权限，不用旧文件覆盖。 |
| `.tmp_server_sync/compat-worktree` / `deploy/contract-detail-compat-20260728` 的 `c6bd491` | 旧提交为合同创建者编辑引入 `assertCreatorFieldsEditable` 和详情模板字段；当前同名方法仍按合同详情模板取允许字段，且后续扩展到动态字段、离职证明和业务范围。 | 合同模板编辑校验已承接并扩展；不重复移植旧方法。 |
| `.release/full-sync-20260819` 的 `94946eb5` | `git rev-list --count main..94946eb5` 为 0。 | 该 HEAD 已在 main 的提交历史内；保留为发布证据，不作为新开发版本。 |
| `.tmp_server_sync/primary-worktree` / `sync/prod-snapshot-20260730-151038` 的 `2d87a61` | 该生产快照含 105 个变化文件；当前已有 7 月 29 日直单/业务范围迁移、`in_service_orders`、`backend/src/assets/certificates` 的在职/收入证明模板及后来版本的在职表单。 | 代表性的直单、范围隔离、证明能力已承接。此分支是整站旧快照，不能据其“独有 46 提交”整体覆盖主项目；尚未逐提交逐行业务证明。 |

### 历史 AI 工作树与同族 refs

表中工作树简称均相对主项目的 `.spectrai-worktrees/`；准确路径和原 SHA 在安全快照 `worktrees.json` 与各目录 `manifest.json` 中保留。

| 工作树 / refs | 已核对的旧内容 | 当前承接位置及结论 |
|---|---|---|
| `agent-0a3750bd-ms20era9`，同族 `worktree/agent-0a3750bd`、`backup/team-366f…/1785084427728` | 旧 `in-service-work-order.service.ts` 通过 `WorkOrderService.createDraft` 创建在职主单；另有 `ProvinceHandler` 第二张映射表、旧迁移、旧控制器。 | 当前 `in-service-orders` 使用独立直单；第 19、21 节禁止恢复在职主单拆分，省份映射用现有 `module_handlers` 命名空间。缺失的旧路径属于被后续规则替代的架构，不补回旧表/旧主单控制器。 |
| `agent-24c73a33-ms20etsw`，同族 `worktree/agent-24c73a33` | 旧 `pages/InService/*`、`inServiceWorkOrders.ts`、省外详情及旧枚举；与上一行共用旧基线。 | 当前 `pages/InServiceOrders/*`、`services/inServiceOrders.ts`、`constants/inService.ts` 与 `/out-of-province/*` 路由承接新建、列表、详情和独立流程。旧 `/out-of-province/orders*` 书签已有重定向；不恢复旧页面或改回旧菜单。 |
| `agent-26c9fa9b-ms218kvl` | 旧迁移给 `ModuleHandler` 增加 businessType/province；`province-handler-in-service.seed.ts` 的映射数组为空，缺账号时直接抛错。 | 当前 `province-handler.seed.ts` 和 `HandlerPickerService.pickProvinceMapping` 使用 27 省、Sheet4/Sheet5 命名空间、活动主办过滤，无映射返回待配置；不补回空 seed、旧列方案或遇单个缺账号中断整批的行为。 |
| `agent-4a94a980-ms2tfoq5`（detached）、`team/aac039a0-…/integration` 的 `c0358af` / `0d3fd74` | 旧省外主单/子单复用、`out-of-province-import.dto.ts`，以及范围隔离和社保反馈。 | 当前 `WorkOrderImportService.writeOutOfProvinceOrder` 转入 `InServiceOrdersService.create`；Sheet5 省份匹配及三项反馈完成门禁有现有测试。旧主单架构被第 21、26 节替代；范围隔离/反馈意图已承接，不能重放旧主单创建链。 |
| `agent-66293863-ms249zky` | 4 个旧测试和报告；例如 `in-service-state-machine.spec.ts` 在测试里自行定义状态图/分类数组，使用旧 `DispatchedOrderStatus.PENDING_INFO`，未调用正式状态机。 | 作为历史测试草稿保留。当前 `in-service-flow.spec.ts`、`in-service-orders.spec.ts`、`province-handler-dispatch.spec.ts` 已直接验证正式实现；不把自造数组或旧枚举测试当成功能遗漏补回。 |
| `agent-1ea8a2cb-ms218fog`、`agent-cbe24e04-ms21k1x1`、`agent-d0ba9cb6-ms218s9r` | 未提交差异分别只有架构报告、在职架构文档、QA 计划。 | 文档残留已入快照；属于历史参考，不是主项目缺少的运行功能，不覆盖现行回归规则。 |
| `integrations/fa5bffae-…` 的未提交变更 | 唯一变更为生成的 `frontend/public/mockServiceWorker.js`；该副本标记 MSW 2.15.0，主项目文件标记 2.7.0。 | 依赖生成物差异，不单独复制旧目录的 worker；将来若升级依赖，应依据主项目锁文件重新生成并验证。 |
| `team/fa5bffae-…/integration`、`worktree/agent-0577d9ef` 的已提交旧实现 | `sheet4-province-mapping.seed.ts` 含 `user-beijing-1` 等占位 ID、合并省份字符串，并包含当前不使用的浙江映射；旧在职 DTO/页面依赖主单架构。 | 当前真实账号映射与直单模块已取代；第 18 节禁止虚构账号、跨表兜底，不能重新引入占位 seed。 |
| `team/d97c7fc8-…/integration` 的 `94f6620` 及旧 `in-service-business` | `in-service-management.spec.ts` 自述“测试框架”，大量 `it` 只有注释；旧 service 直接保存 `WorkOrderStatus.DRAFT`，使用临时 `Date.now()` 编号。 | 旧主单实现和未完成测试框架，均不按“缺同名文件”恢复；当前直单和真实服务测试为准。 |
| `worktree/agent-014fecc0` 的 `01da211` | 独有顶层变化只有 `docs/阶段2-在职管理三级分类与状态机设计方案.md`。 | 设计稿证据，保留 refs；不将旧分类、审批节点覆盖现有第 19、26 节。 |

### 其他 refs、剩余核查边界

- `fix/contract-term-conditional-required` 的 `fa6fa63`：无固定期限时合同期限/终止日可空的规则已在第 13.1、22 节及当前入职导入、续签表单测试中落实。旧 commit 同时混入大量历史文档和启动脚本，不能为了这一规则整提交重放。
- `master` / `.spectrai-worktrees/deploy-ba48efd-20260727`、`backup/main-pre-main-sync-*`、其余 `backup/team-*`、`worktree/agent-*`：保留为历史检查点及集成来源。缺失正式路径主要落在上表的旧主单、旧 ProvinceHandler 表、测试草稿；未发现第二套应直接取代当前直单和派单底座的完成实现。这里不是“所有旧提交无遗漏”的结论。
- `backup/remote-main-20260625` 与 main **没有共同祖先**，三点 diff 不适用；本次仅做两端正式路径比对，不能把它作为普通待合并分支。后续若需追查该备份中的某个明确需求，应按功能比较，而不是强行合并两段历史。
- 4 份 stash 已受 Git bundle 保护；本节没有将其逐个恢复，也不据分支审计推断 stash 内全部改动已经承接。
- **另有旧接口残留需单独处理**：虽然当前前端省外业务/批量导入走直单，`AppModule` 仍注册 `OutOfProvinceOrdersModule`，`POST /out-of-province-orders` 对应旧 service 仍调用 `WorkOrderService.createDraft`。这属于当前代码中的历史写入口收口核查项，不能以“新页面已替换”推断旧接口已关闭；应核实调用方和 HTTP 行为后按现行规则处理。本审计未调用该写接口，也未删旧模块。
- **前后端条件仍有一处不一致**：`frontend/src/pages/WorkOrders/New/index.tsx` 的 `CONDITIONAL_REQUIRED_BY_TYPE.onboarding` 仍将银行卡四项必填写为 `need_payroll_slip=是 AND need_onboarding_contact=否`，而当前后端 `requiresCreatorPayrollBankCardFields` 只判断不集约收集。用户选择“不需要工资单、不集约收集”时，后端会阻止缺银行卡资料提交，前端这个显式条件却不触发。应收敛为只判断不集约收集，并验证“否/否”和“是/否”都必填、“集约收集是”可空；这是当前规则同步问题，不应通过恢复旧测试把 AND 条件固定下来。

### 本轮验证

在 D 盘后端运行了以下 6 个现有定向测试文件，**6 suites / 101 tests 全部通过**：`province-handler-seed.spec.ts`、`province-handler-dispatch.spec.ts`、`in-service-orders.spec.ts`、`in-service-flow.spec.ts`、`work-order-import.service.spec.ts`、`work-order-validation.service.spec.ts`。这验证了所抽查的现行派单、直单、流程快照、续签历史/存量和创建者银行卡门禁；没有构建 Docker 镜像，也没有验证旧接口写入行为，不把这些未执行项目写成通过。

本节发现的明确可补入项是 `41025f5` 的 Docker runtime scripts 拷贝；此外记录了当前前端银行卡条件未同步、旧省外写接口仍注册两项核查问题。其余不应直接合并的旧实现已分别记录。所有旧分支和文件继续保留。


## 2026-09-14 多窗口发布范围复核

- 当前仍为 22 个注册工作树、41 个本地分支和 4 个 stash；对照 9 月 10 日 refs 备份，只有 main 的引用发生变化。
- .ao 与 .springai 下 project-b21f2d21 门户目录经 samefile 检查为同一个目录别名，不能重复计算为两套修改。
- 主目录之外 11 份历史脏工作树备份中的 197 个文件逐项 SHA256 一致；本次业务源码/测试/门户/文档扫描没有发现备份之外新增的脏文件路径。
- 近期改动集中于主目录。现有 818 文件发布清单与当前主目录逐项校验无漂移，包含当时落在主目录的其他窗口运行代码改动；不能把“只在本轮提交中”作为发布范围。
- 4 份 stash 引用未变化，均为 7—8 月历史暂存；本次只检查文件清单，不宣称其中每一项业务语义已合并。
- 历史门户、权限、派单工作树按上文既有归拢结论保留；未整分支覆盖、未切换分支、未恢复 stash、未清理目录。
- 本次确认没有发现新增的独立近期修复来源，不代表所有历史分支逐行无遗漏；原清单已记录的旧接口、模板及权限遗留事项仍需作为发布范围审核项。
- 发布继续暂停，先前回归结果仅对应当时主目录版本。本轮是只读来源审计和文档留痕，没有修改业务源码或生产，不重复执行代码回归。
- 证据：.tmp_server_compare/multi-window-audit.json、multi-window-snapshot-diff.json；目录发现范围为项目注册工作树及 .codex/worktrees、.ao/.springai 的项目工作树和 D:/AI/SpeceAppDate 的有限层级目录，不代表扫描了电脑所有磁盘或能识别每个文件的 AI 作者。
