# A–H 协作契约 — 0602 会议 / 06-03 上午 9 点演示

> 出品：架构与需求统筹
> 日期：2026-06-02
> 配套文档：`docs/验收矩阵-0602演示-20260602.md`（裁决基准），本文件是**可执行协作契约**——每项拆为：业务口径 / 后端验收点 / 前端验收点 / 测试场景 / 优先级 / 风险。
>
> 优先级定义：P0=演示阻断，不过则 Demo 失败；P1=演示必现但可降级演示；P2=可延后。
>
> 硬约束：后端权限必须兜底（不能只隐藏前端）；不回滚他人改动；前后端角色码经 `roles.ts` 归一化对齐；改 bug 必补回归测试；禁止使用豆包。

---

## 人员–模块分工矩阵（权威，所有项以此为准）

| 人员 | username | 后端角色码(seed) | 归一码(前端) | 负责 moduleCode | 业务名称 |
|---|---|---|---|---|---|
| 杨纯 | yangchun | contract_specialist | labor_contract_member | `contract`、`renewal_contract` | 入职劳动合同签订 / 在职续签 |
| 毛雅妮 | maoyani | onboarding_specialist | onboarding_resignation_member | `onboarding_contact`、`resignation_contact`、`resignation_cert` | 入职联系 / 离职材料收集 |
| 江璐 | jianglu | shared_leader(+contract_specialist+onboarding_specialist) | shared_team_owner | 杨纯 ∪ 毛雅妮（5模块合集/备份） | 两人合集，**不含**录入/社保/待遇 |
| 安娜祯 | annazhen | data_entry_leader | data_entry_leader | `data_entry`、`data_entry_resign` | 入职/在职/离职数据录入 |
| （留空） | — | — | — | `onboarding_social_insurance`、`resignation_social_insurance` | 社保公积金办理（待定） |
| （留空） | — | — | — | `benefit_apply` | 在职待遇申报（待定） |

> **不强行绑定付倩文（fuqianwen）** 到社保模块。

---

## A. 角色菜单权限

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| A1 业务负责人 | 仅保留 仪表盘、我的工单(团队工单+历史工单)；去掉 我发起的/入职/在职/离职管理 | 业务负责人(biz_manager)访问 /work-orders 写操作、/onboarding 等被拒（管理只读） | routeVisibility.ts 把 business_owner 移出 INITIATED/ONBOARDING/IN_SERVICE/OFFBOARDING_ROLES，仅留 dashboard/team/history/dashboards.leader | aolei 登录→菜单只有 仪表盘+团队工单+历史工单 | P0 | 误删导致业务负责人连仪表盘都看不到；改后需核可见性矩阵 |
| A2 业务员 | 保留 我发起的、我的退回、历史工单 | 业务员对自己工单可建/改/提交/重提；对他人工单拒写 | INITIATED/RETURNED/HISTORY_WORK_ROLES 含 business_group_member；无团队工单切换 | 业务员登录→见三菜单，无团队工单 | P0 | 历史工单(R1)漏配则业务员丢历史 |
| A3 业务组长 | 业务员全部 + 团队工单切换 | 组长可查本组工单列表，不参与审批 | TEAM_WORK_ROLES 含 business_group_leader | 组长登录→团队工单切换可用 | P1 | 组长误获审批权 |
| A4 后道人员 | 只显示各自负责子工单菜单 | 后端按 moduleHandler/角色限定列表范围 | 后道菜单按 routeVisibility 各模块路由收紧 | 各后道登录→只见自己模块菜单 | P0 | 江璐越权见录入/社保（见 B3） |

---

## B. 后道分工与权限

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| B1 分工 seed | 按§1人员模块矩阵 | seed-module-handlers.ts 与矩阵一致；**补 data_entry_resign→annazhen** | — | 重跑 seed→各模块处理人正确 | P1 | data_entry_resign 缺失致安娜祯离职录入无主 |
| B2 江璐范围(后端兜底) | 江璐数据=杨纯+毛雅妮合集，禁见录入/社保/待遇 | 江璐查 social_insurance/data_entry/benefit 列表返回 403；hasModuleAccess 依赖 moduleHandler 配置（江璐仅5模块） | — | jianglu 调团队列表 moduleCode=data_entry→403 | P0 | shared_leader 经 hasModuleSupervisorRole+hasSupervisorLevel 可能放大范围，需回归确认 |
| B3 江璐菜单(前端) | 同上，菜单不出现录入/社保/待遇 | — | routeVisibility 从 /onboarding/data_entry、/onboarding/benefit_apply、/in-service/benefit-claim、/offboarding/social-suspend-pool 移除 SHARED_TEAM_OWNER | jianglu 登录→无 数据录入/待遇申报/社保停保 入口 | P0 | 前端隐藏+后端403须成对，缺一即漏 |
| B4 社保码断裂 | 社保权限识别正确 | roleAllowsModule/roleAccessibleModules(service:1561-1571,1593-1601)增加 onboarding/resignation_social_insurance 识别，保留旧码 social_insurance | — | 新社保子单→权限函数正确识别归属社保角色 | P1 | 不修则社保子单权限/列表范围错配 |
| B5 社保不强绑付倩文 | 社保暂留空 | seed-module-handlers.ts:85-94 停止把 social_insurance 强 set 给 fuqianwen；历史子单 handlerId 保留不动 | — | 重跑 seed→社保模块无新增固定处理人；历史数据不变 | P1 | 直接删绑定可能动到他人演示数据，须只停新绑定 |

---

## C. 子工单命名

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| C1 社保命名区分 | 入职/离职社保公积金办理名称区分，不混淆；负责人留空 | DISPATCH_MODULE_LABELS 已含"入职社保公积金办理/离职社保公积金办理"✅；enum 新码已存在 | 子单列表/详情显示区分后名称 | 入职社保子单显示"入职社保公积金办理"，离职显示"离职社保公积金办理" | P1 | 旧 social_insurance 单码历史数据仍显示旧名（兼容，不迁移） |
| C2 不绑付倩文 | 见 B5 | 见 B5 | — | 见 B5 | P1 | 见 B5 |

---

## D. 消息通知

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| D1 取消催办反馈/过细分类 | 去掉催办反馈类及过细分类 | 通知桶收敛，移除催办反馈桶 | 通知列表不再有"催办反馈"等过细 Tab | 通知页→无催办反馈分类 | P1 | bizType 自由字符串，收敛需兼容历史通知 |
| D2 仅"已读"+"处理"两动作 | 每条通知只暴露两动作 | 接口支持 已读/处理 两动作语义 | 列表项只渲染 已读、处理 两按钮 | 任一通知→只有两按钮 | P0 | QA阻断 faf6fa7d 的核心 |
| D3 已读只消提醒 | 已读仅置 isRead、未读数-1，不改业务状态 | markRead 只改 isRead，不触业务状态 | 点已读→顶部未读 Badge -1，业务不变 | 点已读→未读数减1，工单状态不变 | P0 | 误把已读当处理会错改状态 |
| D4 处理需业务状态变后才消失 | 处理跳办理；业务状态真变更后通知才消失 | 通知与子单状态联动：业务状态变更信号到达后该通知标记消失 | "处理"跳对应子单办理页 | 处理未完成→通知仍在；完成后→消失 | P0 | 需与 backend_workflow 对齐"业务状态变更"触发信号 |
| D5 处理完成消息留存+变已读 | 处理完成消息留"全部"列表，状态已读，未读-1 | 处理完成后通知 isRead=true，不删除记录 | "全部"列表可见已处理消息(已读态) | 处理完→全部列表仍有该条且为已读 | P1 | 误删记录导致消息丢失 |
| D6 已读/未读筛选 | 增加已读/未读筛选 | DTO 已支持 isRead/unread✅ | 通知页暴露 已读/未读 筛选切换 | 切筛选→列表正确过滤 | P1 | 低 |

---

## E. 作废 / 重新提交

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| E-1 修改不自动重提 | 修改仅保存内容，不自动重新提交 | work-order.service:178/211/250 shouldRequireResubmitAfterEdit 改为**不自动 resubmit**；update 只存 extraData、不跑派发、不改流转状态 | 修改保存后状态不变，重提是独立按钮 | 退回工单→修改保存→状态仍为 returned，未重新流转 | **P0** | 状态机核心改动，须回归 提交→退回→修改→重提 全链路；可能有流程依赖旧自动行为 |
| E-2 撤销作废合并入重提 | 撤销作废合并到重新提交 | 后端保留 restoreVoidByCreator + resubmit 两能力；定义"重新提交"对作废态的路由语义(先恢复再重提 或 直接resubmit) | 已作废子单只显示"重新提交"，无独立"撤销作废" | 已作废子单→点重新提交→恢复并重新流转后道 | P0 | 前端不得臆测接口语义，须后端先给契约 |
| E-3 撤回后作废跳审批 | 撤回后作废不需后道再审批，直接作废 | dispatched-order.service:689 voidByCreator 按源状态分支：源自 WITHDRAWN/RETURNED→直接置 VOID(写 voidAt+审计)，免 approveVoid；PROCESSING/PENDING→保留 VOID_PENDING 审批 | 已撤回子单点作废→提示直接作废 | 已撤回子单→作废→直接 VOID，无审批节点 | P0 | 当前对所有态强制 VOID_PENDING，与口径冲突；改动须回归 |
| E-4 三终态支持重提 | 已退回/已撤回/已作废子单都支持重提 | creator_update_resubmit/dispatch_resubmit 已支持子单级✅；确认三态都能进重提 | 三态子单详情页都有"重新提交" | 退回/撤回/作废子单详情→均有重提按钮且生效 | P0 | 作废态重提依赖 E-2 语义 |
| E-5 重提后流转 | 重提后子单重新流转对应后道，状态进 待处理/处理中 | resubmit 重跑派发引擎，子单状态置 pending/processing，通知后道 | 重提后子单状态显示 处理中/待处理 | 重提→子单回到后道队列，后道收到通知 | P0 | 派发引擎重入幂等性 |

---

## F. 批量导入办理

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| F1 按权限限可选类型 | 办理人只能选有权工单类型 | assertCanBatchImportModule(service:458)已校验✅ | 导入下拉按办理人权限过滤可选模块 | 杨纯导入→只能选 contract/renewal | P1 | 前端不过滤则用户选了被后端拒，体验差 |
| F2 动作二选一 | 只"批办理完成/批办理退回"二选一，禁自由输入 | 后端只接受这两个动作枚举 | 动作改单选(二选一)，选中态清晰，去自由输入框 | 导入办理→只有两选项 | P0 | 自由输入会传非法动作 |
| F3 导出模板再导入失败 | 系统导出模板能原样再导入成功 | 导入解析兼容导出模板表头/行格式 | 导出→导入闭环不报"缺少行/字段" | 导出模板→不改→重新导入→成功 | P0 | 表头/字段映射不一致是根因，须复现 |
| F4 刷新/重进异常消失 | 刷新或重进子界面状态不丢 | — | 导入向导/列表状态持久(刷新不丢) | 导入中刷新→向导/列表正常恢复 | P1 | 状态仅存内存则刷新丢失 |

---

## G. 数据看板

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| G1 待处理拆分 | 总待处理 + 单月待处理 | DTO 含 totalPending/monthPending✅ | 首页展示两张卡 | 首页→两数字卡 | P0 | 口径混淆 |
| G2 月初不丢历史遗留 | 默认口径不在月初丢历史遗留待办 | totalPending 不受选中月限制(全历史未结) | 月初查看仍含历史遗留 | 切到本月初→totalPending 仍含上月遗留 | P0 | 若 totalPending 误按月过滤则丢历史 |
| G3 按月筛选 | 支持按月筛选 | monthPending 按选中月计算 | 前端月份选择器 | 切月→monthPending 变化 | P1 | 低 |
| G4 口径可解释 | 统计口径可解释 | — | 报告含口径说明 | 验收报告有 total vs month 文字 | P2 | 低 |

> 口径锚定：**totalPending = 全历史未结子工单数；monthPending = 选中月新增未结子工单数**。QA 断言期望值以此为准。

---

## H. 我的工单模块

| 项 | 业务口径 | 后端验收点 | 前端验收点 | 测试场景 | 优先级 | 风险 |
|---|---|---|---|---|---|---|
| H1 一子工单一行 | 所有我的工单列表按一条子工单一条数据 | 列表查询返回子工单粒度，不按主工单聚合 | 各"我的工单"列表行=子工单 | 一主单拆多子单→列表显示多行 | P0 | 误按主单聚合则丢子单 |
| H2 我的退回去重复 | 修复重复筛选/重复数据区 | — | 我的退回页只一套筛选、无重复行 | 我的退回页→单筛选区、无重复 | P1 | 重复渲染 |
| H3 团队工单字段补全 | 显示员工信息/工单类型/发起人，修复发起人空值 | list 拼装补 createdBy/发起人，确保非空 | 团队工单列补 员工/类型/发起人列 | 团队工单列表→三列齐全，发起人有值 | P0 | 发起人空值是已知反馈 |
| H4 列表支持筛选 | 所有列表支持筛选 | 列表接口支持筛选参数 | 各列表补筛选项 | 各列表→可筛选 | P1 | 低 |
| H5 团队工单只读 | 团队工单可打开只读详情，不允许操作 | 后端对团队工单(非本人发起/处理)拒绝所有写操作 | 团队工单详情隐藏全部操作按钮 | jianglu/组长打开团队工单详情→只读，无按钮，后端拒写 | P0 | 只隐藏前端不兜底=安全漏洞 |

---

## 集成顺序与成对验收

```
第1层(并行)：backend_workflow(B/C-1/E-1/E-2/E-3) + dashboard_engineer(G)
第2层(依赖第1层接口语义)：frontend_permissions_ui(A/B3/E2-E3/H) + import_notification_engineer(D/F)
第3层：qa_regression 全量回归 + architect 集成核对
```

**成对验收(前端隐藏 + 后端兜底，缺一不过)：**
- B2(后端江璐403) ↔ B3(前端江璐菜单收紧)
- H5(后端团队工单拒写) ↔ H5(前端只读隐藏按钮)

**接口语义先定后接：**
- E-1(修改不重提) → 决定 E3 前端按钮行为
- E-2(作废态重提路由) → 决定前端"重新提交"按钮调用链

---

## 高风险测试清单（必须有回归覆盖）

| 风险点 | 测试 | 负责 |
|---|---|---|
| E-1 状态机改动 | 修改后状态不变 + 重提独立触发 | backend_workflow + qa |
| E-3 撤回后作废 | 撤回态作废直接VOID、PROCESSING态仍审批 | backend_workflow + qa |
| B2 江璐范围 | 江璐查社保/录入列表403 | backend_workflow + qa |
| F3 导入闭环 | 导出模板→重新导入成功 | import_notification + qa |
| D4 通知生命周期 | 处理未完通知在、完成后消失 | import_notification + qa |
