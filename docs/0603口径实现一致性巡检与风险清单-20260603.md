# 0603口径实现一致性只读巡检与风险清单

> 任务：0603口径实现一致性只读巡检与风险清单  
> 日期：2026-06-03  
> 性质：只读巡检；不修改业务代码，不回滚他人改动。  
> 基线口径：`docs/验收矩阵-0603会议口径-20260603.md`。  
> 巡检范围：主工作区 `d:\ai\speceappdate\工单系统` 当前可见代码/文档 + integration worktree `D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998`。

---

## 1. 集成证据与范围说明

| 项 | 巡检结论 | 证据/说明 |
|---|---|---|
| integration HEAD | 当前本地复审视图为 `6af9f099 docs: add 0603 acceptance matrix` | `git log --oneline -3` 显示 `6af9f099 -> 0443fa0a -> 21c49848` |
| integration 差异 | `main..HEAD` 仅新增 `docs/验收矩阵-0603会议口径-20260603.md` | 说明 integration 中尚未集成实现改动，本巡检的代码风险主要来自主工作区可见未提交改动 |
| 0603验收矩阵 tracked | integration 中已 tracked | `git ls-files -- docs/验收矩阵-0603会议口径-20260603.md` 可见 |
| 主工作区状态 | 有大量其他成员未提交实现改动；本巡检未修改 | 涉及后端seed/dispatch/dashboard/import，前端routeVisibility/BasicLayout/Dashboard/MyDispatched等 |
| 本巡检报告 | 新增本文档 | 为补齐本任务交付物：风险点、风险文件列表、重点回归清单 |

> 集成风险澄清：此前系统曾提示 integration 最新 commit 为 `00250199`，后续更新为 `6af9f099`。本次返工以复审指定 integration worktree 当前 HEAD `6af9f099` 为证据。若评审端再次切换到其他 HEAD，需重新确认本文档是否已被合入。

---

## 2. 口径一致性风险总览

| 风险ID | 检查主题 | 状态 | 风险等级 | 关联P0 | 主要风险文件 | 给实现成员的定位 | QA重点验证 |
|---|---|---|---|---|---|---|---|
| R1 | 在职菜单隐藏 | 已看到隐藏实现，但需防动态权限绕开 | 中 | P0-2/12 | `frontend/src/layouts/BasicLayout.tsx`, `frontend/src/config/routeVisibility.ts` | `BasicLayout` 中 in-service group 已 `menuVisible:false`；`IN_SERVICE_ROLES=[]`；但动态权限白名单仍列出 in-service/renewal | 各角色直输 `/in-service`、`/renewal`、`/onboarding/renewal_contract` 必须不可见/403 |
| R2 | 在职看板/仪表盘/趋势仍可见 | 疑似不一致 | 高 | P0-2/3/7/12 | `frontend/src/pages/Dashboard/index.tsx`, `frontend/src/services/dashboard.ts`, `backend/src/modules/dashboard/dashboard.service.ts` | 仍检索到 `renewal`/`benefit` 趋势、矩阵、order type labels | dashboard/cards、order-type-matrix、leader-trend 不得返回/显示在职、待遇 |
| R3 | 入职导入入口 | 方向符合，但需实测 | 中 | P0-1/13 | `frontend/src/layouts/BasicLayout.tsx`, `frontend/src/config/routeVisibility.ts`, `frontend/src/pages/WorkOrders/Import/index.tsx`, `backend/src/modules/imports/*` | 菜单有 `/work-orders/import`；创建角色含业务员/组长，不含业务负责人 | `yaoyiping`、`shenwenjun` 可导入；`aolei` 403 |
| R4 | 子工单旧名称残留 | 疑似不一致 | 高 | P0-4/13 | `frontend/src/services/dashboard.ts`, `frontend/src/utils/notificationDisplay.ts`, `frontend/src/services/workOrders.ts`, `frontend/src/services/moduleConfigs.ts`, `backend/src/common/constants/dispatch-modules.ts` | 常量部分已新名；dashboard/通知/mock仍有“劳动合同签订/数据录入/社保公积金办理/离职证明/社保停保”等旧名 | 菜单、列表、详情、通知、dashboard、导入导出模板都应显示0603新名 |
| R5 | 傅倩雯/付倩雯社保负责人 | 代码方向符合，数据库/Mock风险 | 高 | P0-5/6/12 | `backend/src/database/seeds/seed-module-handlers.ts`, `seed-module-configs.ts`, `seed-users.ts`, `frontend/src/services/moduleHandlers.ts`, `frontend/src/services/users.ts` | 后端seed已见 `fuqianwen`；但mock/历史注释存在“负责人暂不硬编码”风险；目标库可能handler inactive | `fuqianwen` 只看社保增/减且handler active；付/傅名字不影响账号映射 |
| R6 | 江璐合集配置 | 疑似不一致风险 | 高 | P0-5/6/12 | `seed-module-handlers.ts`, `dispatched-order.service.ts`, `role-permissions.ts`, `routeVisibility.ts` | 江璐应仅杨纯+毛雅妮合集；需防 `shared_leader` 或 supervisor 逻辑扩大到社保/报岗/待遇 | `jianglu` API/UI 不得出现 `social_insurance`、`data_entry`、`benefit_apply` |
| R7 | 后道可见无关模块 | 疑似不一致风险 | 高 | P0-3/6/12 | `role-permissions.ts`, `dispatched-order.service.ts`, `frontend/src/utils/moduleAccess.ts`, `routeVisibility.ts`, `MyDispatched/*` | `social_insurance_specialist` 出现在 supervisor角色集合，需验证不会跨模块放权 | 傅倩雯不可见入职联系/合同/报岗；杨纯不可见社保/联系/报岗；毛雅妮不可见合同/社保/报岗 |
| R8 | 社保公积金状态流 | 代码有锁定分支，但E2E未验证 | 高 | P0-8/9/13 | `dispatched-order.service.ts`, `WorkOrders/Detail`, `MyDispatched/Detail`, `services/dispatchedOrders.ts` | 代码存在未接单直改/撤/废与接单后冲突逻辑；仍需完整链路与按钮态验证 | 未接单业务自由操作；接单后需审批/同意；完成后不可直接操作；不新增细分反馈状态 |
| R9 | 社保/医保/公积金细分状态 | 暂未见核心枚举新增，仍需UI文案确认 | 中 | P0-9/11 | `backend/src/entities/enums.ts`, `dispatched-order.service.ts`, `frontend/src/services/dispatchedOrders.ts`, `notificationDisplay.ts` | `DispatchedOrderStatus` 仍为通用状态；但社保批量/反馈文案需确认不做三分状态 | UI只有办结/退回/处理中；不得出现社保/医保/公积金独立失败状态 |
| R10 | 总待处理/单月待处理 | 疑似不完整 | 高 | P0-7/13 | `dashboard.service.ts`, `dashboard-cards.dto.ts`, `Dashboard/index.tsx` | QA报告曾指出 `dashboard/cards` 缺 `totalPending/monthPending`，前端fallback不可靠 | 构造上月遗留待办，总待处理>0且单月待处理按月 |

---

## 3. 风险文件列表（给实现成员定位）

### 3.1 前端菜单/路由

| 文件 | 巡检发现 | 风险级别 | 建议 |
|---|---|---|---|
| `frontend/src/layouts/BasicLayout.tsx` | 入职导入入口可见；在职group已 `menuVisible:false`；离职社保减员入口为 `/onboarding/social_insurance_resign` | 中 | 保留入职导入；确认减员路由与后端module一致；截图回归业务/后道菜单 |
| `frontend/src/config/routeVisibility.ts` | `IN_SERVICE_ROLES=[]`；但动态权限白名单仍列出 `/onboarding/renewal_contract`、`/in-service`、`/in-service/contract-renewal` | 高 | 移除或收紧动态权限白名单中的在职路径；直输URL测试 |
| `frontend/src/config/routeVisibility.test.ts` | 测试断言需同步0603 | 中 | 覆盖业务员/组长/负责人/五后道角色 |

### 3.2 仪表盘/看板

| 文件 | 巡检发现 | 风险级别 | 建议 |
|---|---|---|---|
| `frontend/src/pages/Dashboard/index.tsx` | trendMap 初始化仍包含 `renewal`/`benefit`；趋势请求可能仍查在职/待遇 | 高 | 第一阶段趋势只保留入职/离职；或按权限过滤后不渲染在职 |
| `frontend/src/services/dashboard.ts` | `ORDER_TYPES` 含 `renewal`/`benefit`；`MODULE_LABELS` 有旧名：数据录入/社保公积金办理/劳动合同签订/离职证明/社保停保 | 高 | 同步0603新名；隐藏无权0模块；对接 `totalPending/monthPending` |
| `backend/src/modules/dashboard/dashboard.service.ts` | SQL values仍有 `renewal`/`benefit`；leader trend normalize允许在职/待遇 | 高 | 后端过滤在职/待遇；修复leader-trend SQL fallback；直接返回总/单月待处理 |
| `backend/src/modules/dashboard/dto/dashboard-cards.dto.ts` | 需确认包含 `totalPending/monthPending` | 高 | 无字段则补DTO和SQL；有字段则QA验证payload |

### 3.3 子工单名称/模块映射

| 文件 | 巡检发现 | 风险级别 | 建议 |
|---|---|---|---|
| `frontend/src/constants/modules.ts` | 新名多数已更新；仍保留在职模块选项、`resignation_cert`旧名 | 中 | 在职配置可保留但UI选项不得对业务/后道出现；离职材料收集对外合并 |
| `backend/src/common/constants/dispatch-modules.ts` | 后端label方向符合0603；需旧名兼容映射 | 中 | 兼容“劳动合同签订/数据录入”等旧中文输入映射到新module_code |
| `frontend/src/utils/notificationDisplay.ts` | 检索到旧名风险 | 高 | 通知文案同步新名；不新增社保细分反馈状态 |
| `frontend/src/services/workOrders.ts` | mock/字段反馈映射残留旧名 | 中 | 真实路径优先；mock用于测试时也应同步，避免测试失败 |
| `frontend/src/services/moduleConfigs.ts` | mock模块名可能仍旧 | 中 | 同步0603展示名，或测试明确不走mock |

### 3.4 负责人/后道权限

| 文件 | 巡检发现 | 风险级别 | 建议 |
|---|---|---|---|
| `backend/src/database/seeds/seed-module-handlers.ts` | 代码方向：杨纯/江璐、毛雅妮/江璐、安娜祯、傅倩雯；需确认DB执行 | 高 | 补seed执行/迁移/数据修复验收；handler active |
| `backend/src/database/seeds/seed-module-configs.ts` | module/supervisor/action配置含社保增/减、在职配置 | 中 | 在职后台可保留但界面/API业务统计过滤 |
| `backend/src/database/seeds/seed-users.ts` | `fuqianwen`/江璐/杨纯/毛雅妮/安娜祯账号存在 | 中 | QA使用这些账号固定验证 |
| `backend/src/common/auth/role-permissions.ts` | `social_insurance_specialist` 属于 module supervisor 角色集合 | 高 | 验证不会因supervisor级别看到所有模块；必要时按module handler约束 |
| `backend/src/modules/dispatched-orders/dispatched-order.service.ts` | `applyUserScope`/`hasModuleAccess`/`roleAllowsModule` 需防江璐和傅倩雯越权 | 高 | 补直接API越权测试：江璐查社保、傅倩雯查合同/报岗/联系 |
| `frontend/src/utils/moduleAccess.ts` | 前端phase1模块集合与别名映射存在，需与后端一致 | 中 | 前后端统一 `social_insurance_resign` vs `resignation_social_insurance` |

### 3.5 工单交互/社保状态流

| 文件 | 巡检发现 | 风险级别 | 建议 |
|---|---|---|---|
| `backend/src/modules/dispatched-orders/dispatched-order.service.ts` | `creatorUpdateFields`、`withdraw`、`voidByCreator` 有社保未接单直操作与接单后冲突逻辑 | 高 | 补单测和E2E；验证acceptedAt/status边界 |
| `frontend/src/pages/WorkOrders/Detail/index.tsx` | 需验证业务员按钮态 | 高 | 未接单显示改/撤/废；接单后显示申请/禁用；完成后禁用 |
| `frontend/src/pages/MyDispatched/Detail/index.tsx` | 后道待办/已办只读与回负责列表操作需验证 | 中 | QA复测团队只读、后道只读详情 |
| `frontend/src/pages/MyDispatched/index.tsx` | 筛选项需按负责人模块过滤 | 高 | 傅倩雯只见社保，江璐不见社保/报岗 |
| `frontend/src/services/dispatchedOrders.ts` | 社保批量/反馈逻辑需防细分状态 | 中 | 保持办结/退回/处理中三类 |

### 3.6 导入/通知

| 文件 | 巡检发现 | 风险级别 | 建议 |
|---|---|---|---|
| `frontend/src/pages/WorkOrders/Import/index.tsx` | 入职导入UI存在 | 中 | 业务员/组长预览确认全链路复测 |
| `backend/src/modules/imports/import-job.service.ts` | 导入权限/订单类型需确保业务员可入职导入、负责人拒绝 | 中 | API测试 `preview/confirm` |
| `backend/src/modules/imports/import-permissions.ts` | 新权限工具未集成到integration实现风险 | 中 | 确认最终分支是否包含；业务负责人导入403 |
| `backend/src/modules/notifications/notification-display.util.ts` / `frontend/src/utils/notificationDisplay.ts` | 通知旧名/反馈状态风险 | 中 | 通知仅办结/退回/处理中；旧子工单名同步 |

---

## 4. 重点回归清单（QA可执行）

### 4.1 角色菜单与路由

| 用例 | 角色/账号 | 操作 | 期望 |
|---|---|---|---|
| Q1 | 业务员 `yaoyiping` | 看菜单并访问 `/work-orders/import` | 可见入职导入，可上传/预览/确认；无在职/后道入口 |
| Q2 | 业务组长 `shenwenjun` | 看菜单、团队工单、入职导入 | 有团队视角；团队详情只读；可入职导入；无在职入口 |
| Q3 | 业务负责人 `aolei` | 看菜单、直输 `/work-orders/import` | 仅仪表盘+团队/历史；导入/发起403 |
| Q4 | 杨纯 `yangchun` | 看后道菜单/列表/dashboard | 只见劳动合同新签；不见联系/报岗/社保/在职续签界面 |
| Q5 | 毛雅妮 `maoyani` | 看后道菜单/列表/dashboard | 只见入职联系、离职材料收集 |
| Q6 | 江璐 `jianglu` | 看后道菜单/API | 只见杨纯+毛雅妮合集；不得出现社保、报岗、待遇 |
| Q7 | 安娜祯 `annazhen` | 看后道菜单/API | 只见增员报岗录入、减员报岗录入 |
| Q8 | 傅倩雯 `fuqianwen` | 看后道菜单/API | 只见社保公积金增员/减员；不见合同/联系/报岗 |

### 4.2 仪表盘与看板

| 用例 | 操作 | 期望 |
|---|---|---|
| Q9 | 各角色打开dashboard | 无权限模块不显示，不能显示0卡片 |
| Q10 | 业务/后道查dashboard | 第一阶段不显示在职、续签、待遇、社保变更统计/趋势/矩阵 |
| Q11 | 调 `GET /dashboard/cards?month=2026-06` | payload直接含 `totalPending`、`monthPending`；历史遗留待办不被月初清零 |
| Q12 | 调 order-type-matrix/leader-trend | 不返回在职/待遇；无SQL fallback错误 |

### 4.3 子工单名称

| 用例 | 覆盖位置 | 期望新名 |
|---|---|---|
| Q13 | 菜单/列表/详情/通知/dashboard | 入职联系、劳动合同新签、增员报岗录入、社保公积金增员、离职材料收集、减员报岗录入、社保公积金减员 |
| Q14 | 后台配置/导入导出模板 | 在职名称可保留后台配置，但业务/后道界面不可见 |
| Q15 | 搜索旧名 | 业务/后道界面不出现：劳动合同签订、数据录入、社保公积金办理、离职证明、社保停保等旧口径 |

### 4.4 后道越权API

| 用例 | 操作 | 期望 |
|---|---|---|
| Q16 | `jianglu` 调 `/dispatched-orders?moduleCode=social_insurance` | 403或空；不得返回社保 |
| Q17 | `fuqianwen` 调合同/联系/报岗module | 403或空 |
| Q18 | `yangchun` 调社保/联系/报岗module | 403或空 |
| Q19 | `maoyani` 调合同/社保/报岗module | 403或空 |
| Q20 | `annazhen` 调合同/联系/社保module | 403或空 |

### 4.5 社保公积金状态流

| 用例 | 步骤 | 期望 |
|---|---|---|
| Q21 | 创建入职社保子单，未接单前业务员修改 | 允许直接保存，不需审批 |
| Q22 | 未接单前业务员撤回/作废 | 允许直接撤回/作废，不需后道审批 |
| Q23 | 傅倩雯接单/受理后业务员修改/撤回/作废 | 需后道同意或审批；直接操作返回冲突/申请态 |
| Q24 | 社保子单完成后业务员操作 | 不可直接修改/撤回/作废 |
| Q25 | 社保失败反馈 | 只保留办结、退回、处理中；不出现社保/医保/公积金细分失败状态 |

---

## 5. 当前判断与优先级

1. **最高优先级P0**：仪表盘无权0模块/在职统计泄露、江璐返回社保、傅倩雯handler active、dashboard totalPending/monthPending、后端/前端测试失败。
2. **高优先级回归**：10个子工单新名全路径一致；后道越权API矩阵；社保状态流E2E。
3. **集成风险**：integration 当前仅有文档差异，主工作区实现改动尚未在本巡检报告中证明已集成。评审/QA需以后续实际integration HEAD重新跑本清单。
4. **二期暂缓防误做**：待遇申报详细流程、新开户、城市特殊字段模板、社保公积金变更复杂流程不应作为本轮上线阻断；但若界面对业务/后道可见，则按P0-2隐藏失败处理。

---

## 6. 本报告对评审失败点的补齐说明

- 已明确不是验收矩阵，而是“实现一致性只读巡检与风险清单”。
- 已覆盖检查范围与证据来源。
- 已列出在职菜单/看板/仪表盘/后道列表可见性风险。
- 已列出入职导入入口状态与回归点。
- 已列出旧子工单名称残留风险。
- 已列出傅倩雯/付倩雯、江璐合集、后道无关模块风险。
- 已列出社保公积金细分状态和状态流风险。
- 已输出风险文件列表与QA重点回归清单。
