# NO-GO阻塞项二次针对性复测报告

- 任务：`19e21cc5-cf30-41fa-8f45-7949f4e23030`
- 验证日期：2026-06-03（Asia/Shanghai）
- 验证基线：integration tracked HEAD `49dd10f7e2f99dcd7959fe166dbbf8bef3873a53`（`docs: refresh workorder ui QA handoff`）
- 干净验证工作树：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998\.spectrai-worktrees\qa-validations\0603-targeted-49dd10f7`
- 说明：integration 根工作区存在大量未提交业务改动，本轮所有 build/test 仅以 detached clean worktree 为准；`node_modules` 通过 junction 指向主工作区已安装依赖。

## 结论：NO-GO

本轮旧 NO-GO 阻塞中的大部分后端权限/状态流与前端菜单/路由主链路已在最新 tracked HEAD 的 clean worktree 上转绿；但最终 release gate 仍未全绿：

1. `frontend npm test` 在最新 tracked HEAD 上仍失败 1 个用例：`BasicLayout notification dropdown > uses localized display content instead of raw backend content in top dropdown`；
2. 当前 `localhost:3000` 运行环境的只读 API 探测结果与 tracked HEAD 单测结论冲突，显示服务可能未重启到最新代码（江璐仍返回 `social_insurance`、`dashboard/cards` 缺少 `totalPending/monthPending`、`module-handlers` 社保 handler 仍为 inactive）。

因此，**不能沿用旧 7b1aafb0 结论，但以最新 HEAD 49dd10f7 为准，当前仍是 NO-GO**。

## 一、命令与结果（clean tracked worktree）

| 类别 | 命令 | 结果 | 关键证据 |
|---|---|---|---|
| 后端构建 | `cd backend && npm run build` | PASS | `nest build && tsc-alias` 成功 |
| 后端单测 | `cd backend && npm test` | PASS | 46 passed / 1 skipped suites；303 passed / 16 skipped tests |
| 前端构建 | `cd frontend && npm run build` | PASS | `tsc -b && vite build` 成功，4590 modules transformed |
| 前端全量单测 | `cd frontend && npm test` | FAIL | 33 passed / 34 files；172 passed / 173 tests；仅 `BasicLayout.test.tsx` 1 个通知下拉本地化用例失败 |
| 前端定向菜单/路由/仪表盘 | `cd frontend && npm run test -- src/layouts/BasicLayout.test.tsx src/config/routeVisibility.test.ts src/pages/Dashboard/index.test.tsx` | FAIL | 21/22 通过；唯一失败仍是 `BasicLayout` 通知下拉本地化用例，菜单/路由/仪表盘相关断言本身未再失败 |
| 前端定向只读/状态流 | `cd frontend && npm run test -- src/pages/MyDispatched/Detail/index.test.tsx src/pages/WorkOrders/Detail/index.test.tsx src/pages/MyDispatched/index.test.tsx src/pages/TeamDispatched/index.test.tsx` | PASS | 4 files / 23 tests PASS |
| 当前服务基础冒烟 | `cd frontend && npm run smoke:live` | PASS | 登录、`/auth/me`、`/work-orders`、`/admin/users`、`/dashboard/salesperson` 成功 |
| 当前服务 phase5/6 冒烟 | `cd frontend && npm run verify:phase56` | PASS | 工单详情、子工单详情、通知、模板预览、dashboard processor、SSE 成功 |

> 注意：两条 smoke 脚本与下述 API 只读探测访问的是当前 `http://localhost:3000` 运行服务；它们不自动等同于 latest tracked HEAD 对应服务，因为当前服务可能尚未重启到最新代码。

## 二、最新 HEAD 上已转绿的阻塞项（以 clean worktree build/test 为准）

### 2.1 后端江璐 scope / 模块过滤
- 证据：`backend/test/control-flow-regression.spec.ts`
- 通过用例：`shared leader jianglu module scope is Yang Chun plus Mao Yani phase-1 modules only`
- 结论：最新 tracked HEAD 的后端单测已覆盖并通过“江璐仅杨纯+毛雅妮合集、第一期模块过滤”。

### 2.2 dashboard totalPending/monthPending 与第一期隐藏
- 证据：`backend/test/dashboard.spec.ts`
- 通过用例：
  - `exposes totalPending/monthPending aliases for dashboard cards`
  - `hides in-service order types from leader trend instead of returning onboarding data`
  - `filters leader trend by moduleCode or Chinese module name`
- 结论：后端单测已覆盖 dashboard 卡片别名字段与第一期隐藏逻辑，并通过。

### 2.3 傅倩雯社保增/减员 active、在职/待遇不开放
- 证据：`backend/test/control-flow-regression.spec.ts`
- 通过用例：`assigns social insurance increase/decrease to Fu Qianwen and keeps in-service modules inactive`
- 结论：在 tracked HEAD 单测层面已通过。

### 2.4 社保公积金状态流
- 证据：`backend/test/social-insurance-state-flow.spec.ts`
- 通过用例：
  - `allows salesperson to directly edit, withdraw and void social insurance before acceptance`
  - `locks salesperson direct edit after social insurance is accepted and keeps withdraw/void approval-bound`
  - `keeps retry/waiting batch feedback in processing instead of adding extra failure states`
- 结论：后端单测已覆盖“未接单可改/撤/废、接单后锁定/审批、保持处理中不细分失败状态”。

### 2.5 导入权限
- 证据：`backend/test/import-permissions.spec.ts`
- 通过用例：
  - `allows admin, business member and business group leader to import phase-1 onboarding/resignation orders`
  - `blocks business owner and backend processor roles even if they hit the import API directly`
  - `blocks in-service import types in phase 1 for otherwise eligible business users`
- 结论：业务员/业务组长允许，负责人/后道拒绝，renewal/benefit 拒绝，在 tracked HEAD 单测层面通过。

### 2.6 前端菜单/路由/仪表盘主链路
- 证据：
  - `frontend/src/layouts/BasicLayout.test.tsx`
  - `frontend/src/config/routeVisibility.test.ts`
  - `frontend/src/pages/Dashboard/index.test.tsx`
- 通过断言包括：
  - 业务负责人仅仪表盘/团队工单/历史工单；
  - 业务员恢复入职列表/导入与我的工单视图；
  - 业务组长恢复导入并具有团队视角；
  - 江璐仅杨纯+毛雅妮第一期模块；
  - 傅倩雯仅社保增/减员；
  - 隐藏在职路由；
  - Dashboard 显示总待处理与单月待处理，并隐藏在职矩阵行；
  - 社保后道仪表盘仅显示增/减员。
- 结论：在最新 tracked HEAD 上，这些菜单/路由/仪表盘主链路断言已通过；当前唯一残留失败不在权限主链路，而在通知下拉本地化文案。

### 2.7 工单只读与前端状态流交互
- 证据：
  - `frontend/src/pages/MyDispatched/Detail/index.test.tsx`
  - `frontend/src/pages/WorkOrders/Detail/index.test.tsx`
  - `frontend/src/pages/MyDispatched/index.test.tsx`
  - `frontend/src/pages/TeamDispatched/index.test.tsx`
- 命令结果：4 files / 23 tests PASS
- 通过断言包括：
  - 团队只读详情隐藏所有操作按钮；
  - 后道从只读视图打开详情时无操作；
  - 主工单详情保持只读并引导到子工单操作；
  - 社保子工单未接单时允许业务员修改/撤回/作废；
  - 已接单/处理中后锁定直接操作；
  - 退保社保 accepted alias 同样锁定；
  - 我的工单仍保持子工单级展示；
  - 团队工单打开的是只读子工单详情而非主工单可操作页。

## 三、最新 HEAD 仍未通过的项

### 3.1 前端通知下拉本地化用例失败
- 失败文件：`frontend/src/layouts/BasicLayout.test.tsx`
- 失败用例：`uses localized display content instead of raw backend content in top dropdown`
- 失败断言：无法找到 `劳动合同新签反馈 更新`
- 影响：
  - 这不是 0603 P0 菜单/权限主链路问题；
  - 但它属于前端全量测试红灯，且影响顶部通知展示的本地化质量；
  - 因任务要求以最新 HEAD 的 backend/frontend build/test 为准，所以仍构成最终 GO 阻塞。

## 四、当前 localhost 运行环境只读 API 探测（非 tracked HEAD 启动证明）

> 探测脚本：clean worktree 下临时脚本 `tmp-qa-targeted-probe.mjs`。仅登录与 GET 查询，不做导入写入、状态变更或审批操作。

### 4.1 结果摘要
- 登录成功账号：`yaoyiping / shenwenjun / aolei / yangchun / maoyani / annazhen / fuqianwen / jianglu / lizhanbo`
- `/dispatched-orders`：
  - `jianglu` → `contract,onboarding_contact,social_insurance`
  - `fuqianwen` → `social_insurance`
  - `yangchun` → `contract`
  - `maoyani` → `onboarding_contact`
  - `annazhen` → `data_entry`
- `/dashboard/cards`：所有探测角色返回 keys 仅含 `completed, completionRate, completion_rate, myMessages, processing, scope, totalThisMonth, voidCount, void_count, voided`，未见 `totalPending/monthPending` 或 `pendingTotal/pendingThisMonth`
- `/admin/module-handlers`：`social_insurance` 相关 handler 仍为 `active=false`；`benefit_apply` 仍为 `active=false`

### 4.2 解释
这些 live API 结果与最新 tracked HEAD 的 clean worktree 单测结论冲突，说明**当前 `localhost:3000` 服务很可能未重启到最新 integration 代码**，或运行环境与 tracked commit 不一致。因此：
- 它们可以作为**环境漂移风险**记录；
- 但**不能**直接推翻 latest tracked HEAD 的单测结论；
- 若要把角色 API 结果与 tracked HEAD 一一绑定，需要在独立端口启动最新后端实例后复测。本轮尝试启动隔离后端被权限层拒绝，未能完成该步。

## 五、GO/NO-GO 判定

### 当前判定：NO-GO

### 直接阻塞原因
1. **最新 integration tracked HEAD 的前端全量测试仍失败 1 项**：`BasicLayout` 通知下拉本地化用例；
2. **当前 live 服务与 tracked HEAD 单测结果不一致**，江璐 scope / dashboard pending 字段 / module-handlers active 在运行环境中仍表现为旧问题，说明演示环境存在漂移风险，不能直接放行。

## 六、剩余风险与建议

### 剩余风险
1. **前端测试红灯**：通知下拉本地化文案未通过，影响 release gate；
2. **运行环境漂移**：`localhost:3000` 服务未证明来自最新 integration HEAD，导致 live API 仍暴露旧问题；
3. **导入权限与社保状态流的 live 端到端**未在独立最新服务上执行真实写操作，本轮主要依赖后端/前端单测与只读探测；
4. **傅倩雯减员模块 (`resignation_social_insurance`)** 在当前 live `/dispatched-orders` 没有现成数据样本，运行态未直接观测到列表返回，但单测已覆盖配置与权限逻辑。

### 建议的最小收口动作
1. 修复 `frontend/src/layouts/BasicLayout.test.tsx` 的通知下拉本地化失败后，至少重跑：
   - `cd frontend && npm test`
   - `cd frontend && npm run build`
2. 使用**最新 integration HEAD 启动的后端服务**（不要复用旧 3000 进程）重新做一次角色 API 探测，确认：
   - `jianglu /dispatched-orders` 仅 `contract,onboarding_contact,resignation_contact`（或实际第一期合集，不含社保/报岗/待遇）
   - `dashboard/cards` 返回 `totalPending/monthPending` 或约定等价字段
   - `/admin/module-handlers` 中傅倩雯社保增/减员 active=true，benefit/change 不开放
3. 若允许在隔离环境写测试数据，再补一次社保公积金 live 端到端：未接单可改/撤/废、接单后锁定/审批、完成后不可直接操作。

## 七、最终摘要

- **不要再沿用旧 7b1aafb0 结论。**
- 以最新 integration tracked HEAD `49dd10f7` 的 clean worktree 为准：
  - 后端 build/test：PASS
  - 前端 build：PASS
  - 前端全量 test：FAIL（仅 1 项通知下拉本地化）
  - 菜单/路由/仪表盘主链路定向测试：权限相关断言已通过
  - 工单只读/社保状态流前端定向测试：PASS（23/23）
- **最终 GO/NO-GO：NO-GO**
  - 直接阻塞：前端全量测试未全绿；
  - 运行环境风险：当前 3000 服务看起来仍是旧行为，需用最新 HEAD 重启后再做 live 角色 API 确认。
