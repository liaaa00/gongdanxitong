# NO-GO阻塞项二次针对性复测报告（0603）

- 任务 ID：`19e21cc5-cf30-41fa-8f45-7949f4e23030`
- 验证日期：2026-06-03（Asia/Shanghai）
- 验证基线：integration tracked HEAD `8a45fe6c74f38aeab28da0ce35ef644e77d4fae0`（`docs: refresh qa handoff traceability`）
- 干净验证工作树：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998\.spectrai-worktrees\qa-validations\0603-targeted-8a45fe6c`
- 说明：integration 根工作树存在其他成员未提交改动，本轮 build/test 一律以 detached clean worktree 为准；`node_modules` 通过 junction 复用主工作区已安装依赖。

## 结论：GO

上一轮阻塞项已在最新 integration HEAD 上完成收口：

1. 后端阻塞项定向回归通过：江璐 scope/模块过滤、dashboard `totalPending/monthPending`、傅倩雯社保增减员 handler 激活、导入权限、社保公积金状态流全部转绿。
2. 前端阻塞项定向回归通过：`BasicLayout` / `routeVisibility` / `Dashboard` 最新定向测试全部通过，上一轮通知下拉本地化失败已消失。
3. 前端只读详情与社保状态流相关页面定向测试通过；前后端全量 test/build 全部通过。
4. `smoke:live` 与 `verify:phase56` 在当前 `http://localhost:3000` 运行环境通过，可作为演示环境健康辅助证据。

因此，本轮针对 0603 阻塞项的最新判定从旧版 **NO-GO** 更新为 **GO**。

## 一、执行命令与结果

### 1.1 后端定向阻塞项回归

| 命令 | 结果 | 说明 |
|---|---|---|
| `npm --prefix backend test -- --runTestsByPath test/control-flow-regression.spec.ts test/dashboard.spec.ts test/import-permissions.spec.ts test/social-insurance-state-flow.spec.ts` | PASS | 4 suites / 34 tests PASS |

### 1.2 后端全量回归

| 命令 | 结果 | 说明 |
|---|---|---|
| `npm --prefix backend test` | PASS | 46 passed / 1 skipped suites；303 passed / 319 tests |
| `npm --prefix backend run build` | PASS | `nest build && tsc-alias` 成功 |

### 1.3 前端定向阻塞项回归

| 命令 | 结果 | 说明 |
|---|---|---|
| `npm --prefix frontend run test -- src/layouts/BasicLayout.test.tsx src/config/routeVisibility.test.ts src/pages/Dashboard/index.test.tsx src/pages/MyDispatched/Detail/index.test.tsx src/pages/WorkOrders/Detail/index.test.tsx src/pages/MyDispatched/index.test.tsx src/pages/TeamDispatched/index.test.tsx` | PASS | 7 files / 45 tests PASS |

### 1.4 前端全量回归与冒烟

| 命令 | 结果 | 说明 |
|---|---|---|
| `npm --prefix frontend test` | PASS | 34 passed / 34 files；173 passed / 173 tests |
| `npm --prefix frontend run build` | PASS | `tsc -b && vite build` 成功 |
| `npm --prefix frontend run smoke:live` | PASS | 登录、`/auth/me`、`/work-orders`、`/admin/users`、`/dashboard/salesperson` 正常 |
| `npm --prefix frontend run verify:phase56` | PASS | 工单详情、子工单详情、通知、模板预览、dashboard processor、SSE 正常 |

## 二、阻塞项逐条复测结论

### 2.1 江璐 `/dispatched-orders` scope 必须仅为杨纯 + 毛雅妮第一期合集

后端证据来自 `backend/test/control-flow-regression.spec.ts`，并已通过：

- `shared leader jianglu module scope is Yang Chun plus Mao Yani phase-1 modules only`
- 断言允许：`contract`、`onboarding_contact`、`resignation_contact`
- 断言排除：`data_entry`、`data_entry_resign`、`social_insurance`、`onboarding_social_insurance`、`resignation_social_insurance`、`benefit_apply`、`renewal_contract`

结论：**PASS**。江璐不再包含社保、报岗、待遇、续签等越权模块。

### 2.2 dashboard/cards 必须返回 `totalPending/monthPending`，且第一期隐藏在职

后端证据来自 `backend/test/dashboard.spec.ts`，并已通过：

- `exposes totalPending/monthPending aliases for dashboard cards`
- `hides in-service order types from leader trend instead of returning onboarding data`
- `filters leader trend by moduleCode or Chinese module name`

前端证据来自 `frontend/src/pages/Dashboard/index.test.tsx` 定向测试已通过，覆盖：

- 按权限隐藏无关模块
- 不显示无权限 0 卡片
- 保留总待处理 / 单月待处理展示
- 第一阶段隐藏在职矩阵/在职统计
- 后道社保角色仅显示社保增员/减员相关卡片

结论：**PASS**。

### 2.3 `/admin/module-handlers` 中傅倩雯仅社保增/减员 active=true，变更/待遇不开放

后端证据来自 `backend/test/control-flow-regression.spec.ts`，并已通过：

- `assigns social insurance increase/decrease to Fu Qianwen and keeps in-service modules inactive`

结论：**PASS**。傅倩雯只负责社保公积金增员、社保公积金减员；在职相关配置保持 inactive。

### 2.4 社保公积金未接单 / 已接单 / 已完成状态流

后端证据来自 `backend/test/social-insurance-state-flow.spec.ts`，并已通过：

- `allows salesperson to directly edit, withdraw and void social insurance before acceptance`
- `locks salesperson direct edit after social insurance is accepted and keeps withdraw/void approval-bound`
- `keeps retry/waiting batch feedback in processing instead of adding extra failure states`

前端证据来自 `frontend/src/pages/MyDispatched/Detail/index.test.tsx`，并已通过，覆盖：

- 未接单子工单显示“修改 / 撤回 / 作废”
- 已接单 / processing 后显示“已接单/已受理”提示
- 已接单后改为“修改需后道同意 / 撤回需审批 / 作废需审批”且按钮禁用
- `accepted` alias 对社保减员同样锁定

结论：**PASS**。

### 2.5 导入权限：业务员/组长允许，负责人/后道拒绝，renewal/benefit 拒绝

后端证据来自 `backend/test/import-permissions.spec.ts`，并已通过：

- `allows admin, business member and business group leader to import phase-1 onboarding/resignation orders`
- `blocks business owner and backend processor roles even if they hit the import API directly`
- `blocks in-service import types in phase 1 for otherwise eligible business users`

前端证据来自：

- `frontend/src/layouts/BasicLayout.test.tsx`
- `frontend/src/config/routeVisibility.test.ts`

已通过断言包括：

- 业务员恢复“入职导入”入口
- 业务组长恢复“入职导入”并保留团队视角
- 业务负责人不可访问 `/work-orders/import`
- 在职 `renewal/benefit` 相关路由与入口在第一阶段不可访问

结论：**PASS**。

### 2.6 前端菜单 / 仪表盘 / 关键角色 UI 冒烟

定向用例已通过，覆盖以下角色：

1. **业务负责人**
   - 仅保留仪表盘、我的工单下团队工单/历史工单
   - 不显示我发起的、导入、创建、在职入口
2. **业务员**
   - 恢复入职主工单列表、入职导入、我发起的、我的退回、历史工单
   - 不显示团队工单、我的待办、在职管理
3. **业务组长**
   - 在业务员基础上保留团队工单视角
4. **傅倩雯/社保后道**
   - 仅显示社保公积金增员/减员相关菜单与看板
5. **江璐**
   - 仅保留杨纯 + 毛雅妮第一期合集，不包含社保/报岗/待遇/续签
6. **第一阶段隐藏**
   - `/renewal`、`/benefit`、`/in-service/*` 等路由均不可访问

结论：**PASS**。

### 2.7 我的工单子工单级展示、团队工单只读详情

前端证据来自：

- `frontend/src/pages/MyDispatched/index.test.tsx`
- `frontend/src/pages/TeamDispatched/index.test.tsx`
- `frontend/src/pages/WorkOrders/Detail/index.test.tsx`
- `frontend/src/pages/MyDispatched/Detail/index.test.tsx`

已通过断言包括：

- 主工单详情页明确只读，提示操作请进入对应子工单
- 团队工单打开为只读详情，不暴露操作按钮
- 后道在我的工单/我的待办视图中保持只读
- 我的工单继续按子工单级展示与操作

结论：**PASS**。

## 三、对旧版 NO-GO 的修正

旧版 NO-GO 的唯一直接阻塞项为前端 `BasicLayout` 通知下拉本地化失败。该问题已在 integration 后续提交修复；在当前基线 `8a45fe6c` 上：

- `src/layouts/BasicLayout.test.tsx`
- `src/config/routeVisibility.test.ts`
- `src/pages/Dashboard/index.test.tsx`

相关定向测试现已 **3 files / 22 tests PASS**；扩展后的 7 文件定向集也已 **45/45 PASS**。因此，旧版 NO-GO 结论已失效，不得再沿用旧基线报告作为当前 release gate。

## 四、剩余风险与说明

### 4.1 非阻塞风险

1. `smoke:live` 与 `verify:phase56` 访问的是当前 `http://localhost:3000` 运行环境，结果可作为演示环境健康辅助证据，但不替代 clean tracked worktree 的 build/test 结论。
2. 后端全量测试中存在 AI 配置 401 / timeout、dashboard fallback 等日志告警，但套件本身通过，当前判定为非阻塞测试噪音。
3. 前端 vitest 存在 `getComputedStyle()` pseudo-element 未实现告警、Vite `esbuild` 选项弃用告警，但不影响测试通过与构建产物。
4. 本轮未额外启动一套隔离新端口的最新后端实例做真实写操作式端到端，因为任务边界要求以最新 integration tracked commit 和最小扰动方式完成针对性复测；当前以前后端定向测试、全量测试和 live smoke 共同作为放行证据。

### 4.2 演示建议

推荐演示路径：

1. **业务员**：登录后查看入职主工单列表、入职导入、我发起的、我的退回
2. **业务组长**：在业务员基础上演示团队工单
3. **业务负责人**：仅演示仪表盘、团队工单、历史工单
4. **傅倩雯**：演示仅社保公积金增员/减员菜单与仪表盘
5. **江璐**：演示仅合同/入职联系/离职材料收集合集，不出现社保/报岗/待遇/续签
6. **社保状态流**：用未接单与已接单样例分别演示“可直接改撤废”与“需后道同意/审批锁定”差异

## 五、最终判定

- **GO / NO-GO：GO**
- 放行依据：
  1. 后端定向阻塞项测试 PASS
  2. 后端全量 `npm test` / `npm run build` PASS
  3. 前端定向阻塞项测试 PASS
  4. 前端全量 `npm test` / `npm run build` PASS
  5. `smoke:live` / `verify:phase56` PASS
  6. 关键角色菜单、导入权限、仪表盘过滤、在职隐藏、子工单只读与社保状态流均有可复核测试证据

> 结论更新：**以最新 integration tracked HEAD `8a45fe6c` 为准，本轮 0603 阻塞项修复后二次针对性复测结论为 GO。**
