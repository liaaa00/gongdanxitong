# 0603 修复最终二次全量回归报告

- 验证时间：2026-06-03 12:28-12:40（Asia/Shanghai）
- 代码基线：integration tracked commit `def3c1da4061a25b182172e251d8306deb4e286b`
- 干净验证工作树：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998\.spectrai-worktrees\qa-validations\0603-final-def3c1da`
- 说明：原 integration 工作树存在未提交业务文件变更，因此本轮 build/test 在 detached clean worktree 执行；依赖通过 junction 指向主工作区已安装 `node_modules`，源码仍为 tracked commit。

## 结论

**NO-GO（代码单测/构建已全绿，但 live 权限/运行配置仍有阻断风险）**

理由：
1. 后端 `build/test`、前端 `build/test` 在 tracked commit 上均已通过，上一轮 `benefit-state-machine` 与前端 `MyDispatched` 单测阻断已消失。
2. 但针对当前 `localhost:3000/5173` 的 live API 探测仍显示：江璐 `/dispatched-orders` 返回 `social_insurance`，不符合“江璐=杨纯+毛雅妮合集，不含社保/报岗/待遇”。
3. `/dashboard/cards` 返回字段仍未包含 `totalPending/monthPending` 或等价 `pendingTotal/pendingThisMonth`，无法证明“总待处理/单月待处理、历史遗留不清零”已按接口兜底。
4. `/admin/module-handlers` 中 `social_insurance` 相关 active 配置仍均为 false，与“傅倩雯负责社保公积金增员/减员”的运行库配置要求不一致；同时傅倩雯列表又能看到 `social_insurance`，说明运行库/权限兜底存在不一致。
5. 社保公积金“未接单自由修改/撤回/作废、接单后锁定/审批、完成后禁止”未完成 live 端到端验证。
6. UI 全角色探测因上一轮超时中断后未再执行长脚本；本报告不把未完成 UI 探测作为通过证据。

## 一、构建与测试命令结果（tracked commit）

| 类别 | 命令 | 结果 |
|---|---|---|
| 后端构建 | `cd backend && npm run build` | PASS |
| 后端测试 | `cd backend && npm test` | PASS：44 passed / 1 skipped suites；296 passed / 16 skipped tests；`benefit-state-machine.spec.ts` PASS |
| 前端构建 | `cd frontend && npm run build` | PASS：4589 modules transformed |
| 前端测试 | `cd frontend && npm test` | PASS：33 files passed；154 tests passed |

## 二、live smoke 结果（当前 localhost 服务）

| 命令 | 结果 | 说明 |
|---|---|---|
| `cd frontend && npm run smoke:live` | PASS | 登录、auth/me、work-orders、admin/users、dashboard/salesperson 成功 |
| `cd frontend && npm run verify:phase56` | PASS | work-order detail、dispatched-order detail、notifications、export templates、dashboard processor、SSE 成功 |

> 注意：live smoke 请求的是当前 `http://localhost:3000` 运行服务；未证明该服务一定由本轮 tracked commit 启动。

## 三、关键角色 API 探测结果

临时脚本：`tmp/qa_0603_final_api_probe.mjs`（只读登录、GET 查询、空导入权限校验）。

### 通过项

1. **入职导入接口权限**
   - `yaoyiping`、`shenwenjun`：`/work-orders/import/preview` 与 `/confirm` 空数据返回 400/4400，说明非 403，入口权限存在。
   - `aolei`、`yangchun`、`maoyani`、`jianglu`、`annazhen`、`fuqianwen`：导入 preview/confirm 均返回 403。
2. **一期隐藏矩阵**
   - `order-type-matrix?dimension=node` 未返回 `renewal_contract`、`benefit_apply`、`social_insurance_change`。
   - `leader-trend/onboarding?moduleCode=benefit_apply` 返回 404。
3. **后道基础模块隔离（部分通过）**
   - 杨纯：`contract`。
   - 毛雅妮：`onboarding_contact`。
   - 安娜祯：`data_entry`。
   - 傅倩雯：`social_insurance`。

### 失败/风险项

1. **江璐社保模块泄露（FAIL）**
   - `jianglu` `/dispatched-orders` 返回：`contract,onboarding_contact,social_insurance`。
   - 预期：仅杨纯+毛雅妮合集，不应包含 `social_insurance`、`resignation_social_insurance`、`data_entry`、`benefit_apply`。
2. **dashboard/cards 待处理字段缺失（FAIL）**
   - 所有角色返回 keys 仅含：`completed, completionRate, completion_rate, myMessages, processing, scope, totalThisMonth, voidCount, void_count, voided`。
   - 未见 `totalPending/monthPending` 或 `pendingTotal/pendingThisMonth`。
3. **社保 handler active 配置不一致（FAIL）**
   - `/admin/module-handlers` 中 `social_insurance` 多条记录 `isActive=false`。
   - 预期：傅倩雯负责社保公积金增员/减员，运行库 active 配置应可核验。
4. **业务员 `/dispatched-orders` 可见多个后道模块（需确认）**
   - `yaoyiping` 返回 `contract,data_entry,onboarding_contact,social_insurance`。
   - 若该接口对业务员用于“我的子工单进展”展示可能合理；若作为后道列表则需后端继续收敛。

## 四、UI 与状态流验证说明

- UI 长脚本探测上一轮因超时被系统中断，本轮未重复执行长全角色脚本。
- `localhost:5173` 可访问，Playwright 依赖存在；但为避免再次超时，未将 UI 菜单结果作为通过证据。
- 社保公积金状态流未完成 live 端到端：未创建从业务员发起、傅倩雯接单、业务员二次修改/撤回/作废、办结后禁止操作的完整链路。

## 五、上一轮 NO-GO 项复测状态

| 项目 | 本轮结果 |
|---|---|
| `benefit-state-machine` 失败 | 已消失，后端全量测试 PASS |
| 前端 `MyDispatched` returned 用例失败 | 已消失，前端全量测试 PASS |
| 后端 build/test | PASS |
| 前端 build/test | PASS |
| smoke:live / verify:phase56 | PASS（当前 localhost 服务） |
| 江璐社保模块泄露 | FAIL，live API 仍返回 `social_insurance` |
| 傅倩雯只见社保增/减 | 部分通过：列表仅 `social_insurance`；未验证减员 `resignation_social_insurance` 数据与 active handler |
| 业务员/组长导入权限 | API 通过（空数据 400 非 403）；UI 未完成最终验证 |
| 业务负责人/后道导入 403 | PASS |
| dashboard/cards 权限过滤与待处理字段 | FAIL：未返回 total/month pending 字段；0 卡片 UI 未完成最终验证 |
| order-type-matrix / leader-trend 一期隐藏 | PASS：未返回 renewal/benefit/change；benefit trend 404 |
| 社保公积金状态流端到端 | 未完成，不能作为 GO 证据 |

## 六、可演示账号/路径

默认密码均为 `123456`：

| 角色 | 账号 | 路径 |
|---|---|---|
| 管理员 | `lizhanbo` | `/dashboard`、管理接口 |
| 业务员 | `yaoyiping` | `/dashboard`、`/work-orders`、`/work-orders/import` |
| 业务组长 | `shenwenjun` | `/dashboard`、`/work-orders`、`/work-orders/import` |
| 业务负责人 | `aolei` | `/dashboard`、`/my-work/team`、`/my-work/history` |
| 合同后道 | `yangchun` | 合同子工单列表、`/dashboard` |
| 入离职联系 | `maoyani` | 入职联系/离职材料收集、`/dashboard` |
| 共享负责人 | `jianglu` | 合同+入离职联系相关列表、`/dashboard`，但当前 live API 泄露社保 |
| 数据录入 | `annazhen` | 增员/减员报岗录入、`/dashboard` |
| 社保公积金 | `fuqianwen` | 社保公积金增员/减员、`/dashboard` |

## 七、最终 GO/NO-GO

**NO-GO。**

放行前必须至少完成：
1. 修复或解释 live API 中江璐返回 `social_insurance` 的问题，并用接口复测证明不再泄露。
2. `/dashboard/cards` 明确返回并验证 `totalPending/monthPending` 或等价字段，证明历史遗留待办不会月初清零。
3. 校准运行库 `social_insurance` / `resignation_social_insurance` handler active 配置，证明傅倩雯负责增/减员，江璐不参与社保。
4. 完成社保公积金状态流端到端验证。
5. 完成关键角色 UI 菜单/导入入口/无权限 0 卡片验证，且不再依赖长脚本超时结果。
