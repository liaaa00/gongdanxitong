# 0603 修复最终二次全量回归报告

- 验证日期：2026-06-03（Asia/Shanghai）
- 验证角色：全量回归与演示验收工程师
- 验证基线：integration tracked commit `7b1aafb0bf0c94f584a4e910b6397437df78fbad`（`fix import notification names and handler seeds`）
- 验证工作树：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998\.spectrai-worktrees\qa-validations\0603-final-7b1aafb0`
- 说明：integration 根工作区存在未提交业务改动，本轮 build/test 只在 detached clean worktree 上执行；`node_modules` 通过 junction 指向主工作区已安装依赖。报告不使用主工作区未提交改动作为通过证据。

## 结论：NO-GO

阻塞原因：最新 integration tracked commit 的后端单测与前端单测均未通过。虽然后端/前端构建通过，且当前 localhost 的两条短冒烟脚本通过，但 release gate 仍被单测失败阻断。

## 一、命令执行结果

| 类别 | 执行目录 | 命令 | 结果 | 关键证据 |
|---|---|---|---|---|
| 后端构建 | clean worktree `/backend` | `npm run build` | PASS | `nest build && tsc-alias` 成功，退出码 0 |
| 后端单测 | clean worktree `/backend` | `npm test` | FAIL | `test/control-flow-regression.spec.ts` 1 个用例失败；44 passed、1 skipped suites；298 passed、16 skipped tests |
| 前端构建 | clean worktree `/frontend` | `npm run build` | PASS | `tsc -b && vite build` 成功，4590 modules transformed，退出码 0 |
| 前端单测 | clean worktree `/frontend` | `npm test` | FAIL | `src/layouts/BasicLayout.test.tsx` 3 个用例失败；33 passed / 34 files；163 passed / 166 tests |
| live smoke | clean worktree `/frontend`，目标 `http://localhost:3000` | `npm run smoke:live` | PASS | 登录、auth/me、work-orders、admin/users、dashboard/salesperson 均返回成功 |
| phase5/6 smoke | clean worktree `/frontend`，目标 `http://localhost:3000` | `npm run verify:phase56` | PASS | 工单详情、子工单详情、通知、导出模板、dashboard processor、SSE 均返回成功 |

> 注意：两条 smoke 脚本请求的是当前运行中的 `localhost:3000` 服务；它们不能抵消 tracked commit 单测失败，也不能证明该服务一定由本轮 `7b1aafb0` 干净代码启动。

## 二、后端失败明细

### 2.1 江璐模块范围仍包含第一期应隐藏/不应展示的模块

- 失败文件：`backend/test/control-flow-regression.spec.ts`
- 失败用例：`shared leader 江璐 module scope is Yang Chun plus Mao Yani phase-1 modules only`
- 断言结果：
  - 期望：`contract,onboarding_contact,resignation_contact`
  - 实际：`contract,onboarding_contact,renewal_contract,resignation_contact`
- 影响验收点：
  - 第一阶段在职/续签相关模块需对业务和后道隐藏；
  - 江璐应为杨纯+毛雅妮合集，不能超出第一期可见范围；
  - `/dispatched-orders` 等后道列表权限仍存在越权/超范围风险。

### 2.2 已确认改善项

- `backend/test/benefit-state-machine.spec.ts` 在本轮后端全量单测中通过，上一轮提到的 `benefit-state-machine` 阻断未复现。
- `backend/test/import-permissions.spec.ts` 在本轮后端全量单测中通过，覆盖：业务员/业务组长可导入第一期入职/离职，业务负责人和后道角色命中导入 API 时应被阻断，在职导入类型第一期被阻断。
- `backend/test/control-flow-regression.spec.ts` 中傅倩雯社保增/减与安娜祯减员报岗种子断言未再作为失败项出现，说明 `7b1aafb0` 对 handler seed 的一部分修复已生效；但江璐 scope 仍未收敛。

## 三、前端失败明细

### 3.1 业务员菜单仍显示 `入职管理`

- 失败文件：`frontend/src/layouts/BasicLayout.test.tsx`
- 失败用例：`keeps salesperson initiated, returned and history without team switch`
- 断言：期望菜单不包含 `入职管理`，实际菜单文本为 `仪表盘入职管理主工单列表离职管理离职主工单列表我的工单我发起的我的退回历史工单消息通知`。
- 影响：业务员菜单口径与当前前端测试预期不一致，需要产品/测试口径确认。0603 P0 又要求业务员恢复入职导入入口，因此此处可能是测试预期与最新需求存在冲突；但在当前 tracked commit 中它是红灯，release gate 不可忽略。

### 3.2 业务组长菜单仍显示 `我发起的` / `我的退回`

- 失败文件：`frontend/src/layouts/BasicLayout.test.tsx`
- 失败用例：`keeps business group leader on team and history work only`
- 断言：期望不包含 `我发起的`，实际菜单文本包含 `我发起的`、`我的退回`、`团队工单`、`历史工单`。
- 影响：业务组长在业务员基础上拥有团队视角的口径，与当前测试“仅团队/历史”预期不一致；需明确最终菜单验收口径并同步前端测试。

### 3.3 江璐菜单标签/范围不一致

- 失败文件：`frontend/src/layouts/BasicLayout.test.tsx`
- 失败用例：`keeps shared team owner on Yang Chun plus Mao Yani modules only`
- 断言：期望包含 `劳动合同续签子工单`，实际菜单仍显示 `劳动合同签订子工单` 且缺少 `劳动合同续签子工单`。
- 影响：子工单名称更新与江璐合集菜单仍不一致；也与后端江璐 scope 失败共同指向江璐权限/菜单矩阵未稳定。

## 四、0603 P0 验收矩阵复测状态

| 验收项 | 本轮状态 | 证据/说明 |
|---|---|---|
| 业务员/业务组长入职导入入口恢复 | 部分通过但需 UI 复核 | 后端 `import-permissions.spec.ts` PASS；前端菜单单测仍 FAIL，业务员/组长菜单预期需与 0603 口径重新对齐 |
| 业务负责人/后道导入 403 兜底 | 单测通过 | 后端 `import-permissions.spec.ts` PASS，覆盖 business_owner、biz_manager、后道角色 |
| 第一阶段隐藏在职模块 | FAIL | 后端江璐 scoped modules 实际包含 `renewal_contract`；前端江璐菜单对续签/合同标签也不一致 |
| 仪表盘按权限隐藏无关模块、不显示 0 卡片 | 未形成最终通过证据 | `dashboard.spec.ts` 本轮全量单测通过；但因后端/前端 gate 已失败，未再执行长 API 全角色探测。需在修复后复跑 dashboard/cards、order-type-matrix、leader-trend 权限探测 |
| 后道账号只显示负责模块；傅倩雯仅社保增/减 | 部分通过/需复核 | 后端傅倩雯 seed 相关断言未失败；江璐仍超范围失败，说明后道矩阵整体不能 GO |
| 子工单名称与负责人配置正确 | FAIL | 前端仍显示/断言合同标签不一致；江璐合同续签显示缺失；后端江璐 scope 含 `renewal_contract` |
| 看板总待处理/单月待处理 | 未完成端到端确认 | 本轮未重复长 API 探测；修复后需确认 `/dashboard/cards` 返回并展示 `totalPending/monthPending` 或约定等价字段，且历史遗留待办不因月初归零 |
| 社保公积金状态流 | 单测覆盖部分，通过；端到端未完成 | `benefit-state-machine.spec.ts` PASS；但“未接单自由修改/撤回/作废、接单后审批/锁定、完成后禁止”仍需在修复后以 API/UI 端到端流程复核 |
| 我的工单子工单级展示、团队工单只读详情 | 未完成端到端确认 | `verify:phase56` 仅覆盖详情基础可访问；未覆盖全角色只读/不可操作矩阵 |
| live smoke / phase5-6 | PASS（非 gate 通过） | 当前 localhost 两条短脚本通过；但不能覆盖单测红灯 |

## 五、建议的最小复测清单（修复后）

1. 后端：`cd backend && npm test`，重点确认 `test/control-flow-regression.spec.ts` 全绿，江璐 scoped modules 不再包含 `renewal_contract` / 社保 / 报岗 / 待遇。
2. 后端：`cd backend && npm run build`。
3. 前端：`cd frontend && npm test`，重点确认 `src/layouts/BasicLayout.test.tsx` 角色菜单用例与 0603 最新口径一致并全绿。
4. 前端：`cd frontend && npm run build`。
5. 当前服务启动后短冒烟：`cd frontend && npm run smoke:live`、`cd frontend && npm run verify:phase56`。
6. API 角色探测：
   - 业务员/业务组长：`/work-orders` 与 `/work-orders/import/preview|confirm` 应可用；
   - 业务负责人/后道：导入接口应 403；
   - 江璐 `/dispatched-orders` 只返回杨纯+毛雅妮合集且第一期可见模块；
   - 傅倩雯/付倩雯只返回 `social_insurance`、`resignation_social_insurance`；
   - `dashboard/cards` 不返回无权限 0 卡片，并包含总待处理/单月待处理字段；
   - `order-type-matrix`、`leader-trend` 不返回第一期隐藏的在职/待遇模块。
7. 社保公积金状态流端到端：未接单可自由修改/撤回/作废；接单/已受理后业务操作需后道同意或审批；已完成不可直接操作。

## 六、可演示账号/路径建议

> 以下为演示路径清单；因当前结论为 NO-GO，正式演示前必须先修复单测红灯并完成 API/UI 复核。

- 业务员：`/dashboard`、`/work-orders`、`/work-orders/import`、`/work-orders/new`、`/work-orders/history`
- 业务组长：`/dashboard`、`/work-orders`、`/work-orders/import`、`/work-orders/team`、`/work-orders/history`
- 业务负责人：`/dashboard`、`/work-orders/team`、`/work-orders/history`；导入路径应禁止
- 杨纯：劳动合同新签/续签负责模块列表与详情
- 毛雅妮：入职联系/离职材料收集负责模块列表与详情
- 江璐：杨纯+毛雅妮合集，但第一阶段不得暴露在职/社保/报岗/待遇无关模块
- 安娜祯：增员报岗录入、减员报岗录入
- 傅倩雯/付倩雯：社保公积金增员、社保公积金减员

## 七、最终判定

**NO-GO。**

必须先修复：
1. 后端江璐模块 scope 泄露 `renewal_contract`；
2. 前端 `BasicLayout` 角色菜单 3 个失败用例，尤其业务员/业务组长菜单口径与江璐合集/子工单名称；
3. 修复后复跑后端/前端 test/build，并重新做角色 API/UI 与社保状态流端到端复核。
