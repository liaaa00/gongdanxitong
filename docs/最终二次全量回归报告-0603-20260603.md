# 0603 修复最终二次全量回归报告

- 验证日期：2026-06-03（Asia/Shanghai）
- 验证角色：全量回归与演示验收工程师
- 验证基线：integration tracked commit `e4c3338c`（`fix workorder readonly phase1 interactions`）
- 验证工作树：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998\.spectrai-worktrees\qa-validations\0603-final-e4c3338c`
- 说明：integration 根工作区存在未提交业务改动，本轮 build/test 只在 detached clean worktree 上执行；`node_modules` 通过 junction 指向主工作区已安装依赖。报告不使用主工作区未提交改动作为通过证据。

## 结论：NO-GO

阻塞原因：最新 tracked commit 的后端 build/test 已全绿，前端 build 通过，两条短冒烟通过；但前端全量单测仍有 1 个失败用例，release gate 未全绿，不能给 GO。

## 一、命令执行结果

| 类别 | 执行目录 | 命令 | 结果 | 关键证据 |
|---|---|---|---|---|
| 后端构建 | clean worktree `/backend` | `npm run build` | PASS | `nest build && tsc-alias` 成功，退出码 0 |
| 后端单测 | clean worktree `/backend` | `npm test` | PASS | 46 passed / 1 skipped suites；303 passed / 16 skipped tests；`control-flow-regression.spec.ts`、`social-insurance-state-flow.spec.ts`、`benefit-state-machine.spec.ts`、`import-permissions.spec.ts` 均通过 |
| 前端构建 | clean worktree `/frontend` | `npm run build` | PASS | `tsc -b && vite build` 成功，4590 modules transformed，退出码 0 |
| 前端单测 | clean worktree `/frontend` | `npm test` | FAIL | `src/layouts/BasicLayout.test.tsx` 1 个用例失败；33 passed / 34 files；172 passed / 173 tests |
| live smoke | clean worktree `/frontend`，目标 `http://localhost:3000` | `npm run smoke:live` | PASS | 登录、auth/me、work-orders、admin/users、dashboard/salesperson 均返回成功 |
| phase5/6 smoke | clean worktree `/frontend`，目标 `http://localhost:3000` | `npm run verify:phase56` | PASS | 工单详情、子工单详情、通知、导出模板、dashboard processor、SSE 均返回成功 |

> 注意：两条 smoke 脚本请求的是当前运行中的 `localhost:3000` 服务；它们不能抵消前端单测失败，也不能证明该服务一定由本轮 `e4c3338c` 干净代码启动。

## 二、失败明细

### 2.1 前端通知下拉本地化文案单测失败

- 失败文件：`frontend/src/layouts/BasicLayout.test.tsx`
- 失败用例：`BasicLayout notification dropdown > uses localized display content instead of raw backend content in top dropdown`
- 失败断言：`Unable to find an element with the text: 劳动合同新签反馈 更新`
- 失败位置：`BasicLayout.test.tsx:244-246`
- 期望：通知下拉展示本地化文案 `劳动合同新签反馈 更新`，正文包含 `杨纯 修改了【子工单】：【劳动合同新签反馈】由【待确认】改为【已完成签订】`，并且不显示原始内部 key `contract_feedback`。
- 实际：测试点击通知铃后未找到目标本地化标题，导致 waitFor 超时失败。
- 影响：这是前端通知展示质量问题，不属于 0603 P0 菜单/权限主链路，但属于前端 release gate 红灯，且影响演示时顶部通知文案可信度，因此维持 NO-GO。

## 三、上一轮 NO-GO 阻断项复测状态

| 上一轮阻断/重点项 | 本轮状态 | 证据 |
|---|---|---|
| `benefit-state-machine` 失败 | 已消失 | 后端 `benefit-state-machine.spec.ts` PASS |
| 江璐 `/dispatched-orders` 范围含社保/报岗/待遇或在职续签泄露 | 单测层面已消失 | 后端 `control-flow-regression.spec.ts` PASS，江璐 phase-1 scope 用例已通过 |
| 傅倩雯/付倩雯只见社保增/减，handler seed active | 单测层面已通过 | 后端 `control-flow-regression.spec.ts` PASS，傅倩雯社保增/减与在职隐藏 seed 用例通过 |
| 社保公积金状态流：未接单自由操作、接单后锁定/审批、完成后禁止 | 单测层面已通过 | 后端 `social-insurance-state-flow.spec.ts` PASS |
| 业务员/业务组长导入入口与导入权限；业务负责人/后道导入 403 | 单测层面已通过 | 后端 `import-permissions.spec.ts` PASS |
| 第一阶段在职模块隐藏、order-type-matrix/leader-trend | 单测层面已通过 | 后端 `dashboard.spec.ts` PASS；前端菜单权限相关测试未再失败 |
| 业务员/业务组长/业务负责人/后道菜单权限 | 前端单测主链路已改善 | `BasicLayout.test.tsx` 角色菜单相关用例不再失败；当前唯一失败为通知下拉本地化 |
| 我的工单子工单级展示、团队工单只读详情 | 基础冒烟通过，仍需演示复核 | `verify:phase56` 覆盖详情基础可访问；未替代全角色手工 UI 验收 |
| dashboard/cards 总待处理/单月待处理与无权限 0 卡片 | 单测/短冒烟未形成完整端到端证明 | `dashboard.spec.ts` PASS，`verify:phase56` dashboard processor PASS；仍建议修复前端测试后做一次角色 API/UI 抽检 |

## 四、0603 P0 验收矩阵状态

| 验收项 | 本轮判定 | 说明 |
|---|---|---|
| 业务员入职模块和入职导入入口恢复 | 需最终 UI 演示复核 | 后端导入权限单测通过；短 smoke work-orders 成功；前端菜单角色用例不再是失败项 |
| 业务员/业务组长/业务负责人菜单权限 | 单测主链路通过 | 当前前端失败不在菜单权限用例；仍建议最终演示走账号验证 |
| 第一阶段在职模块隐藏 | 单测通过 | 后端 dashboard/control-flow 相关用例通过，前端角色菜单用例不再失败 |
| 仪表盘按权限隐藏无关模块、不显示 0 卡片 | 部分通过，需 UI/API 抽检 | 后端 dashboard 单测与 phase5/6 dashboard processor 冒烟通过；未做长全角色 API 探测 |
| 后道账号只显示负责模块，付/傅倩雯只见社保增减 | 单测通过，需演示复核 | 后端 handler seed/scope 用例通过；建议最终演示检查后道列表 |
| 子工单名称与负责人配置 | 单测主链路通过 | 前后端相关角色菜单和 handler 用例不再失败；通知下拉本地化仍需修复 |
| 看板总待处理/单月待处理 | 单测/短冒烟不足以完全证明 | 需在最终 GO 前对 `/dashboard/cards` 字段和 UI 展示做一次短 API/UI 复核 |
| 社保公积金状态流 | 后端单测通过 | `social-insurance-state-flow.spec.ts` PASS；仍建议演示前做端到端数据流抽检 |
| 我的工单子工单级展示、团队工单只读详情 | 基础通过，需角色 UI 抽检 | `verify:phase56` 基础详情通过，`e4c3338c` 提交说明为只读交互修复；未跑长 UI 全角色脚本 |

## 五、可演示账号/路径建议

> 以下为演示路径清单；因当前结论为 NO-GO，正式演示前必须先修复前端通知下拉单测红灯，并补一次关键角色 UI/API 抽检。

- 业务员：`/dashboard`、`/work-orders`、`/work-orders/import`、`/work-orders/new`、`/work-orders/history`
- 业务组长：`/dashboard`、`/work-orders`、`/work-orders/import`、`/work-orders/team`、`/work-orders/history`
- 业务负责人：`/dashboard`、`/work-orders/team`、`/work-orders/history`；导入路径应禁止
- 杨纯：劳动合同新签/续签负责模块列表与详情
- 毛雅妮：入职联系/离职材料收集负责模块列表与详情
- 江璐：杨纯+毛雅妮合集，但第一阶段不得暴露在职/社保/报岗/待遇无关模块
- 安娜祯：增员报岗录入、减员报岗录入
- 傅倩雯/付倩雯：社保公积金增员、社保公积金减员

## 六、最终判定与剩余风险

**NO-GO。**

必须修复：
1. 前端 `BasicLayout` 通知下拉本地化用例失败，需让顶部通知展示 `劳动合同新签反馈 更新` 等本地化文案且不泄露 `contract_feedback`。

修复后最小复测：
1. `cd frontend && npm test`，确认 `BasicLayout.test.tsx` 全绿；
2. `cd frontend && npm run build`；
3. `cd backend && npm test`、`cd backend && npm run build` 作为回归确认；
4. `cd frontend && npm run smoke:live`、`cd frontend && npm run verify:phase56`；
5. 对业务员、业务组长、业务负责人、杨纯、毛雅妮、江璐、安娜祯、傅倩雯/付倩雯做一次短 UI/API 抽检，重点看入职导入、dashboard/cards、后道列表、团队工单只读、社保状态流。
