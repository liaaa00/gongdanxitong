# 0603 全量回归与演示验收报告

- 返工验证时间：2026-06-03 12:17-12:25（Asia/Shanghai）
- 验证目录：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998`
- integration HEAD：`a54361a81f1ee8b1e1354a00ff03b1d74f57a999`（本 QA 文档提交）
- 验证原则：不修改业务实现，不回滚其他成员改动；仅更新 QA 报告交付物。

## 最终结论

**NO-GO**

原因：在指定 integration worktree 内，后端依赖缺失且安装失败，导致后端 build/test 无法执行；前端 build 通过，但前端 test 仍失败 1 个用例。live smoke 与 phase56 在当前本机运行服务上通过，但该运行服务不能证明 integration 后端构建产物可发布。按质量门槛，后端 build/test 未通过或不可执行、前端 test 未全绿，均不能 GO。

## 执行命令与结果（integration worktree）

| 类别 | 命令 | 结果 |
|---|---|---|
| 后端依赖检查 | `Test-Path backend\node_modules` | 初始为 False |
| 后端依赖安装 | `cd backend && npm ci` | FAIL：无 `package-lock.json`，`npm ci` 不可用 |
| 后端依赖安装 | `cd backend && npm install` | FAIL：`bcrypt` 预构建包下载 GitHub 超时，fallback 编译缺 Visual Studio C++ Build Tools |
| 后端构建 | `cd backend && npm run build` | FAIL/环境阻断：`nest` is not recognized |
| 后端测试 | `cd backend && npm test` | FAIL/环境阻断：`jest` is not recognized |
| 前端依赖安装 | `cd frontend && npm install` | PASS：安装 622 packages；存在 6 个 npm audit 风险（2 moderate, 4 high） |
| 前端构建 | `cd frontend && npm run build` | PASS：`tsc -b && vite build` 成功，4622 modules transformed |
| 前端测试 | `cd frontend && npm test` | FAIL：34 files 中 1 failed，158 tests 中 1 failed |
| live 冒烟 | `cd frontend && npm run smoke:live` | PASS：登录、auth/me、work-orders、admin/users、dashboard/salesperson 成功 |
| Phase 5/6 live 校验 | `cd frontend && npm run verify:phase56` | PASS：详情、通知、导出模板、dashboard processor、SSE 成功 |

> 说明：`smoke:live` 与 `verify:phase56` 请求的是 `http://localhost:3000` 当前运行服务。由于 integration 后端依赖安装失败、后端 build/test 未能执行，该 live 服务不能等同于本 integration worktree 的后端产物。

## 前端测试失败明细

### `src/pages/MyDispatched/index.test.tsx`

- 用例：`MyDispatched processing status filter > shows returned work as child-order rows only`
- 失败现象：期望 `[{ id: 'd-returned', status: 'returned' }]`，实际为 `[]`。
- 影响：我的工单/后道子工单级展示中“退回/returned”筛选仍存在回归风险；与 0603 要求“我的工单继续保持子工单级展示”相关。

## 后端环境阻断明细

1. integration 工作树初始无 `backend/node_modules`，`npm run build` 直接失败：`'nest' is not recognized`。
2. `npm ci` 因无 lock 文件失败，无法 clean install。
3. `npm install` 在 `bcrypt@5.1.1` 安装阶段失败：
   - 预构建包下载 `https://github.com/kelektiv/node.bcrypt.js/...` 超时；
   - fallback node-gyp 编译因 Windows 缺 Visual Studio C++ Build Tools 失败。
4. 由于后端依赖未安装完成，`npm test` 失败：`'jest' is not recognized`。

## 关键角色与业务验收覆盖状态

由于 integration 中缺少 `tmp\qa_0603_api_probe.mjs`、`tmp\qa_0603_config_probe.mjs`、`frontend\tmp\qa_0603_ui_probe.mjs`，本轮无法在 integration worktree 复用首轮角色探测脚本。结合当前执行结果，以下项仍需二次回归：

1. 业务员/业务组长：`yaoyiping`、`shenwenjun` 是否可见 `/work-orders/import` 入职导入入口，且 `/work-orders` 无动态模块加载异常。
2. 业务负责人：`aolei` 是否只看 dashboard 和我的工单团队/历史，访问主工单发起/导入为 403。
3. 后道角色：`yangchun`、`maoyani`、`jianglu`、`annazhen`、`fuqianwen` 是否只显示负责模块。
4. 第一阶段隐藏：业务和后道菜单、dashboard、后道列表均不出现“在职/续签/待遇”。
5. dashboard 权限过滤：无权限模块不返回/不显示 0 卡片或无关 0 子节点。
6. `totalPending/monthPending`：总待处理不因月份切换或月初而清零历史遗留待办。
7. 江璐：不得返回/显示 `social_insurance`、`resignation_social_insurance`、`data_entry`、`benefit_apply`。
8. 傅倩雯：只见社保公积金增员/减员；运行库 handler active 与 seed/migration 一致。
9. 社保公积金状态流：未接单前自由修改/撤回/作废；接单后需后道同意；已完成不可直接操作。
10. 团队工单只读详情、后道我的工单/我的待办只读并回负责子工单列表操作。

## 已通过项

1. 前端 build 在 integration worktree 通过。
2. 当前运行服务的基础 live smoke 通过。
3. 当前运行服务的 phase56 smoke 通过。
4. QA 报告已作为 integration tracked 文件提交，避免仅存在于成员摘要或主工作区。

## 阻塞项 / NO-GO 原因

1. 后端 build/test 在 integration worktree 不可执行，环境阻断未解除。
2. 前端 test 未全绿，仍有 `MyDispatched` returned 子工单筛选失败。
3. 关键角色 API/UI 探测脚本在 integration worktree 缺失，本轮无法提供 integration 内的菜单/权限/仪表盘自动证据。
4. 社保公积金状态流端到端未完成验证。
5. live smoke 不代表 integration 后端产物。

## 可演示账号与路径（待二次验证）

默认密码均为 `123456`：

| 角色 | 账号 | 待验证路径 |
|---|---|---|
| 管理员 | `lizhanbo` | `/dashboard`、管理接口 |
| 业务员 | `yaoyiping` | `/dashboard`、`/work-orders`、`/work-orders/import` |
| 业务组长 | `shenwenjun` | `/dashboard`、`/work-orders`、`/work-orders/import`、团队视角 |
| 业务负责人 | `aolei` | `/dashboard`、`/my-work/team`、`/my-work/history` |
| 合同后道 | `yangchun` | 合同子工单列表、`/dashboard` |
| 入离职联系 | `maoyani` | 入职联系/离职材料收集、`/dashboard` |
| 共享负责人 | `jianglu` | 合同+入离职联系相关列表、`/dashboard` |
| 数据录入 | `annazhen` | 增员/减员报岗录入、`/dashboard` |
| 社保公积金 | `fuqianwen` | 社保公积金增员/减员、`/dashboard` |

## GO/NO-GO 判定

**NO-GO。**

放行前至少需要：

1. 解决 integration 后端依赖安装问题，确保 `cd backend && npm run build`、`cd backend && npm test` 可执行并通过。
2. 修复前端 `MyDispatched` returned 子工单筛选失败，确保 `cd frontend && npm test` 全绿。
3. 将角色 API/配置/UI 探测脚本纳入 integration 或提供等价自动检查，并完成关键角色权限、dashboard、导入入口验证。
4. 完成社保公积金状态流端到端验证。
5. 确认 live smoke 指向的服务确实由当前 integration 构建产物启动。
