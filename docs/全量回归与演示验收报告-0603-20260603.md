# 0603 全量回归与演示验收报告

- 验证时间：2026-06-03 11:35-11:58（Asia/Shanghai）
- 验证目录：`D:\AI\SpeceAppDate\工单系统`
- 集成基线说明：本报告为 QA 全量回归任务交付物，补入 integration 工作树供评审复核；命令结果来自 0603 全量回归执行记录，live smoke 使用当时本机已运行的后端 3000 / 前端 5173 服务。
- 验证原则：未回滚他人改动，未做业务实现修改；只产出 QA 报告与验收结论。

## 最终结论

**NO-GO**

原因：重派后后端 build、前端 build、前端 test 均已通过，但后端 `npm test` 仍有 1 个 suite / 1 个用例失败（待遇申报 benefit 状态机在第一阶段隐藏逻辑下返回“子工单不存在”）。同时 live 角色探测仍发现江璐 API 返回 `social_insurance`、业务员/组长 `/work-orders` 动态模块加载异常、dashboard 仍有无关 0 模块/子节点展示风险，社保公积金状态流未完成端到端验证。因此不建议上线/正式演示放行。

## 执行命令与结果

| 类别 | 命令 | 结果 |
|---|---|---|
| 后端构建 | `cd backend && npm run build` | PASS |
| 后端测试 | `cd backend && npm test` | FAIL：1 failed suite / 44 passed / 1 skipped；1 failed test / 300 passed / 16 skipped |
| 前端构建 | `cd frontend && npm run build` | PASS |
| 前端测试 | `cd frontend && npm test` | PASS：34 files passed，168 tests passed |
| 基础 live 冒烟 | `cd frontend && npm run smoke:live` | PASS：登录、auth/me、work-orders、admin/users、dashboard/salesperson 均返回成功 |
| Phase 5/6 live 校验 | `cd frontend && npm run verify:phase56` | PASS：详情、通知、导出模板、dashboard processor、SSE 等通过 |
| 角色 API 探测 | `node tmp\qa_0603_api_probe.mjs` | 完成；发现江璐列表含 `social_insurance` 等风险 |
| 角色 UI 探测 | `node frontend\tmp\qa_0603_ui_probe.mjs` | 完成；导入入口/403/总待处理可验证，但 `/work-orders` 动态加载异常 |

> 说明：live smoke 使用当前已运行服务（后端 3000、前端 5173），只能证明运行环境基础可用；不能替代 build/test，也不代表当前代码已满足发布质量门槛。

## 已通过/可演示项

1. **业务员/业务组长入职导入入口恢复**：UI 探测确认 `yaoyiping`、`shenwenjun` 可进入 `/work-orders/import`，页面展示“入职导入”、模板下载、上传、字段校验、规则验证等导入流程元素。
2. **业务负责人/后道导入权限限制**：`aolei`、`yangchun`、`maoyani`、`jianglu`、`annazhen`、`fuqianwen` 访问 `/work-orders/import` 为 403；API 空 preview/confirm 对负责人/后道返回 403。
3. **第一阶段在职隐藏（UI 文案层面）**：角色 UI 探测中各角色 dashboard/menu 未检出“在职/续签/待遇”等文字。
4. **看板总待处理/单月待处理**：各角色 UI 探测均可见“总待处理”和“单月待处理”，文案明确总待处理不受月份影响。
5. **后道基础隔离（部分通过）**：API 显示 `yangchun` 只返回 `contract`，`maoyani` 只返回 `onboarding_contact`，`annazhen` 只返回 `data_entry`，`fuqianwen` 只返回 `social_insurance`；后道访问主工单/导入页面为 403。
6. **傅倩雯演示方向**：`fuqianwen` 登录后 dashboard 可见社保公积金增员相关统计，API dispatched list 仅返回 `social_insurance`。
7. **前端质量门槛已恢复**：前端 build/test 均通过。
8. **live 冒烟基础服务**：登录、鉴权、列表、通知、导出模板、SSE 等基础接口在当前运行环境可用。

## 阻断失败项

### 后端测试唯一失败

`test/benefit-state-machine.spec.ts`

- 用例：`P7 benefit state machine › moves draft to submitted and writes an order_stages row`
- 现象：`DispatchedOrderService.loadDispatchedOrder` 因 `benefit_apply` 不属于第一阶段可见模块，抛出 `NotFoundException: 子工单不存在`。
- 判断：该用例属于暂缓的待遇申报流程，与 0603 “待遇申报详细流程后续阶段、不在本轮复杂实现”口径相关。需要由后端/需求确认：是将该测试调整为第一阶段隐藏验证，还是保留 P7 流程但绕过第一阶段隐藏限制。无论哪种，当前后端 `npm test` 未全绿，不能作为 GO 放行。

## 业务验收风险项

1. **江璐权限未完全达标**：API 探测中 `jianglu` 的 `/dispatched-orders` 返回 `contract,onboarding_contact,social_insurance`，与“江璐=杨纯+毛雅妮合集，不含社保公积金”的口径冲突。
2. **无权限/无数据模块仍可能显示 0**：UI dashboard 样本中业务/后道仍出现大类或子节点 0 行，例如业务员看到入职下多个 0 子节点、后道看到入职/离职大类 0 统计；需产品确认是否属于“无关模块 0 卡片”禁止范围，建议继续收敛为仅显示有权限模块/子模块。
3. **业务员/业务组长 `/work-orders` 页面异常**：UI 探测对 `yaoyiping`、`shenwenjun` 打开 `/work-orders` 出现 ErrorBoundary：`Failed to fetch dynamically imported module: .../WorkOrders/index.tsx`，影响主工单列表演示稳定性。
4. **社保公积金状态流未完成端到端验证**：本轮未创建完整“未接单自由修改/撤回/作废、接单后后道同意、已完成不可直接操作”的 E2E 链路；不能给出放行结论。
5. **权限后端兜底仍需复核**：后道导入/主工单 403 已通过，但江璐 API 泄露社保模块说明后端模块范围过滤仍有缺口或运行数据与 seed 不一致。
6. **运行库与 seed 可能不一致**：配置探测 `/admin/module-handlers` 中社保 active 配置与 live 列表表现存在不一致，需要明确迁移/seed 执行策略。

## 角色验收清单覆盖情况

| 角色 | 账号/密码 | 路径 | 当前验收结论 |
|---|---|---|---|
| 管理员 | `lizhanbo / 123456` | `/dashboard`、管理接口 | live smoke 可用 |
| 业务员 | `yaoyiping / 123456` | `/dashboard`、`/work-orders/import` | 导入入口可演示；`/work-orders` 动态加载异常，NO-GO 风险 |
| 业务组长 | `shenwenjun / 123456` | `/dashboard`、`/work-orders/import`、团队视角 | 导入入口可演示；`/work-orders` 动态加载异常，NO-GO 风险 |
| 业务负责人 | `aolei / 123456` | `/dashboard`、`/my-work/team`、`/my-work/history` | 主工单/导入 403 符合；需继续确认团队/历史只读详情 |
| 合同后道 | `yangchun / 123456` | 合同子工单列表、`/dashboard` | API 仅合同模块；dashboard 0 模块展示需收敛 |
| 入离职联系 | `maoyani / 123456` | 入职联系/离职材料收集、`/dashboard` | API 仅入职联系；离职材料收集需补数据/路径演示 |
| 共享负责人 | `jianglu / 123456` | 合同+入离职联系、`/dashboard` | API 泄露 `social_insurance`，不满足 0603 口径 |
| 数据录入 | `annazhen / 123456` | 增员/减员报岗录入、`/dashboard` | API 仅 `data_entry`；离职减员报岗需补演示验证 |
| 社保公积金 | `fuqianwen / 123456` | 社保公积金增员/减员、`/dashboard` | API 仅 `social_insurance`，增员可见；减员路径/状态流需补端到端验证 |

## GO/NO-GO 决策

**NO-GO。**

上线/正式演示前至少需要：

1. 修复或调整 `benefit-state-machine.spec.ts`，确保后端 `npm test` 全通过，且与第一阶段隐藏/暂缓待遇申报口径一致。
2. 修复江璐 `/dispatched-orders` 返回社保模块的问题。
3. 修复业务员/组长 `/work-orders` 动态加载异常。
4. 收敛 dashboard 权限过滤，确保无权限模块/无关 0 卡片不展示。
5. 补充并通过社保公积金状态流端到端验证。
6. 明确并执行目标环境 seed/migration，使负责人/处理人配置与 0603 会议口径一致。
