# 演示前终轮烟测报告 - 2026-05-14

> ⚠️ 当前演示口径（2026-06-03）：所有 seed/演示账号默认密码统一为 `123456`；`admin123` 是历史旧口径，会返回 401，不可用于演示。本文下方如出现 `admin123`，仅为历史记录/旧脚本背景，不代表当前可用密码。

> 角色：测试工程师  
> 环境时间：2026-05-14 18:41-18:55（Asia/Shanghai）  
> 前端：`npm run build` 通过后使用 `vite preview`，Base URL `http://127.0.0.1:4173`  
> 后端：当前已运行后端 `http://127.0.0.1:3000/api`（任务允许使用当前 dev 后端）  
> 自动化脚本：`tests/final-smoke-20260514.mjs`

## 0. 总结

- 前端生产构建：PASS（`tsc -b && vite build` 成功）
- 生产预览：PASS（4173 可访问）
- 后端健康检查：PASS（`GET /api/health` = 200）
- 页面白屏 / ErrorBoundary：未发现
- P0：0
- P1：4
- P2：4
- SKIP/未完整自动化：3

> 额外风险：尝试启动最新 `backend npm run start:prod` 到 3001 失败，启动时报数据库 schema 缺列：`column User.group_code does not exist`。因此本轮接口烟测使用当前 3000 后端；该 schema 漂移会影响真正 prod 启动。

## A. P0 复验（3 页）

| 项 | 页面 | 结果 | 证据 |
|---|---|---|---|
| A1 | `/export-templates` | PASS | 无 ErrorBoundary；无 `rawData.some`；无 5xx；无重复 key。页面可渲染“导出模板 / 暂无数据”。控制台仅 favicon/静态 404，非接口 5xx。 |
| A2 | `/admin/export-templates` | PASS | 无 ErrorBoundary；无 `rawData.some`；无 5xx；无重复 key。页面可渲染导出模板配置列表。 |
| A3 | `/admin/fields` | PASS | 无 ErrorBoundary；无 `rawData.some`；无 5xx；无重复 key。页面可渲染字段配置表格。 |

结论：本轮未发现 P0 崩溃，无需即时上报 P0。

## B. P1 复验

| 项 | 场景 | 预期 | 实际 | 结果 | 严重级别 |
|---|---|---|---|---|---|
| B1 | `GET /api/work-orders/not-exist-qa` | 404/403 | 500，body：`{"code":1000,"message":"Internal server error"}` | FAIL | P1 |
| B2 | `GET /api/dispatched-orders/not-exist-qa` | 404/403 | 500，body：`{"code":1000,"message":"Internal server error"}` | FAIL | P1 |
| B3 | `/notifications` | 不出现 `pageSize must not be greater than 100` | 页面未崩溃，但正文出现 `pageSize must not be greater than 100`；Network：`400 /api/notifications?unread=true&page=1&pageSize=200` | FAIL | P1 |

代码位置推测：
- B1/B2：后端详情接口异常过滤/运行版本问题。源码中 `work-order.service.ts` / `dispatched-order.service.ts` 已有 `NotFoundException`，但当前 3000 运行实例仍返回 500，需确认部署进程与 dist/schema 是否一致。
- B3：前端通知页仍以 `pageSize=200` 请求，位置推测：`frontend/src/pages/Notifications` 或通知服务调用处；后端分页 DTO 上限 100。

## C. Stage A 联调

| 项 | 场景 | 结果 | 证据 / 说明 | 严重级别 |
|---|---|---|---|---|
| C1 | 后台客户/商社管理页面增删改（商社代码唯一冲突友好） | FAIL | 客户新增/重复/编辑通过：新增 201、重复 400 “客户编码已存在”、编辑 200；但商社接口缺失：`GET /api/admin/branches` = 404，`POST /api/admin/branches` = 404。 | P1 |
| C2 | 后台业务员↔客户绑定 | FAIL | `POST /api/admin/customer-assignees` = 404；当前运行后端未暴露该路由。 | P2 |
| C3 | 后台派发规则编辑 | PASS（部分） | `GET /api/admin/dispatch-rules` = 200；选取现有规则 PUT 编辑 = 200。备注：customer/group + 3 子工单负责人 + AB 角字段未完成 UI 级全量校验。 | - |
| C4 | 入职发起：同一身份证本月重复 → `DUPLICATE_ID_CARD_IN_MONTH` | FAIL | 补测使用 active 客户后，同一身份证连续创建两张草稿均 201，未触发重复身份证校验。 | P2 |
| C5 | 批导入：一行重复 → partial + 错误行分组跳原单 | SKIP | 本轮未自动上传 Excel；页面 `/work-orders/import` 已 sanity PASS。需用专用 Excel 夹具复跑。 | P2 |
| C6 | 改发起人字段 → 3 个下游 + 发起人收到站内消息 | FAIL | 因 C4/C1 造成工单提交链路未跑通；脚本无法完成 3 下游通知验收。 | P2 |
| C7 | 已办结工单修改：业务员可改字段 → 同步三快照 + 通知 | SKIP | 未构造完整已办结三子单闭环；需在 C4/C6 修复后复跑。 | P2 |
| C8 | AB 角认领：双窗口同时点只一人成功 | SKIP | 未成功构造可认领子工单，未执行并发认领。 | P2 |
| C9 | 首次登录强制改密：默认 123456 登录 → 拦截 → 改后不再拦截 | FAIL | `lizhanbo/123456` 返回 401 “用户名或密码错误”；共享账号使用 `admin123` 可登录。未对演示账号实际改密，避免破坏账号。 | P2 |

## D. 角色 sanity（生产预览）

| 角色账号 | 主路径 | 结果 | 备注 |
|---|---|---|---|
| 超管 `lizhanbo/admin123` | `/dashboard`, `/admin/users`, `/admin/customers`, `/admin/fields` | PASS | 无白屏；`/admin/customers` 页面可用但有 pageSize=200 的 400 文案。 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| Leader `aolei/admin123` | `/dashboard`, `/work-orders` | PASS | 无白屏。 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| 业务组长 `shenwenjun/admin123` | `/dashboard`, `/work-orders`, `/my-todos` | PASS | 无白屏。 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| 业务员 `yaoyiping/admin123` | `/dashboard`, `/work-orders/new`, `/work-orders/import` | PASS | 无白屏；`/work-orders/new` 有 403/404 和 unhandledrejection 文案，但页面仍可操作。 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| 入离职联系 `maoyani/admin123` | `/dashboard`, `/my-dispatched`, `/onboarding/onboarding_contact` | PASS | 无白屏；模块列表请求 `module_code` 被后端 DTO 拒绝，页面提示 `property module_code should not exist`。 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| 合同专员 `yangchun/admin123` | `/dashboard`, `/my-dispatched`, `/onboarding/contract` | PASS | 同上，模块列表 `module_code` 400。 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| 数据录入 `annazhen/admin123` | `/dashboard`, `/my-dispatched`, `/onboarding/data_entry` | PASS | 同上，模块列表 `module_code` 400。 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)

## 1. 失败清单（需修复/确认）

### P1-1：不存在主工单详情接口返回 500
- 复现：登录超管，调用 `GET /api/work-orders/not-exist-qa`
- 预期：404/403
- 实际：500 `Internal server error`
- 代码位置推测：`backend/src/modules/work-orders/work-order.service.ts`、全局 `HttpExceptionFilter`、当前运行 dist/进程版本
- 是否需前端修复：否，优先后端/部署修复

### P1-2：不存在子工单详情接口返回 500
- 复现：登录超管，调用 `GET /api/dispatched-orders/not-exist-qa`
- 预期：404/403
- 实际：500 `Internal server error`
- 代码位置推测：`backend/src/modules/dispatched-orders/dispatched-order.service.ts`、全局异常过滤器、当前运行 dist/进程版本
- 是否需前端修复：否，优先后端/部署修复

### P1-3：通知页仍请求 `pageSize=200`
- 复现：登录超管访问 `/notifications`
- 预期：不出现 `pageSize must not be greater than 100`
- 实际：页面显示该错误；Network：`400 /api/notifications?unread=true&page=1&pageSize=200`
- 代码位置推测：`frontend/src/pages/Notifications` 或 notifications service 默认分页参数
- 是否需前端修复：是

### P1-4：Stage A 商社接口未在当前后端暴露
- 复现：`GET /api/admin/branches`、`POST /api/admin/branches`
- 预期：商社列表/新增/唯一冲突校验可用
- 实际：404 `Cannot GET/POST /api/admin/branches`
- 代码位置推测：后端缺 `AdminBranchesController/Module` 或当前 3000 不是 Stage A 最新后端；前端调用位置：`frontend/src/services/branches.ts`、`frontend/src/pages/Admin/Customers/index.tsx`
- 是否需前端修复：否，优先后端路由/部署修复

## 2. P2 / 体验或联调缺口

1. `/api/admin/customer-assignees` 当前 404，业务员↔客户绑定无法验证。
2. 同一身份证本月重复创建草稿未触发 `DUPLICATE_ID_CARD_IN_MONTH`（active 客户补测也未触发）。需确认重复校验触发时机是草稿创建还是提交；按任务预期“入职发起”应拦截。
3. 子工单模块页请求参数为 `module_code`，后端报 `property module_code should not exist`；页面不崩但会展示错误。
4. `/work-orders/new` 业务员侧加载字段/权限时出现 403/404 与 unhandledrejection，页面不崩但演示体验较差。
5. 默认密码 `123456` 登录当前演示账号失败；若演示需要“首次登录强制改密”，需准备专门未改密账号。

## 3. 生产启动阻塞

尝试执行最新后端 `npm run build` 后再 `node backend/dist/main.js`（PORT=3001）失败：

```text
QueryFailedError: column User.group_code does not exist
at seedUsers ...
query: SELECT ... "User"."group_code" ... FROM "public"."users"
```

影响：真正 `start:prod` 可能无法在当前数据库上启动。建议演示前确认 migrations 已执行，或临时回滚实体/seed 对 `group_code` 的依赖。

## 4. ae14 复验（ace4e16d / 3a516c8a 后）

> 复验时间：2026-05-14 19:32-19:34  
> 后端：临时启动 `PORT=3101 npm run start:prod`，`GET /api/health` = 200  
> 前端：`VITE_API_BASE_URL=http://127.0.0.1:3101 npm run build` 后，临时 `vite preview` 端口 4174  
> 账号说明：3101 环境中旧密码 `admin123` 返回 401；默认密码 `123456` 可登录，且 `mustChangePassword=true`。

| 项 | 复验内容 | 结果 | 证据 |
|---|---|---|---|
| 1 | `PORT=3101 npm run start:prod` 能否启动 | PASS | `GET http://127.0.0.1:3101/api/health` → 200，body `data.status=ok`。 |
| 2 | `GET /api/work-orders/not-exist-qa` | FAIL | 返回 500，body：`code=1000, message=Internal server error`，traceId `req_1dd1c878-3c47-41d3-9c08-da64a99908de`。预期 404。 |
| 3 | `GET /api/dispatched-orders/not-exist-qa` | FAIL | 返回 500，body：`code=1000, message=Internal server error`，traceId `req_df478325-d60f-4ebf-b767-412d12a801d7`。预期 404。 |
| 4 | `GET /api/dispatched-orders/not-exist-qa/supplement-logs` | FAIL | 返回 500，body：`code=1000, message=Internal server error`，traceId `req_270f7bd1-b4ad-4dac-9684-a7b4282db3f6`。预期 404。 |
| 5 | `/api/admin/branches` GET/POST/PUT/DELETE | PASS | `GET /admin/branches?page=1&pageSize=20` → 200；`POST /admin/branches` → 201；`PUT /admin/branches/{id}` → 200；`DELETE /admin/branches/{id}` → 200。 |
| 6 | `/notifications` 不出现 pageSize=200/上限错误 | PASS | Network：`/api/notifications?unread=true&page=1&pageSize=100` → 200；`pageSize=50` → 200；`current=1&pageSize=20` → 200；页面正文无 `pageSize must not be greater than 100`。 |
| 7 | 6+ 角色底线 sanity | PASS | `lizhanbo/aolei/shenwenjun/yaoyiping/maoyani/yangchun/annazhen` 使用默认 `123456` 登录后主路径无白屏、无 ErrorBoundary。业务员新建页仍有字段权限 403/404 与 unhandledrejection 文案，但不构成白屏。 |

复验结论：本轮 8 项中 PASS=5、FAIL=3。残余 FAIL 均集中在详情类 404 返回码，仍为 P1 后端问题；已即时通过 `team_message_role` 通知 Leader。

## 5. 结论

- P0 热修三页已通过，页面崩溃风险本轮未复现。
- 不建议宣称 Stage A 全量联调通过：商社接口、客户绑定接口、通知分页、404/403 返回码仍有失败项。
- 若明天演示聚焦“页面雏形 + P0 不白屏”，当前前端 preview 可展示；若演示 Stage A 完整业务闭环，需先处理 P1-1 至 P1-4。
