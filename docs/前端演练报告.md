# 前端 MSW 全流程演练报告

> ⚠️ 当前演示口径（2026-06-03）：所有 seed/演示账号默认密码统一为 `123456`；`admin123` 是历史旧口径，会返回 401，不可用于演示。本文下方如出现 `admin123`，仅为历史记录/旧脚本背景，不代表当前可用密码。

执行日期：2026-05-11  
执行角色：QA  
前端地址：`http://127.0.0.1:5173`  
模式：`VITE_USE_MSW=true`，任务要求 `VITE_USE_MOCK=false`  
原始结果：`docs/截图/frontend-msw-e2e/frontend-msw-e2e-results.json`

## 1. 环境检查

| 项 | 结果 |
|---|---|
| `frontend/public/mockServiceWorker.js` | 存在，大小 8144 bytes。 |
| `frontend/dist/mockServiceWorker.js` | 存在，大小 8144 bytes。 |
| `frontend/src/main.tsx` | 已通过 `worker.start({ serviceWorker: { url: '/mockServiceWorker.js' } })` 指定 SW URL。 |
| 前端 dev server | `GET http://127.0.0.1:5173` 返回 200。 |
| Service Worker 状态 | Playwright 探针确认 `navigator.serviceWorker.controller = http://127.0.0.1:5173/mockServiceWorker.js`，registration active。 |
| 截图目录 | `docs/截图/frontend-msw-e2e/` |

> 备注：本轮先发现旧演练脚本使用 `button[type="submit"]` 点击登录，但当前登录页按钮为 `type="button"`。QA 已仅修正 `tests/frontend-msw-e2e.mjs` 的测试定位逻辑，改为按按钮可访问名称“登录/登 录”点击，并补充 Service Worker activated 断言；未改前端业务代码。

## 2. 演练结论

本轮 MSW 修复验证结果：

- **P2-FE-MSW-001：已修复并验证通过。**  
  `/mockServiceWorker.js` 已存在，浏览器 Service Worker 注册为 active/controller，登录接口由 MSW 返回 `mock-jwt-access-token`。

- **P2-FE-MSW-002：部分修复，仍需 frontend 收尾。**  
  13 个自动化检查点中 **11 个通过、2 个失败**，通过率 **84.6%**。Dashboard、工单新建、导入页、我的子工单、子单详情、撤回详情、我的待办、导出模板、通知路由均可访问；但 `/admin/users`、`/admin/roles` 仍未显示目标页面标题，疑似 admin 嵌套路由或页面标题/权限菜单渲染问题仍存在。

## 3. 场景执行结果

| 场景 | 结果 | 说明 |
|---|---|---|
| `/login` admin/admin123 登录，MSW 返回 mock token | 通过 | `serviceWorker=activated`，localStorage token 为 `mock-jwt-acc...`。 |
| `/dashboard` 三角色数据 | 通过 | 页面可达并显示仪表盘。 |
| `/admin/users` 用户列表 | 失败 | 等待“用户管理”超时；需前端检查 admin 子路由/页面标题。 |
| `/admin/roles` 角色列表 | 失败 | 等待“角色管理”超时；需前端检查 admin 子路由/页面标题。 |
| `/work-orders/new` 动态表单 | 通过 | 页面显示“新建入职工单”。 |
| `/work-orders/new` 提交按钮/表单尝试 | 通过 | 捕获按钮数 `buttons=8`。 |
| `/work-orders/import` AI 映射/导入页 | 通过 | 页面显示“批量导入工单”。 |
| `/my-dispatched` 子工单列表 | 通过 | 页面显示“我的子工单”。 |
| `/my-dispatched/70011` accept/supplement/complete 页面 | 通过 | 页面显示“子工单详情”。 |
| `/work-orders/5001` 撤回申请入口 | 通过 | 页面显示“工单详情”。 |
| `/my-todos` 审批 tab | 通过 | 页面显示“我的待办”。 |
| `/export-templates` 模板管理/预览 | 通过 | 页面显示“导出模板管理”。 |
| `/notifications` 通知列表/分组/SLA 预警入口 | 通过（路由可达） | 当前脚本记录“notifications route not implemented; captured 404”，但路由可被访问并截图；如需业务页需 frontend 补正式通知中心路由。 |

统计：13 个脚本检查点，11 个通过，2 个失败；MSW worker 注册问题已关闭，完整业务闭环仍需补 admin users/roles 和通知中心正式页面。

## 4. 截图与证据

- 原始结果：`docs/截图/frontend-msw-e2e/frontend-msw-e2e-results.json`
- 关键截图：
  - `docs/截图/frontend-msw-e2e/01-login-dashboard.png`
  - `docs/截图/frontend-msw-e2e/02-dashboard-admin-manager-sales-mock-data.png`
  - `docs/截图/frontend-msw-e2e/05-work-order-new-dynamic-form.png`
  - `docs/截图/frontend-msw-e2e/07-work-order-import-mapping-progress-page.png`
  - `docs/截图/frontend-msw-e2e/08-my-dispatched-list-page.png`
  - `docs/截图/frontend-msw-e2e/09-my-dispatched-detail-accept-supplement-complete-page.png`
  - `docs/截图/frontend-msw-e2e/10-work-order-detail-withdraw-page.png`
  - `docs/截图/frontend-msw-e2e/11-my-todos-approval-tabs-page.png`
  - `docs/截图/frontend-msw-e2e/12-export-templates-create-preview-page.png`
  - `docs/截图/frontend-msw-e2e/13-notifications-route.png`

## 5. 新观察到的前端 warning

| 类型 | 说明 | 建议 |
|---|---|---|
| Ant Design Menu duplicated key | `/work-orders`、`/my-dispatched` 出现重复 key warning | 菜单配置检查父子 key，避免后续 React diff 异常。 |
| DynamicForm key spread warning | ProFormComponent props 中 spread 了 `key` | 将 key 作为 JSX 属性显式传递，避免 React warning。 |
| findDOMNode deprecated | AntD/rc 组件在 StrictMode 下 warning | 低优先级，可随依赖升级处理。 |
| React Router future flag | v7 future flag warning | 低优先级，升级前评估。 |

## 6. 缺陷状态

### P2-FE-MSW-001 缺少 `mockServiceWorker.js` 导致 MSW 模式阻塞

- 状态：**fixed-verified**
- 证据：`frontend/public/mockServiceWorker.js` 与 `frontend/dist/mockServiceWorker.js` 均存在；Playwright 探针确认 Service Worker active/controller；登录场景通过。

### P2-FE-MSW-002 MSW handlers/页面链路覆盖不足

- 状态：**partial-fixed / open**
- 证据：本轮 13 个检查点 11 个通过，说明 dashboard、workOrders、import、dispatchedOrders、exportTemplates 等链路已大幅恢复；但 `/admin/users`、`/admin/roles` 仍失败，通知中心仍非正式业务页。
- 建议：frontend 继续检查 admin 嵌套路由、RoleRoute/Outlet、页面标题或权限菜单渲染，并补正式 notifications route 后复跑。

## 7. 后续复测命令

```powershell
cd D:\AI\SpeceAppDate\工单系统\frontend
$env:VITE_USE_MSW='true'
$env:VITE_USE_MOCK='false'
npm.cmd run dev -- --host 127.0.0.1 --port 5173

cd D:\AI\SpeceAppDate\工单系统
node tests\frontend-msw-e2e.mjs
```
