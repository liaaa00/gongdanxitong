# 集成前闭环对照清单 — 实现 → 验收 → 演示步骤（0602 会议 / 06-03 09:00 Demo Gate）

> 出品：架构与需求统筹（architect_requirements）
> 日期：2026-06-02
> 任务：`42369c77` 集成前口径闭环核对与演示映射
> 配套：`docs/验收矩阵-0602演示-20260602.md`（裁决基准）、`docs/协作契约-AH验收-0602演示-20260602.md`（六维执行细则）、`docs/QA_REGRESSION_DEMO_ACCEPTANCE_20260602.md`（QA 复测）
>
> **本清单用途**：供 QA 最终复测与 Leader 在 integration worktree 上直接验收使用。每行 = 已裁决口径 → 当前代码实现状态（本人本轮真实核查）→ 验收断言 → 演示步骤。
>
> **核查基准**：main 工作区 HEAD=`962bf65b`，下列"实现状态"均为本轮 grep/read 实际读码结论，非成员自述转录。凡标 ❌ 的项即当前仍未收敛、需对应成员继续修复的实现点。

---

## 0. 核查方法与可信度声明

- ✅ = 已在代码中确认实现到位（附文件:行号证据）。
- ⚠️ = 部分实现 / 需回归确认运行时行为。
- ❌ = 当前代码仍与口径冲突，需对应成员修复（演示阻断风险）。
- 未直接读到运行时数据库状态的项，标注"需 QA 运行时确认"，不臆断。

---

## 1. 五个重点核对项（任务指定）

### 核对项 ①：江璐前端 routeVisibility 越权收紧（B3）

| 维度 | 内容 |
|---|---|
| 已裁决口径 | 江璐(`SHARED_TEAM_OWNER`)菜单不得出现 数据录入/社保/待遇申报/离职停保 入口；前端隐藏须 + 后端 403 成对。 |
| **当前实现状态** | ✅ **已收敛**（前端）。`frontend/src/config/routeVisibility.ts`：`/onboarding/data_entry`(L147)=[ADMIN,DATA_ENTRY_LEADER]、`/onboarding/social_insurance`(L148)=[ADMIN,SOCIAL_INSURANCE_SPECIALIST]、`/onboarding/benefit_apply`(L150)=[ADMIN]、`/in-service/benefit-claim`(L158)=[ADMIN]、`/offboarding/social-suspend-pool`(L164)=[ADMIN,DATA_ENTRY_LEADER]、`/onboarding/data_entry_resign`(L153)=[ADMIN,DATA_ENTRY_LEADER]——**均已不含 `SHARED_TEAM_OWNER`**。江璐仅保留 contract/renewal/onboarding_contact/resignation_contact/resignation_cert（L145-152 含 SHARED_TEAM_OWNER）。 |
| 验收断言 | jianglu 登录后侧边菜单无 数据录入/社保公积金/待遇申报/离职停保 入口；仅见 合同/续签/入职联系/离职联系/离职证明。 |
| 演示步骤 | 1) 用 jianglu 登录 → 2) 展开"我的工单/入职管理"菜单 → 3) 确认无录入/社保/待遇/停保项。 |
| 备注 | ⚠️ 成对验收：后端 B2 兜底见核对项⑤分支。前端已 OK，**后端 403 仍需运行时确认**。 |

### 核对项 ②：TeamDispatched 子工单一条一行 + 发起人非空（H1/H3/H5）

| 维度 | 内容 |
|---|---|
| 已裁决口径 | 团队工单列表按子工单粒度一行一条；显示员工信息/工单类型/发起人，发起人不为空；只读，可打开只读详情不可操作。 |
| **当前实现状态** | ✅ **已实现**（前端）。`frontend/src/pages/TeamDispatched/index.tsx`：发起人列(L79 `title:'发起人'`)、`createdByName` 兼容多字段读取(L38 `readString(params.createdByName, params.created_by, params.created_by_name)`)、只读详情入口(L114-115 `navigate('/my-dispatched/${record.id}?readonly=1&from=team')` + `<EyeOutlined/>只读详情`)。 |
| 验收断言 | 团队工单每行一个子工单；员工/类型/发起人三列齐全且发起人有值；点"只读详情"进入详情页无任何操作按钮。 |
| 演示步骤 | 1) 业务负责人(aolei)或组长登录 → 2) 进团队工单 → 3) 确认一子单一行、三列齐全、发起人非空 → 4) 点"只读详情"→ 详情只读无按钮。 |
| 备注 | ⚠️ 发起人非空依赖后端 list 拼装 `createdByName`，**需 QA 运行时确认无空值**。H5 只读须后端拒写兜底（运行时确认）。 |

### 核对项 ③：消息仅"已读/处理"两动作 + 生命周期 + 筛选（D2/D3/D4/D5/D6）

| 维度 | 内容 |
|---|---|
| 已裁决口径 | 每条通知只暴露 已读/处理 两动作；已读只消提醒不改业务状态；处理须业务状态变更后提醒消失；处理完成消息留存全部列表变已读；支持已读/未读筛选。 |
| **当前实现状态** | ✅ **已实现**（前端）。`frontend/src/pages/Notifications/index.tsx`：仅 已读(L281)/处理(L288) 两按钮；已读筛选 `read`/`unread`(L99-101 `readFilterParams`，L321 筛选项)；已读 Tag(L229)；已读后提示"提醒已消除"(L176)；说明文案明确"已读只消除提醒；处理需进入关联业务完成状态变化后提醒消失，已处理消息仍保留全部消息"(L307)。 |
| 验收断言 | 任一通知只有 已读/处理 两按钮；点已读→未读数-1、业务状态不变、消息留"全部"列表标已读；点处理→跳关联工单，业务状态真变更后提醒消失；有 已读/未读 筛选切换。 |
| 演示步骤 | 1) 任意用户登录看通知 → 2) 确认每条仅两按钮 → 3) 点已读→顶部未读 Badge-1，消息仍在"全部"标已读 → 4) 点处理→跳子单办理→完成业务→回通知确认该条消失 → 5) 切换 已读/未读 筛选验证过滤。 |
| 备注 | ⚠️ D4 "处理后消失"依赖后端通知与子单状态联动，**需 QA 端到端运行时确认**（import_notification + backend_workflow 对齐触发信号）。 |

### 核对项 ④：子工单重提 + 撤回后作废直达终态（E-1 / E-3 / E-4 / E-5）

| 维度 | 内容 |
|---|---|
| 已裁决口径 | (E-1)修改仅保存内容不自动重提；(E-3)撤回/退回态作废直接 VOID 免审批，在办态仍审批；(E-4)退回/撤回/作废三态都支持子单级重提；(E-5)重提后子单重新流转后道。 |
| **当前实现状态** | ❌ **E-1 未收敛**：`backend/src/modules/work-orders/work-order.service.ts` L178 `shouldRequireResubmitAfterEdit=[PROCESSING,RETURNED,WITHDRAWN].includes(status)`，L211-213 仍把状态置 `PENDING`，L250-252 仍 `resubmitService.resubmit(...)` 自动重提 —— **与"修改仅保存内容"口径直接冲突**。<br>❌ **E-3 未收敛**：`backend/src/modules/dispatched-orders/dispatched-order.service.ts` L701-702 `voidByCreator` 注释"returned/withdrawn child orders must also enter VOID_PENDING"，对**所有**状态一律置 `VOID_PENDING` —— 与"撤回后作废直达 VOID"冲突。<br>✅ **E-4/E-5 子单级重提路径存在**：`work-order-resubmit.service.ts` resubmit 事务 + advisory lock(L51)、限 PROCESSING/RETURNED(L97-98)、重派发通知后道 `dispatch_resubmit`(L214)；`work-order.controller.ts` `POST :id/resubmit`(L93-100) 已暴露。 |
| 验收断言 | (E-1)退回工单→修改保存→状态仍 RETURNED，未自动重新流转；重提为独立动作。(E-3)已撤回子单点作废→直接 VOID 无审批节点；在办子单作废→仍进 VOID_PENDING。(E-4)三态详情页均有"重新提交"。(E-5)重提后子单回到后道待处理/处理中。 |
| 演示步骤 | E-1：发起人改退回工单→保存→看状态未变；E-3：撤回子单→作废→确认直接终态；E-4/E-5：退回子单→重新提交→后道队列出现该子单且收到通知。 |
| 备注 | 🔴 **E-1、E-3 是后端 backend_workflow 仍需修复的实现点（演示阻断 P0）**，且**必须补回归测试**（修改后状态不变；撤回态作废直接 VOID / 在办态仍审批）。重提依赖 E-2 作废态路由语义后端先给契约。 |

### 核对项 ⑤：后端社保码兼容（B4 / C-1）

| 维度 | 内容 |
|---|---|
| 已裁决口径 | 派发产出新码 `onboarding_social_insurance`/`resignation_social_insurance`；权限层以新码为准并兼容旧码 `social_insurance`，不做数据迁移。 |
| **当前实现状态** | ❌ **未收敛**：`dispatched-order.service.ts` `roleAllowsModule`(L1597-1601) 只判 `data_entry`/`social_insurance`，`return false`；`roleAccessibleModules`(L1603-1608) 只 push `social_insurance` —— **新社保码 `onboarding_social_insurance`/`resignation_social_insurance` 未被识别**，社保子单权限/列表范围会错配。 |
| 验收断言 | 携新社保码的子单，`roleAllowsModule`/`roleAccessibleModules` 能识别归属 `SOCIAL_INSURANCE_MODULE_ROLES`；旧码 `social_insurance` 仍兼容；无数据迁移。 |
| 演示步骤 | （后台/接口层）社保角色账号查询新社保子单列表→正常返回；非社保角色→403。演示可降级为接口验证。 |
| 备注 | 🟡 backend_workflow 仍需修复。最小改动：在两函数加 `onboarding_social_insurance`/`resignation_social_insurance` 分支映射到 `SOCIAL_INSURANCE_MODULE_ROLES`，保留旧码分支。社保负责人**留空**，不强绑 fuqianwen（见 B5）。 |

---

## 2. 实现收敛度总览（截至 962bf65b）

| 验收项 | 口径 | 前端 | 后端 | 阻断级别 | 待修复成员 |
|---|---|---|---|---|---|
| A1-A4 菜单权限 | 见矩阵 | ⚠️ 需运行时核 | ⚠️ 兜底需确认 | P0 | frontend + backend |
| B2 江璐后端403 | 兜底 | — | ⚠️ 运行时确认 | P0 | backend |
| B3 江璐前端收紧 | 隐藏 | ✅ | — | P0 | （已完成） |
| B4/C-1 社保码兼容 | 新码为准 | — | ❌ | P1 | backend |
| B5 社保不绑付倩文 | 留空 | — | ⚠️ 需确认 seed | P1 | backend |
| D2-D6 消息 | 两动作+筛选 | ✅ | ⚠️ D4联动确认 | P0 | import_notification + backend |
| E-1 修改不自动重提 | 解耦 | ⚠️ | ❌ | **P0** | backend |
| E-3 撤回后作废直达 | 分支 | ⚠️ | ❌ | **P0** | backend |
| E-4/E-5 子单重提 | 子单级 | ⚠️ | ✅ 路径存在 | P0 | frontend 接按钮 |
| F1-F4 导入 | 二选一+闭环 | ⚠️ 需运行时核 | ⚠️ | P0 | import_notification |
| G1-G4 看板 | 总/月拆分 | ⚠️ | ✅ DTO 就绪 | P0 | dashboard |
| H1-H5 我的工单 | 一子单一行+只读 | ✅ 关键已实现 | ⚠️ 拒写兜底确认 | P0 | frontend + backend |

> **结论**：前端可见性层（B3/D/H3/H5）已基本收敛；**后端状态机两点 E-1、E-3 与社保码 B4/C-1 是当前最高优先未收敛项**，是 QA 阻断（编译/测试失败）与演示风险的核心，需 backend_workflow 优先修复并补回归测试。

---

## 3. 给 QA 最终复测的直接执行入口

1. **后端先验 E-1/E-3**（P0 阻断）：跑 `backend/test/dispatched-order.service.spec.ts` 与 work-order 相关 spec，确认"修改后状态不变""撤回态作废直接 VOID""在办态仍审批"三条用例存在且通过。当前若编译/测试失败，先卡这两点。
2. **社保码 B4/C-1**：接口层验证社保角色对新码子单的可见性与非社保角色 403。
3. **前端已收敛项做回归**：B3(江璐菜单)、D(通知两动作+筛选)、H3/H5(团队工单一行+只读)按 §1 演示步骤逐账号过。
4. **成对验收**：B2↔B3、H5前端↔H5后端拒写，缺一不过。
5. **演示账号清单**以 `docs/验收矩阵-0602演示-20260602.md` §5 为准逐账号执行。

---

## 4. 集成落盘提醒（针对上轮 deliverable_missing）

> 上轮评审未通过根因：统筹文档写在 main 工作区但为 **untracked**，integration worktree 的 `git diff`/`git ls-files` 审计看不到。本轮已将全部 0602 文档**显式 git add + commit** 为 tracked，确保进入可审查集成视图。各成员代码改动同理须 commit，勿只留在工作区。
