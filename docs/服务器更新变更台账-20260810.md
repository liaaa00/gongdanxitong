# 服务器更新变更台账 - 2026-08-10

> 状态：本地功能已验证并形成可复现提交；完成远端逐项对比、备份和隔离恢复库迁移预演前，禁止服务器写入。

## 1. 目标身份

- 本地仓库：`D:\ai\speceappdate\工单系统`
- 项目：工单系统
- 环境：局域网 Linux 服务器
- 传输：SSH
- SpectrAI profile：`work-order-local-ssh`
- 目标：`192.168.26.195:22`，用户 `admin`
- 部署目录：`/data/apps/work-order-system`
- 运行标识：`ticket_backend`、`ticket_frontend`、`ticket_postgres`、`ticket_nginx`
- 禁止目标：`xiangxin-new`、`xiangxin-prod`

## 2. 发布范围

### F1 业务范围与权限中心

- 行为：北仑/省外数据与后台配置隔离；动态权限不越过静态角色上限；权限配置支持版本与已发布运行态。
- 代码：权限中心、RBAC、RolesGuard、字段权限、业务范围实体/服务/前端路由与菜单。
- 配置表：`field_permissions`、`role_action_permissions`、`action_configs`、`detail_view_templates` 及权限版本表。
- 验证：多账号 E2E、角色菜单、权限中心、后端权限全量测试。

### F2 在职三流程与省外工单流转

- 行为：劳动合同续签复用劳动合同新签流转；证明开具保持独立；省外增减员复用北仑社保增减员流转；省外单项业务复用北仑单项业务。
- 代码：`in-service-flow`、在职工单 controller/service、流程种子、前端详情/列表/路由/菜单。
- 配置表：`workflow_definitions`、`module_configs`、`module_fields`。
- 验证：流程/服务定向测试、详情按钮与状态 E2E、固定回归。

### F3 福保社保公积金增减员

- 行为：参保机构名称兼容、北仑与省外列表/详情字段、35/15 列固定导出、账户目录、三项办结反馈。
- 代码：社保固定导出、省外导出与账户目录、导入映射、表单/列表/详情及前端服务。
- 配置表：`field_configs`、`field_permissions`、`module_fields`、`export_templates`。
- 验证：社保/省外导入导出专项、前端列表详情测试、真实 Excel 列校验。

### F4 省外客户与批量导入

- 行为：省外客户/部门可选；增员、减员独立批量导入；逐行创建独立直单并按既有规则派发。
- 代码：客户实体/种子、导入映射、省外导入页、列表入口与服务。
- 受保护表：`customers` 仅由服务器自身北仑记录幂等复制到省外范围，禁止导入本地客户数据。
- 验证：客户范围种子测试、省外导入 E2E、新建页面验证。

### F5 离职证明 Word 导出

- 行为：离职证明列表单选直接导出已填写 DOCX；多选导出仅包含逐人已填写 DOCX 的 ZIP，不再生成 Excel 索引或附件归档包。详情单张 Word 下载、线下签署成品上传和其他模块通用 Excel/附件导出保持不变。
- 代码：子工单服务、导出返回类型、列表下载链路。
- 验证：服务单元测试、前端下载测试及本地真实 API 单选/双选导出。

### F6 导入模板与 E2E 基线

- 行为：入职/离职模板以当前模板配置接口为准；旧可选字段测试不再访问废弃接口。
- 代码/测试：导入模板配置、字段种子、三份 optional-fields E2E 及权限/角色 E2E 夹具。
- 验证：可选字段 `10/10`，完整 Playwright `32/32`。

### F7 省外福保专员身份与权限

- 行为：新增 `welfare_specialist` 角色和 9 个省外账号，首次登录强制改密；仅开放省外增员、减员、单项业务列表/详情，按 27 省加福建厦门共 28 条规则派单。
- 身份配置：仅新增/幂等修正获批角色、9 个账号、9 条省外福利保障部角色关系、字段权限和活动权限版本；不运行全量 seed，不修改其他生产用户。
- 验证：migration 单测、字段/操作/路由权限测试、本地 migration/seed 实跑及账号/映射计数。

## 3. 待部署迁移

服务器当前数据库有 61 条 migration；最终发布源码将执行以下 23 条待执行 migration：

1. `1785607751717-CreatePermissionCenter.ts`
2. `20260803001000-AddInServicePendingReturnType.ts`
3. `20260804001000-AddBusinessScopeToAdminConfigs.ts`
4. `20260804001500-EnsureCertificateTypes.ts`
5. `20260804002000-IsolateBusinessConfigurations.ts`
6. `20260804003000-BackfillOutOfProvinceBusinessScope.ts`
7. `20260804004500-EnsurePermissionCenterTables.ts`
8. `20260805001000-IsolatePermissionSettings.ts`
9. `20260805002000-AddWorkflowFlowKey.ts`
10. `20260805003000-AddWorkflowPublishedSnapshot.ts`
11. `20260805004000-AddIsIncludedInTemplateToFieldConfigs.ts`
12. `20260805005000-AddResignationCertFieldsToImportTemplate.ts`
13. `20260805005500-EnsureFieldConfigTemplateFlag.ts`
14. `20260805006000-UpdateOnboardingImportTemplateFields.ts`
15. `20260805007000-CorrectOnboardingSystemAndTemplateFields.ts`
16. `20260805008000-AlignOnboardingTemplateWith0724Reference.ts`
17. `20260806001000-CreateContractSubjects.ts`
18. `20260806002000-RemovePostalCodeFromOnboardingTemplate.ts`
19. `20260807001000-RenameSocialLocationLabel.ts`
20. `20260807002000-EnforceConditionalResignationCertificateDispatch.ts`
21. `20260809001000-AlignSocialFundOrdersWithFubaoWorkbook.ts`
22. `20260809002000-BackfillOutOfProvinceCustomers.ts`
23. `20260810001000-ProvisionWelfareSpecialists.ts`

兼容迁移仅补生产旧库缺失的 `certificate_types`、权限中心两表和 `field_configs.is_included_in_template`；原 13 位时间戳字段 migration 已改为幂等且回滚保留共享列，避免 TypeORM 在末尾补跑时重复加列。

### 受保护业务表影响

- `20260804002000`：为 `customers`、`customer_assignees` 等表增加业务范围列并调整唯一约束；不复制本地行。
- `20260804003000`：按服务器用户现有业务范围更新 `user_roles.department_id`，并回填配置表业务范围。
- `20260809001000`：更新现有社保增减员 `dispatched_orders.visible_fields`，使历史子工单按新字段清单显示。
- `20260809002000`：从服务器自身北仑客户幂等生成省外客户记录。
- `20260810001000`：仅幂等新增获批 `welfare_specialist`、9 个省外账号、9 条角色关系、28 条省外派单映射和对应权限版本；不重置现有密码，不运行全量 seed。
- 上述迁移必须在服务器数据库备份后先对隔离恢复库预演，并记录预计/实际影响行数和业务表保护哈希。
- 正式部署不运行 `seed`，不上传本地数据库或任何本地业务行。

## 4. 依赖与部署拓扑

- 后端依赖有变化，`backend/package.json` 与已跟踪 `backend/package-lock.json` 成对发布。
- 前端新增 `socket.io-client`，`frontend/package.json` 与 `frontend/package-lock.json` 成对发布；锁文件已校验与清单一致。
- 本次不发布 `docker-compose.production.yml`、Redis、Prometheus、Grafana 或 TLS 拓扑变化。
- 保留服务器 `.env`、`docker-compose.override.yml`、Nginx 当前局域网配置和 `/data/uploads` 挂载。

## 5. 本地验证证据

- 可选字段定向 E2E：`10/10`
- 完整前端 E2E：`32/32`
- 固定前端关键回归：`129/129`
- 后端全量：`102` 个套件、`688/708` 项通过；`20` 项为既有跳过
- 福保身份/离职证明定向：后端 `50/50` 与 `51/51`、前端 `17/17`
- 本地真实运行：快捷启动退出码 0，前后端 HTTP 200；福保计数 `1|9|9|28|2`；离职证明单选 DOCX、双选 Word ZIP 成功
- 前后端 production build：通过
- `git diff --check`：无空白错误

## 6. 明确排除

- `.env`、本地数据库、`uploads`、`node_modules`、`dist`、构建缓存、`.git`
- `data/`、`monitoring/`、截图、报告、临时脚本、Excel 临时文件
- `.feature-edit-link` 脏标记与 `backend/check-fields.js` 本地诊断修改
- 未经单独维护窗口批准的 Compose/TLS/Redis/监控拓扑升级

## 7. Git 与制品状态

- 当前分支：`feature/permission-center-phase1`
- 累计业务基线提交：`dfe05266243df8e6d1b78d3da79a7fb15b27d5aa`
- 最新功能提交：`95321c177ff648f76d32cce30a981d18232a3487`
- 发布候选已选择性提交；根目录报告、截图、临时脚本、`data/`、`monitoring/`、诊断脚本和子工作树脏标记仍在提交之外。
- 服务器为非 Git 部署；本台账提交完成后，以最终 HEAD 生成不可变制品、精确文件清单和 SHA256，`SOURCE_COMMIT` 记录在制品清单与服务器备份目录。

## 8. 生产快照隔离库最终演练

- 来源：服务器备份 `/data/apps/work-order-system/backups/welfare_word_20260810_121349/ticket_system_before.dump`，本地副本 SHA256 `be32ed7a59962f8d244647b84115f766e2d163da82b531ef94b1fe16fc195ba2`。
- 最终隔离库：`ticket_system_server_rehearsal_20260810_121349_final`；从原始 61 条 migration 起步，以 `--transaction all` 一次执行 23 条待执行 migration，最终 84 条并完整提交。
- 历史兼容：演练先后发现生产旧库缺少 `certificate_types`、权限中心两表和 `field_configs.is_included_in_template`，已用三条幂等兼容 migration 补齐；旧 13 位时间戳 migration 的末尾补跑已验证不会重复加列或在回滚时删除共享列。
- 身份结果：`welfare_specialist=1`、目标省外账号 `9`、角色关系 `9`、省/市办理映射 `28`，9 个账号均启用并强制首次改密；未运行全量 seed。
- 权限结果：北仑/省外各 1 个活动版本，分别为 `1.1.1-welfare-beilun`、`1.1.1-welfare-province`；北仑福保专员仅有 `/dashboard`、`/profile`，省外仅有批准的增员、减员、单项业务及详情路径；SQL 字段权限与管理员矩阵均为 `3594` 条，动态配置字段矩阵无差异。
- 业务保护：`work_orders 73`、`dispatched_orders 215`、`import_jobs 40`、`order_attachments 1`、`notifications 585`、`operation_logs 1311` 前后计数不变；除派单表外哈希完全一致。派单表逐行核对仅有 73 行 `visible_fields` 按已批准福保字段口径变化，无状态、处理人或其他业务字段变化。北仑客户 55 行去除新增 scope 列后的哈希完全一致，服务器自身幂等复制后客户总数为 110。
- 当前生产状态：服务器仅已创建备份目录与后端/前端回滚镜像标签；尚未上传最终代码制品、尚未执行生产 migration、尚未 rebuild/up/restart。
