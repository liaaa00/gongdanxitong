# 总验收清单

> ⚠️ 当前演示口径（2026-06-03）：所有 seed/演示账号默认密码统一为 `123456`；`admin123` 是历史旧口径，会返回 401，不可用于演示。本文下方如出现 `admin123`，仅为历史记录/旧脚本背景，不代表当前可用密码。

> 版本：v1.0（2026-05-11）
> 面向：**项目终用户（客户方业务负责人 + IT 管理员）** 做验收
> 共 **71** 项，分 8 大类。

使用说明：每项包含「验收项 / 验证方法 / 预期结果」。全部勾选 + 两份签字（业务侧 + IT 侧）即视为验收通过。未通过项记入 `docs/PhaseX已知问题.md` 并列出回复时限。

**验收签字**：业务方 \_\_\_\_\_\_\_\_\_\_  IT 方 \_\_\_\_\_\_\_\_\_\_  日期 \_\_\_\_\_\_\_\_\_\_

---

## 一、环境就绪验收（10 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 1 | [ ] Postgres 启动 | `docker compose ps` 或 `tasklist /fi "imagename eq postgres.exe"` | 容器 healthy / 进程存在 |
| 2 | [ ] backend 启动 | `docker compose logs backend` 或 `pnpm start:dev` | 无 ERROR，监听 3000 |
| 3 | [ ] frontend 启动 | `pnpm dev` 或 nginx 容器 healthy | 80 或 5173 能访问 |
| 4 | [ ] `/api/health` 返回 200 | `curl http://localhost/api/health` | `{ "status": "ok" }` |
| 5 | [ ] 数据库 19 张表存在 | `psql -c "\dt"` | 19 张业务表 + typeorm_migrations |
| 6 | [ ] 初始 migrations 已跑 | `SELECT COUNT(*) FROM typeorm_migrations;` | ≥ 21 条 |
| 7 | [ ] seed 默认 admin/admin123 | 登录页输入即可 | 登录成功，首登跳改密 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| 8 | [ ] 19 张表索引齐全 | `docs/数据库ER图.md` 比对 | 齐全，GIN 索引已建 |
| 9 | [ ] 前端资源走 /assets | 浏览器 F12 Network | 资源 200，非 404 |
| 10 | [ ] 反向代理 /api 转发正确 | Chrome Devtools → Network → Request URL | /api/\* 返回 200/401 |

## 二、身份认证（6 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 11 | [ ] 登录成功 | 用 admin/admin123 登录 | 跳到首页，store.token 存在 | (legacy admin123 record; current admin123 returns 401 and must not be used for demos)
| 12 | [ ] 登录失败 5 次锁定 | 故意输错 5 次密码 | 第 6 次提示"账号锁定 15 分钟" |
| 13 | [ ] 首登强制改密 | 新账号首登 | 跳 `/profile/security`，无法跳过 |
| 14 | [ ] 改密强度校验 | 输入弱密码 | 提示"≥ 10 字符且含数字/字母/符号" |
| 15 | [ ] Token 续签 | 正常使用 30 分钟 | 自动续签，无需重新登录 |
| 16 | [ ] 角色权限隔离 | 业务员登录进 `/admin/users` | 403 拒绝 |

## 三、管理后台（15 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 17 | [ ] 用户：新增/编辑/禁用 | `/admin/users` 全流程 | 各操作成功，审计入库 |
| 18 | [ ] 用户批量导入 | 上传 Excel 10 条 | 成功 10，失败 0 |
| 19 | [ ] 角色：预置 5 类 + 自定义 | `/admin/roles` | admin/supervisor/salesperson/handler/viewer 齐全 |
| 20 | [ ] 部门：三级树 | `/admin/departments` | 可新增、拖拽调整、禁用 |
| 21 | [ ] 客户：新增与黑名单 | `/admin/customers` | 黑名单客户建单时被拦 |
| 22 | [ ] 字段配置 CRUD | `/admin/field-configs` | 增删改查均生效 |
| 23 | [ ] 字段权限矩阵 | `/admin/field-permissions` 选角色保存 | 4 态正确，列表立刻反映 |
| 24 | [ ] 派发规则 CRUD | `/admin/dispatch-rules` | AST 可视化 + 原始 JSON 切换 |
| 25 | [ ] 派发规则 dry-run | 点击"影子验证" | 取最近 10 条工单，返回命中率 |
| 26 | [ ] 导出模板 CRUD | `/admin/export-templates` | 可绑字段顺序 + 客户特殊模板 |
| 27 | [ ] 模块处理人配置 | `/admin/module-handlers` | 权重、rr_cursor、is_backup 可改 |
| 28 | [ ] 字段补充规则 | `/admin/field-supplement-rules` | 后道补字段可映射回主单 |
| 29 | [ ] 操作日志查询 | `/admin/operation-logs` | 可按 action_type / operator 过滤 |
| 30 | [ ] 操作日志导出 | 点击"导出" | CSV / Excel 可下载 |
| 31 | [ ] admin 修改后权限 60s 内生效 | 改字段权限 → 其它用户刷新页面 | 1 分钟内可见新权限 |

## 四、工单核心（12 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 32 | [ ] 业务员新建工单 | `/my/work-orders/new` 填表 | 草稿保存成功 |
| 33 | [ ] 必填校验 | 缺关键字段点提交 | 422 + 清单 |
| 34 | [ ] 条件必填校验 | "户籍≠本地"必须填暂住证 | 缺失则 422 |
| 35 | [ ] 提交触发派发 | 点提交 | 子工单按规则生成，通知 handler |
| 36 | [ ] 派发 4 策略正确 | 用 /simulate 试 fixed/rr/lb/pool | 返回预期 handler |
| 37 | [ ] 字段权限生效 | 业务员看别人建的单 | masked / hidden 符合矩阵 |
| 38 | [ ] 后道接单 | handler 点"接单" | 子单 status=processing，通知业务员 |
| 39 | [ ] 后道完成 | 填 feedback 点"完成" | 子单 completed，触发主单聚合 |
| 40 | [ ] 主单自动完成 | 全部子单 completed | 主单 status=completed，通知业务员 |
| 41 | [ ] 后道退回 | handler 点"退回"带原因 | 子单 returned，主单召回 |
| 42 | [ ] 业务员再提交 | 修改后再提交 | 旧子单 superseded，新一轮派发 |
| 43 | [ ] 并发 accept 防护 | 两个 handler 同时接 pool | 仅 1 人成功，另 1 人收到 4220 |

## 五、导入与回流（8 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 44 | [ ] 上传 Excel | `/my/work-orders/import` 上传 | 预览 10 行 + 校验结果 |
| 45 | [ ] AI 字段映射建议 | 首列 → 字段 code | ≥ 80% 一次命中 |
| 46 | [ ] 手动修正映射 | 切换字段 | 保存，下次导入记住 |
| 47 | [ ] 导入 100 条成功 | 批量提交 | 成功 ≥ 95，失败可下载报告 |
| 48 | [ ] 失败行报告 Excel | 下载导入结果 | 含 row_index / field / reason |
| 49 | [ ] 回流：后道完成触发补字段 | 按补充规则 | 主单 extra_data merge |
| 50 | [ ] 导入幂等 | 同文件重复导入 | 提示"已导入过"，不重复创建 |
| 51 | [ ] 导入进度可见 | 导入任务列表 | 进度条 + 状态 real-time |

## 六、撤回审批（6 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 52 | [ ] 业务员发起撤回 | `/my/work-orders/:id/withdraw` | 创建 withdraw_requests + approvals |
| 53 | [ ] 审批人收到任务 | 审批人登录看到 pending 列表 | 条目可见 |
| 54 | [ ] 同意/拒绝落地 | 审批操作 + 原因 | 状态更新 + 通知发起人 |
| 55 | [ ] 多人审批 | 多审批人依次处理 | 按 withdraw_approvals 聚合状态 |
| 56 | [ ] auto_agree 超时 | 超过 auto_agree_after | 系统自动同意 + 审计留痕 |
| 57 | [ ] admin 强制处理 | 有禁用审批人导致卡死 | `admin-resolve` 可覆盖 |

## 七、看板与通知（8 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 58 | [ ] 个人看板 | handler 登录 `/dashboard` | 今日待办/已完成/平均耗时 |
| 59 | [ ] 模块主管看板 | supervisor 登录 | 所辖 handler 的总览 |
| 60 | [ ] admin 全局看板 | admin 登录 | 全部门 + 趋势折线 + 异常堆积 |
| 61 | [ ] SSE 实时推送 | 浏览器开 Devtools → EventStream | 心跳 + 事件按秒级到达 |
| 62 | [ ] 未读计数 | 头像铃铛 | 与未读记录一致 |
| 63 | [ ] 通知去重 | 同事件短时间内触发多次 | 按分钟桶合并 |
| 64 | [ ] 通知可标记已读 | 点击条目 | 状态更新，计数减 1 |
| 65 | [ ] 全员广播 | admin 发公告 | 所有在线用户 10s 内收到 |

## 八、性能与安全（6 项）

| # | 验收项 | 验证方法 | 预期结果 |
|---|--------|----------|----------|
| 66 | [ ] 工单列表查询 p95 ≤ 500ms | k6 或 `docs/性能测试计划.md` | 10 万数据下达标 |
| 67 | [ ] 派发执行 p95 ≤ 200ms | 压测 /submit 1k 次 | 达标 |
| 68 | [ ] 导入 1k 条 ≤ 60s | 用样本 Excel | 包括 AI 映射时间 |
| 69 | [ ] 密码 bcrypt 强度 | 查 DB `password_hash` | `$2b$12$` 开头 |
| 70 | [ ] HTTPS 反向代理 | 生产用 nginx + cert | 443 可访问，HSTS 开启 |
| 71 | [ ] 审计链完整 | `SELECT COUNT(*) FROM operation_logs WHERE created_at > today();` | 与业务操作次数一致 |

---

## 验收完成判定

- 全部 71 项勾选 → 判定**通过**，进入试运行；
- 任何一项未通过 → 列入 `docs/PhaseX已知问题.md`，注明：项号、复现步骤、责任人、承诺修复时限；
- 试运行期（14 天）若无阻断性问题 → 正式交付。

---

## 变更日志

- v1.0（2026-05-11）：初版 71 项；对齐 README v0.9.0 与 Phase 3 后端评审通过时的产品形态。