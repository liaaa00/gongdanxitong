# NO-GO 后二次回归复测计划与自动检查命令清单（0603）

- 适用范围：0603 P0 修复完成后的二次回归。
- 当前阶段：仅产出复测计划和命令清单；不修改业务实现。
- 建议执行位置：优先在 Leader 指定的 integration 工作树执行，例如：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998`。
- 前置要求：确认后端服务、前端服务、数据库/seed/migration 使用同一份目标代码和目标数据；live smoke 结果只代表当前运行服务，不可替代 build/test。

## 一、复测执行顺序（质量门槛）

### 1. 构建与单测

| 顺序 | 命令 | 预期结果 | 失败处理 |
|---|---|---|---|
| 1 | `cd backend && npm run build` | PASS，无 TS/Nest build 错误 | 阻断后续 GO |
| 2 | `cd backend && npm test` | PASS，所有 unit suites/tests 通过 | 任一失败均 NO-GO；重点关注 `benefit-state-machine.spec.ts` |
| 3 | `cd frontend && npm run build` | PASS，`tsc -b && vite build` 完成 | 阻断后续 GO |
| 4 | `cd frontend && npm test` | PASS，所有 vitest 用例通过 | 任一失败均 NO-GO |

### 2. live 冒烟与阶段校验

需在后端、前端服务已启动且指向目标代码后执行：

| 命令 | 预期结果 |
|---|---|
| `cd frontend && npm run smoke:live` | 登录、`/api/auth/me`、`/api/work-orders`、`/api/admin/users`、`/api/dashboard/salesperson` 全部 2xx/成功 |
| `cd frontend && npm run verify:phase56` | work-order detail、dispatched-order detail、notifications、export templates、dashboard processor、SSE 全部通过 |

### 3. 角色 API / 配置 / UI 探测

| 命令 | 预期结果 |
|---|---|
| `node tmp\qa_0603_api_probe.mjs` | 业务员/组长导入 preview/confirm 空数据为 400 而非 403；负责人/后道导入接口 403；后道仅返回负责模块；江璐不返回社保；dashboard cards 有 `totalPending/monthPending` 或等价字段 |
| `node tmp\qa_0603_config_probe.mjs` | `module-handlers` active 配置与 0603 口径一致：杨纯/江璐合同，毛雅妮/江璐入离职联系，安娜祯报岗录入，傅倩雯社保公积金增员/减员 |
| `node frontend\tmp\qa_0603_ui_probe.mjs` | 业务员/组长可见入职导入；负责人/后道导入 403；菜单和 dashboard 不出现“在职/续签/待遇”；总待处理/单月待处理可见；`/work-orders` 无 ErrorBoundary |

## 二、NO-GO 失败点最小复测项

### A. 后端唯一剩余单测：待遇申报 benefit 状态机

- 失败点：`test/benefit-state-machine.spec.ts` 中 `benefit_apply` 第一阶段隐藏后返回“子工单不存在”。
- 最小命令：
  - `cd backend && npm test -- benefit-state-machine.spec.ts`
  - 若项目 jest 参数不支持文件名过滤，则执行：`cd backend && npm test`
- 预期：
  - 若待遇申报按 0603 暂缓：测试应调整为验证第一阶段隐藏，且后端全量 `npm test` PASS。
  - 若保留 P7 状态机：应明确仅测试内部流程，不影响第一阶段界面/API隐藏，且后端全量 `npm test` PASS。
- GO 标准：后端 `npm test` 全绿。

### B. 仪表盘权限过滤 / 在职隐藏 / 无权限 0 卡片

- 覆盖角色：`yaoyiping`、`shenwenjun`、`aolei`、`yangchun`、`maoyani`、`jianglu`、`annazhen`、`fuqianwen`。
- 最小命令：
  - `node tmp\qa_0603_api_probe.mjs`
  - `node frontend\tmp\qa_0603_ui_probe.mjs`
- 关键接口/页面：
  - `GET /api/dashboard/cards`
  - `GET /api/dashboard/order-type-matrix?dimension=node`
  - `/dashboard`
- 预期：
  - 不出现“在职”“续签”“待遇”等第一阶段暂缓模块文案/统计/趋势。
  - 当前用户无权限模块不返回或不展示；不得展示无关 0 卡片/0 子节点。
  - 后道 dashboard 只展示自己负责的模块或子模块统计。
  - 业务负责人只看 dashboard 和我的工单下团队/历史视角，不出现业务发起/导入入口。
- GO 标准：API 和 UI 均过滤一致，不能只靠前端隐藏。

### C. `totalPending` / `monthPending` 历史遗留待办

- 最小命令：
  - `node tmp\qa_0603_api_probe.mjs`
  - `node frontend\tmp\qa_0603_ui_probe.mjs`
- 关键接口/页面：
  - `GET /api/dashboard/cards`
  - `/dashboard`
- 预期：
  - API 返回可区分“总待处理”和“单月待处理”的字段，如 `totalPending/monthPending` 或 `pendingTotal/pendingThisMonth`。
  - UI 展示“总待处理”“单月待处理”。
  - 总待处理不因月初/所选月份而把历史遗留未办结清零。
- GO 标准：同一角色有历史未办结时，总待处理 >= 单月待处理，且不会错误为 0。

### D. 江璐社保模块泄露

- 覆盖账号：`jianglu / 123456`。
- 最小命令：`node tmp\qa_0603_api_probe.mjs`
- 关键接口：
  - `GET /api/dispatched-orders?page=1&pageSize=200`
  - `GET /api/dispatched-orders?page=1&pageSize=200&status=pending`
  - `GET /api/dashboard/order-type-matrix?dimension=node`
- 预期：
  - 江璐只返回杨纯+毛雅妮合集：合同类、入离职联系/材料收集类。
  - 不返回 `social_insurance`、`resignation_social_insurance`、`data_entry`、`data_entry_resign`、`benefit_apply`。
- GO 标准：后端接口不泄露，UI 也不显示社保/数据录入入口或统计。

### E. `social_insurance` handler active / 傅倩雯负责配置

- 覆盖账号：`fuqianwen / 123456`；配置接口使用管理员 `lizhanbo / 123456`。
- 最小命令：`node tmp\qa_0603_config_probe.mjs`
- 关键配置：
  - `social_insurance` → 傅倩雯 active
  - `resignation_social_insurance` → 傅倩雯 active
  - 江璐不应是社保 active handler
- 预期：
  - 目标运行库 active handler 与 seed/migration 一致。
  - 傅倩雯后道列表/API 只见社保公积金增员/减员。
- GO 标准：配置接口、后道列表、dashboard 三方一致。

### F. 社保公积金状态流端到端

- 覆盖角色：业务员 `yaoyiping` 或业务组长 `shenwenjun`；社保后道 `fuqianwen`。
- 建议最小路径：
  1. 业务员发起含社保公积金增员的入职工单，确认生成 `social_insurance` 子工单。
  2. **未接单前**：业务员尝试修改、撤回、作废。
     - 预期：可自由修改/撤回/作废，不需要后道审批。
  3. 傅倩雯接单/已受理该社保子工单。
  4. **接单后**：业务员再尝试修改、撤回、作废。
     - 预期：不能直接完成操作，需要后道同意或进入审批/申请流程。
  5. 傅倩雯办结成功。
  6. **已完成后**：业务员尝试修改/撤回/作废。
     - 预期：不可直接操作。
  7. 数据问题退回路径：傅倩雯明确业务数据问题时退回；需复办/等待的保持处理中。
- 建议检查接口/页面：
  - `/work-orders/import` 或新建入职工单页面
  - `/work-orders/:id`
  - `/dispatched-orders` / 社保子工单列表
  - `/my-work`、`/my-work/team`、`/my-work/history`
- GO 标准：状态流前后端表现一致，后端接口不能被绕过；不新增社保/医保/公积金细分反馈状态作为当前一期必做。

### G. `/work-orders` 动态模块加载异常

- 覆盖账号：`yaoyiping`、`shenwenjun`。
- 最小命令：`node frontend\tmp\qa_0603_ui_probe.mjs`
- 预期：
  - `/work-orders` 能正常打开。
  - 不出现 ErrorBoundary。
  - 无 `Failed to fetch dynamically imported module: .../WorkOrders/index.tsx`。
  - 业务员/组长可见“入职导入”入口。
- GO 标准：主工单列表和导入入口均可稳定演示。

## 三、角色菜单/路径检查清单

| 角色 | 账号 | 必查菜单/路径 | 预期 |
|---|---|---|---|
| 业务员 | `yaoyiping` | `/dashboard`、`/work-orders`、`/work-orders/import`、`/my-work` | 可发起入职导入；无在职；dashboard 无无关 0 模块 |
| 业务组长 | `shenwenjun` | `/dashboard`、`/work-orders`、`/work-orders/import`、团队视角 | 在业务员基础上有团队视角；可入职导入 |
| 业务负责人 | `aolei` | `/dashboard`、`/my-work/team`、`/my-work/history`、`/work-orders/import` | 只看 dashboard 和团队/历史；导入/主工单发起 403 |
| 合同后道 | `yangchun` | 合同子工单列表、`/dashboard`、`/my-work` | 只见劳动合同新签/续签相关；无社保/数据录入 |
| 入离职联系 | `maoyani` | 入职联系/离职材料收集、`/dashboard` | 只见入职联系/离职材料收集相关 |
| 共享负责人 | `jianglu` | 合同+入离职联系相关列表、`/dashboard` | 只见杨纯+毛雅妮合集；不得见社保/数据录入 |
| 数据录入 | `annazhen` | 增员/减员报岗录入、`/dashboard` | 只见报岗录入相关 |
| 社保公积金 | `fuqianwen` | 社保公积金增员/减员、`/dashboard` | 只见社保公积金增员/减员 |

默认密码均为：`123456`。

## 四、二次回归 GO/NO-GO 判定标准

### GO 条件

1. 后端 build/test 全部通过。
2. 前端 build/test 全部通过。
3. live smoke 与 verify:phase56 通过。
4. 角色 API/UI 探测无权限泄露、无在职展示、无无关 0 卡片。
5. 江璐不再返回社保/数据录入/待遇模块。
6. 傅倩雯 handler active 与运行库一致，且只见社保公积金增员/减员。
7. 社保公积金状态流端到端通过，且后端接口不可绕过。
8. `/work-orders` 主工单列表和 `/work-orders/import` 可稳定演示。

### NO-GO 条件

任一以下情况出现即 NO-GO：

- build/test 任一失败。
- API 返回无权限模块，尤其江璐仍返回社保。
- dashboard 显示第一阶段隐藏模块或无关 0 卡片。
- totalPending/monthPending 不能区分，历史遗留待办被月初清零。
- 社保公积金接单前/接单后/已完成状态流与 0603 口径不一致。
- 关键角色菜单、导入入口或主工单列表无法稳定打开。
