# QA-关键业务路径全量回归矩阵（2026-05-28）

## 1. 本轮测试基线

- 执行时间：2026-05-28 20:23（Asia/Shanghai）
- 执行目录：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\e67c4d14-e561-4cf0-afcb-75c7573485d5`
- Git 分支：`worktree/e67-frontend-0528-resubmit`
- Git HEAD：`52b2093ae12b306432722d77d704bb27e0ce8049`
- 本轮 worktree 初始状态：非干净；存在未提交源码变更：`backend/src/modules/role-action-permissions/role-action-permission.service.ts`，以及 `frontend/tsconfig.tsbuildinfo`、`frontend/tmp_import_page.txt`。
- 依赖状态：前端 `node_modules` 已存在；后端初始缺少 `node_modules`，首次 `npm install --package-lock=false` 失败，改用 `npm install --package-lock=false --ignore-scripts` 成功，但导致 bcrypt 原生 binding 未安装，后端 e2e 受阻。

## 2. 自动化执行结果

| 项目 | 命令 | 结果 | 证据/摘要 |
|---|---|---:|---|
| 前端单测 | `npm test` | FAIL | Vitest：27 files，119 tests；117 passed，2 failed。失败均在 `src/pages/Notifications/index.test.tsx`：找不到 `全部已读`、`当前分类已读`。 |
| 前端 build/typecheck | `npm run build` | PASS | `tsc -b && vite build` 成功，4621 modules transformed，dist 产物生成。 |
| 前端 lint | `npx eslint src --ext .ts,.tsx --max-warnings 10` | FAIL | 1 error + 14 warnings；error：`frontend/src/pages/TeamDispatched/index.tsx:180 jsx-a11y/anchor-is-valid`。 |
| 前端 Playwright e2e | `npm run e2e` | BLOCKED/FAIL | 6/6 failed，原因：Chromium executable 不存在，需 `npx playwright install`；同时 5173 端口为主目录旧 Vite 服务，不可作为本轮 integration 基线 UI 证据。 |
| 后端单测 | `npm test` | FAIL | 40/41 suites 执行：35 passed，5 failed，1 skipped；247 passed，1 failed，14 skipped。失败集中在测试与接口签名不同步、缺少 Excel 模板文件。 |
| 后端 build | `npm run build` | PASS | `nest build && tsc-alias` 成功。 |
| 后端 lint | `npx eslint "{src,test}/**/*.ts"` | PASS with warnings | 0 errors，8 warnings。未使用 `npm run lint`，因为脚本带 `--fix` 会修改源码。 |
| 后端 e2e | `npm run test:e2e` | BLOCKED/FAIL | 3 suites：1 passed，2 failed；20 tests：3 passed，17 failed。主要阻断：`bcrypt/lib/binding/napi-v3/bcrypt_lib.node` 缺失。 |

## 3. 后端单测失败明细

- `test/stage-a-backend.spec.ts`：`DispatchEngineService` 构造函数期望 6 个参数，测试传 5 个。
- `test/onboarding-dispatch.helper.spec.ts`：`buildWorkOrderDispatchChildren` 不再导出；`resolveModuleHandler` 参数数量与测试不一致；另有隐式 any。
- `test/onboarding-split-and-users.spec.ts`：`DispatchEngineService` 构造函数参数数量不一致。
- `test/dispatch-engine-p7.spec.ts`：`DispatchEngineService` 构造函数参数数量不一致。
- `test/excel-parser.service.spec.ts`：缺少 `tests/zhejiang-qifu-onboarding-template.xlsx`。

## 4. 关键业务路径回归矩阵

| 业务路径 | 本轮状态 | 证据 | 风险/限制 |
|---|---:|---|---|
| 工单列表 | PARTIAL | 前端 `WorkOrders/index.test.tsx` 覆盖“我发起只读列表/详情查看”和主列表操作隔离；后端 `work-order.service.spec.ts` 通过。 | 前端全量单测失败、lint 失败；未完成真实 UI e2e。 |
| 我发起的工单 | PARTIAL | 路由 `/my-work/initiated` 指向 `WorkOrders`；测试覆盖只读查询列表。 | 真实浏览器验证被 Playwright 缺浏览器阻断。 |
| 团队工单 | PARTIAL/FAIL | `TeamDispatched/index.test.tsx` 覆盖使用主工单 API 与排序参数；后端权限矩阵包含 `work_order.view_team`。 | 前端 lint 在 `TeamDispatched/index.tsx:180` 有 a11y error，应修复。 |
| 历史工单 | PARTIAL | `HistoryWorkOrders` 默认筛选 `completed/returned/withdrawn/void`，代码核查通过。 | 缺少专项自动断言/浏览器验证。 |
| 撤回 | PARTIAL | `work-order-withdraw.spec.ts` 覆盖非 owner 403、重复 409、审批中、驳回回滚、管理员通过后 withdrawn，并在本轮后端单测中该 suite PASS。 | 后端全量仍 FAIL；e2e 被 bcrypt 阻断。 |
| 作废 | PARTIAL | `work-order-withdraw.spec.ts` 覆盖作废原因、void_pending、驳回回滚、审批通过并标记子单 voidAt，该 suite PASS。 | 同上。 |
| 退回/重新提交 | PARTIAL | `return-resubmit.spec.ts` 覆盖 returned 工单重新提交与幂等，该 suite PASS；`dispatched-order.service.spec.ts` 覆盖父单终态限制与退回相关路径。 | 与入职拆分相关的若干 dispatch/onboarding 测试编译失败，影响整体信心。 |
| 导入 | PARTIAL/FAIL | `import.service.spec.ts`、`import-job.service.spec.ts` PASS；前端导入页支持下载当前字段模板、预览、确认、错误报告。 | `excel-parser.service.spec.ts` 因缺少浙江模板 xlsx FAIL；真实文件导入 e2e 未完成。 |
| 导出与中文字段/模板 | PARTIAL | `export-template.spec.ts` 覆盖 alias、问号别名回退/规范化、应用模板生成 Excel 元数据，suite PASS；前端有批量导出/模板入口。 | 仅单元层验证，未做真实下载文件内容比对。 |
| Dashboard 本月总数/完成率/趋势 | PASS(unit)/PARTIAL | `dashboard.spec.ts` 覆盖 admin、salesperson、business leader、manager、backend handler、完成率排除 void、leader trend、moduleCode/中文模块过滤，suite PASS；前端 `Dashboard/index.test.tsx` 覆盖展示行为。 | 真实 API/e2e 被 bcrypt/环境阻断；但单元覆盖较完整。 |
| 业务员权限 | PARTIAL | `roleVisibility.test.ts`、`admin-route-permissions.spec.ts`、`role-action-permissions` 默认矩阵覆盖业务员视图/创建/导入/更新/撤回/作废/催办。 | 后端 business closed-loop e2e 中 RBAC 场景因 bcrypt 阻断未执行成功。 |
| 组长权限 | PARTIAL | `dashboard.spec.ts` 覆盖 leader scope；`routeVisibility.ts` 含业务组长团队/历史/通知/dashboard 权限；后端 e2e 文件存在组长隔离场景。 | e2e 未通过；无法确认数据库实跑隔离。 |
| 后道/后道主管权限 | PARTIAL | `dashboard.spec.ts` 覆盖后道 handler scope；`dispatched-order.service.spec.ts` 覆盖子单状态/处理限制；`routeVisibility.ts` 含 data_entry/shared/social 等后道角色。 | 真实角色登录/页面验证未完成。 |
| 管理员权限 | PARTIAL | `admin-route-permissions.spec.ts` 覆盖非 admin 访问字段权限/导出模板/配置接口返回 403；`routeVisibility.test.ts` 覆盖 admin-only 路由。 | e2e 登录与 admin users 页面被浏览器/bcrypt 环境阻断。 |
| 消息通知分类 | FAIL/PARTIAL | 后端 `notifications.spec.ts` PASS，覆盖 list/count/bucket、field-change 中文内容与 diff；前端通知页测试 5/7 通过。 | 前端 `Notifications/index.test.tsx` 2 条失败：仍期望 `全部已读`、`当前分类已读`，但页面副标题显示“仅保留处理入口”。需确认测试是否应更新或功能是否误删。 |
| 通知处理后自动已读/保留逻辑 | PARTIAL | 后端通知 service 单测覆盖 markRead/markAllRead/read scope/bucket 计数；页面副标题说明处理完成后后端规则自动消失。 | 未找到本轮成功的端到端处理后自动已读验证；Playwright/e2e 阻断。 |

## 5. 环境限制与执行副作用

- 后端依赖恢复：首次安装失败（tar/EPERM/UNKNOWN + bcrypt node-pre-gyp），清理重装审批被拒，后续 `--ignore-scripts` 安装成功但 bcrypt 原生 binding 缺失，导致后端 e2e auth/业务闭环无法执行。
- Playwright 浏览器缺失：`chromium_headless_shell-1223` 不存在，前端 e2e 全部在浏览器启动前失败。
- 本机 5173/3000/8080 均有服务响应，但 5173 命令行显示来自主目录 `D:\AI\SpeceAppDate\工单系统\frontend`，不是本轮 integration worktree，不能作为本轮 UI 验收基线。
- 测试执行产生/修改了 `frontend/test-results/*`、`frontend/tsconfig.tsbuildinfo` 等文件；未做清理以保留失败现场。
- `npm install --ignore-scripts` 报告 30 个漏洞（3 low、17 moderate、10 high），本轮未执行 `npm audit fix`。

## 6. 结论

本轮不能判定“项目整体无明显问题”。虽然前后端 build 均 PASS，且 dashboard、撤回/作废、导入导出、通知、权限等多个单元级测试有通过证据，但全量前端单测、前端 lint、后端单测、前端 e2e、后端 e2e 均存在失败或环境阻断。建议至少完成以下修复后重新全量回归：

1. 修复/确认通知页“全部已读/当前分类已读”预期：若产品已改为仅保留“处理”，需更新前端测试；若按钮应保留，则恢复 UI。
2. 修复 `TeamDispatched/index.tsx:180` a11y lint error。
3. 同步后端 dispatch/onboarding 相关测试与当前接口签名，补齐或恢复 `tests/zhejiang-qifu-onboarding-template.xlsx`。
4. 正常安装后端依赖并构建 bcrypt 原生 binding，或改用纯 JS bcryptjs 路径以解除 e2e 阻断。
5. 安装 Playwright Chromium，并确保 5173/3000 服务由本轮 integration worktree 启动后再跑真实 UI e2e。
