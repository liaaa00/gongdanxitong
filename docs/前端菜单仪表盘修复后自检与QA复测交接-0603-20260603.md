# 前端菜单仪表盘修复后自检与 QA 复测交接 - 0603

> 负责人：前端菜单与仪表盘工程师  
> 范围：前端菜单、路由、看板与仪表盘修复后的只读自检与 QA 复测交接；不触碰后端。  
> 检查工作区：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998`  
> 日期：2026-06-03

## 结论

- **前端菜单/仪表盘范围：GO，可进入 QA 复测。** 本报告与相关前端修复文件已纳入 integration 暂存/提交范围。
- **第一期只开放入职、离职。** 在职/续签/待遇入口、直输 URL、看板卡片、仪表盘统计均按前端口径隐藏或过滤。
- **后端依赖不能视为前端已完成。** dashboard cards 字段、服务端权限兜底、处理人配置、社保锁定流仍需后端与其他前端交互任务共同验收。

## B2/B3/B4 与 R1/R2/R4/R10 自检表

| 项 | 结论 | 证据 | QA 复测 | 依赖/风险 |
|---|---|---|---|---|
| B2 前端 test/build | 已通过，最终 integration 需复跑 | 相关测试 8 files / 50 tests PASS；全量前端测试 34 files / 169 tests PASS；build PASS | 最终分支执行前端 test/build | 本轮不重复长命令，避免再次超时 |
| B3 仪表盘在职/无权限 0 卡 | 前端已闭环 | Dashboard 只显示入职/离职；按 moduleAccess 过滤角色可访问模块；无权限模块不渲染 0 卡 | 各账号访问 `/dashboard` | 后端 API 仍需服务端过滤 |
| B4 totalPending/monthPending | 前端展示已兼容，最终依赖后端 | 看板显示“总待处理”“单月待处理”，优先读 `totalPending/monthPending` | 构造历史待办 + 当月待办验证 | 后端必须直返字段并保证 SQL 正确 |
| R1 在职直输/菜单/看板/统计 | 前端已闭环 | `IN_SERVICE_ROLES=[]`、隐藏路由集合、在职菜单 `menuVisible:false`、Dashboard 不渲染在职 | 直输 `/renewal`、`/benefit`、`/onboarding/renewal_contract`、`/onboarding/benefit_apply`、`/in-service*` | 后端仍需 403/过滤 |
| R2 总待处理/单月待处理 | 前端已闭环 | 卡片标题和说明文案区分总量与月份 | 2026-06 下验证历史待办不清零 | 后端统计字段 |
| R4 旧名与权限口径 | 前端展示/mock/通知 fallback 已闭环 | 新名覆盖入职联系、劳动合同新签、增员报岗录入、社保公积金增员/减员、离职材料收集等；旧名不再作为业务显示名 | 检查菜单、Dashboard、通知、mock 返回 | 后端 seed/module scope 兜底 |
| R10 我的工单/团队/后道只读 | 本任务范围内完成可见性和名称过滤 | 业务员、组长、负责人、后道菜单按0603口径显示 | 团队详情只读、后道详情只读需联调复测 | 工单详情交互任务 + 后端 action permissions |

## QA 重点复测账号与页面

| 账号 | 角色 | 页面 | 预期 |
|---|---|---|---|
| `yaoyiping` | 业务员 | `/dashboard`, `/work-orders`, `/work-orders/import`, `/my-work/initiated`, `/my-work/returned`, `/my-work/history` | 可见入职导入；不可见团队工单、在职、后道模块 |
| `shenwenjun` | 业务组长 | 业务员页面 + `/my-work/team` | 业务员能力 + 团队视角 |
| `aolei` | 业务负责人 | `/dashboard`, `/my-work/team`, `/my-work/history`, 直输 `/work-orders/import` | 仅仪表盘和团队/历史；导入不可见或 403 |
| `yangchun` | 劳动合同后道 | `/dashboard`, `/onboarding/contract` | 只看劳动合同新签 |
| `maoyani` | 入离职后道 | `/dashboard`, `/onboarding/onboarding_contact`, `/onboarding/resignation_contact` | 只看入职联系、离职材料收集 |
| `jianglu` | 杨纯+毛雅妮合集 | `/dashboard`, `/my-work/*`, `/dispatched-orders` | 合同新签 + 入职联系 + 离职材料收集，不含报岗/社保 |
| `annazhen` | 报岗录入后道 | `/dashboard`, `/onboarding/data_entry`, `/onboarding/data_entry_resign` | 只看增员/减员报岗录入 |
| `fuqianwen` / `付倩雯` | 社保公积金后道 | `/dashboard`, `/onboarding/social_insurance`, `/onboarding/social_insurance_resign` | 只看社保公积金增员/减员 |

## QA 重点接口与直输 URL

直输 URL 必须隐藏或拒绝：

- `/renewal`
- `/renewal/new`
- `/benefit`
- `/benefit/new`
- `/onboarding/renewal_contract`
- `/onboarding/benefit_apply`
- `/in-service`
- `/in-service/contract-renewal`
- `/in-service/benefit-claim`

接口复测：

- `GET /api/dashboard/cards?month=2026-06`：必须包含 `totalPending`、`monthPending`，历史待办计入总待处理。
- `GET /api/dashboard/order-type-matrix?dimension=node&month=2026-06`：无在职/待遇可见行，并按当前用户模块权限过滤。
- `GET /api/dashboard/leader-trend?...`：第一期仅入职/离职趋势。
- `/api/dispatched-orders`：江璐、傅倩雯/付倩雯、安娜祯按各自模块范围返回。

## 已执行命令与结果

执行目录：`D:\AI\SpeceAppDate\工单系统\.spectrai-worktrees\integrations\fbcbdb12-4c80-4f91-ad8a-01642daae998\frontend`

| 命令 | 结果 |
|---|---|
| `npm run test -- src/config/routeVisibility.test.ts src/layouts/BasicLayout.test.tsx src/pages/Dashboard/index.test.tsx src/utils/moduleAccess.test.ts src/pages/Notifications/displayName.test.ts src/pages/Notifications/index.test.tsx src/pages/Resignation/index.test.tsx src/components/DispatchedBatchImportModal.test.tsx` | PASS：8 files / 50 tests |
| `npm run test` | PASS：34 files / 169 tests |
| `npm run build` | PASS：`tsc -b && vite build` completed |

## 剩余依赖与风险

1. 后端 dashboard cards 需直返并正确计算 `totalPending/monthPending`。
2. 后端 dashboard API 需按 phase1 与模块权限过滤，不能只靠前端隐藏。
3. 后端处理人/负责人配置需验证：江璐=杨纯+毛雅妮；傅倩雯/付倩雯仅社保增/减；安娜祯仅报岗增/减。
4. 社保公积金接单/已受理后锁定流需要后端工作流与工单详情交互联合 E2E。
5. 最终发布前需在全部成员合并后重跑前后端 build/test。
