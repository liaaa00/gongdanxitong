# 同步服务器准备清单 · 2026-09-15

> 状态：**本地已改并验证通过，尚未同步服务器**。本清单用于决策与执行同步。
> 服务器：`ssh admin@192.168.26.195` → Docker 形态，部署目录 `/data/apps/work-order-system`（无 git 仓库）。

## 一、本地修改（已完成，已通过回归）

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `backend/src/database/migrations/20260915000000-RestoreBusinessMainOrderCreate.ts` | 新增 | 给北仑+省外两 scope 的 5 个业务角色补回 `work_order.create` + `route.work_order_create`（"新建单个工单"按钮），幂等、FOR UPDATE、down 不回收 |
| `backend/test/restore-business-create-migration.spec.ts` | 新增 | 迁移单测（两 scope 增补正确 / 非目标角色不动 / 幂等 / 无行不创建） |
| `回归测试.ps1` | 修改 | 权限回归清单加入新 spec |
| `docs/AI修改记录.md` | 修改 | 本次改动留痕 |

验证结果：
- 后端 `nest build` 通过；新旧权限迁移 spec 共 5 用例全绿。
- 本地库 `migration:run` 依次执行 09-14 四批 + 新 09-15，成功。
- psql 直查 `system_settings`：两 scope × 5 角色现均含 `work_order.create`/`route.work_order_create`。
- 根 `回归测试.ps1 -SkipBuild`：前端 21 个套件逐个全绿、后端多组全绿，EXIT=0。

## 二、服务器现状（本次 SSH 只读巡检，未做任何写操作）

- 运行态：`ticket_nginx:8080`、`ticket_backend`(healthy)、`ticket_frontend`、`ticket_postgres` 均 Up；`curl 127.0.0.1:8080` → 200。
- 迁移库：`migrations` 共 **137** 条，最新到 `260914220000 RestoreBusinessMainOrderImport`（**即已有 import 修复，缺本次 create 修复**）。
- 权限矩阵实测（`system_settings`）：
  - beilun 五角色：`create=0, import=1`（批量导入在、新建缺——与用户反馈一致）。
  - out_of_province 五角色：`create=0, import=0`（**省外连批量导入也没有**，见"已知遗留"）。
- 版本标记不可信：`SOURCE_COMMIT=194b460`（9-03），但代码文件 mtime 为 9-14 16:31/16:45、后端镜像 9-15 01:01 重建——说明服务器实为随 9-14 一批同步过，`SOURCE_COMMIT` 未更新。
- `backend/dist` 在宿主机不存在 → 迁移在容器内由 dist(js) 执行；同步需**重建后端镜像**方能带入新迁移。

## 三、同步动作（待你确认后再执行，属生产写操作）

1. 推本地 `main`（含新迁移/新 spec/回归脚本）到部署流水线或直接更新服务器源码目录。
2. 重建后端镜像：`docker compose ... build backend`。
3. 迁移前备份（现成脚本 `deploy/Invoke-ProductionMigration.ps1` 已含 `pg_dump`）。
4. 跑迁移：容器内 `npm run migration:run`，会补执行 `RestoreBusinessMainOrderCreate20260915000000`。
5. 验证：重查服务器 `system_settings` 两 scope 五角色含 `work_order.create`；业务员入职/离职主工单页"新建"按钮可见。

## 四、已知遗留（本次未处理，供决策）

- **省外(out_of_province)业务角色连批量导入也缺**：服务器与本地矩阵均为 `import=0`。根因同 09-11 稀疏覆盖，而 09-14 的 import 补救**只覆盖 beilun 一个 scope**。本次 09-15 只按你的需求补 create（两 scope）。如需一并恢复省外的 import，请告知，我再加一条迁移补 `work_order.import`+`route.work_order_import` 到 out_of_province。
- 服务器 `SOURCE_COMMIT` 与实际代码不符：同步时建议一并刷新版本标记，避免后续误判。

---
一句话：本地修复完成且回归通过；服务器只差"重建镜像+跑新迁移"即可生效，省外导入为可选补充项。
