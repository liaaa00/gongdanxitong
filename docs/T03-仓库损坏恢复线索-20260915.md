# T03 阶段快照与仓库异常记录（供恢复用，未提交）

## 1. T03 代码产出（工作区，已自测通过）

| 文件 | 动作 | 状态 |
|---|---|---|
| backend/src/config/cors.ts | 新增：CORS 白名单纯函数模块（resolveAllowedOrigins/isAllowedOrigin/buildCorsOptions） | 完成，spec 8/8 绿 |
| backend/src/main.ts | 改：app.enableCors() → app.enableCors(buildCorsOptions()) | 完成 |
| backend/src/modules/dashboard/dashboard.service.ts | 改：:1106 与 :1183 两处 SET LOCAL statement_timeout 去 $1 参数化（内联常量 7000） | 完成，新用例绿 |
| backend/test/dashboard.spec.ts | 增：leader-trend SET LOCAL 回归用例（断言无 $N、无 fallback WARN） | 完成 |
| backend/test/cors-allowlist.spec.ts | 新增：CORS 白名单 8 用例 | 完成 |
| backend/test/dispatched-order.behavior.spec.ts | 新增：派单域行为测试网 21 用例（accept/claim/complete/returnOrder/批量/权限边界/404） | 完成，21/21 绿 |
| config/env.ps1 | 增：$env:TICKET_LAN_IP 导出（CORS 同源口径） | 完成 |
| 回归测试.ps1 | 增：T03 三项测试纳入固定回归入口（新 Write-Step） | 完成 |
| docs/AI修改前必读.md | 增：§8.1 禁止破坏性 git 命令（team-lead 要求①） | 完成，未提交 |

自测命令（Node v20.20.2，backend 目录）：
`node node_modules/jest/bin/jest.js --config ./test/jest-unit.json --runInBand test/dispatched-order.behavior.spec.ts test/cors-allowlist.spec.ts test/dashboard.spec.ts` → 29+用例全绿
全量后端 jest：148 passed / 5 failed / 4 skipped。5 个失败套件（control-flow-regression、social-insurance-state-flow、onboarding-import-template-seed、work-order-import.service、customer-bisect）均不 import 本次 T03 改动的文件、本次也未改其依赖源码 → 判定与 T03 无关（疑似仓库损坏前已存在，恢复后须复跑确认）。

## 2. 严重仓库异常（冻结现场，勿再写 git）

时间：2026-09-15 02:2x。本会话唯一 git 写操作 = `git stash push -- <6 files>` → EXIT=128（`fatal: a27c3dc... is not a valid object`），未移动任何工作区文件。

只读诊断结论：
- `.git/objects/pack/` 仅剩 3 个 .idx（pack-3ae78b50 / pack-6d0d2b28 / pack-ba014c94）与 multi-pack-index，**对应 .pack 文件全部消失**；松散对象仅剩 1 个（a39680dea...）。
- packed-refs 中 refs/heads/main = 6b417763763703f3065e6ee68c2bd28250459d48，但该对象不存在，且 .git/logs/HEAD reflog **没有** 6b41776 的任何记录（reflog 最后一条是 94649ed）→ main ref 被外部改写。
- T01/T02 提交链对象（ff56e87…94649ed）全部不可解析；tag governance/baseline-20260914 文件仍在（指 94649ed）。
- `git fsck` 报几乎所有分支 ref 的 sha1 pointer invalid；多分支（backup/deploy/feature 等）同灭 → 全局对象丢失，非局部。
- origin（github.com/liaaa00/gongdanxitong）网络可达，origin/main=84c6255（早于 T01 的旧状态）；**T01/T02/T03 从未 push**。
- 本仓有 10+ 个 `.spectrai-worktrees/agent-*` linked worktree 共享同一 objects + 多 AI 窗口并发操作史 → 判定为外部/并发进程删除 pack 并改写 refs。

## 3. 恢复线索

已做保护：
- 冻结：本会话不再执行任何 git 写命令（禁 reset/checkout/gc/prune/fsck --repair/stash）。
- 冷备份：`d:\AI\SpeceAppDate\BACKUP-git-incidents-20260915\`（main-git 整库 3.9M 含 reflog/index/packed-refs + T03 九文件副本 + 本文件副本）。

恢复路线建议（供有权限者决策执行）：
1. **先查 Windows 回收站**找回 3 个 .pack（objects/pack 约几十 MB；找回放回即自愈，成本最低）。
2. 若无：`git fetch origin` 恢复 84c6255 之前的全部基线对象（refs 结构完好，恢复后可重新挂链）。
3. T01/T02 未 push 的提交内容：工作树文件在损坏发生时**并未丢失**（除被对象库引用的历史版本外），IDE 本地历史（.aionrs/.spectrai）可能留有旧 blob；最坏情况按下方 40 位 SHA 序列在恢复后的仓库上**逐组重建提交**（分组清单 docs/archive/2026-Q3-分组清单.md 记录了每组内容，文件本体大多还在磁盘）。
4. 全部恢复后：重跑 `git fsck`（只读）→ `回归测试.ps1 -SkipBuild` → 按 T03 分组补提交。

reflog 完整链（40 位 SHA）：
ff56e874f8962ee2a6a4c249f7ee3af3720343c3  T01 端口口径统一
79a632674bef0d15271faf72c70bf992056ede23  C1 .gitignore
eec98ff4229db29335a32afa3a24e85c2946ac85  C2 6 migration 保全
453a91a3584018343fbfe3e006d66b9a6112f13d  C3 门户域
a3ea4210fcc4931f19eb9cc02655e51f79ea3cda  C4 派单域
05b662afebfa04b9b4c6b6e320024ae257612ac2  C5 工单/字段
2f39286d9eaf349161c56f87f7ac51f614ac72ba  C6 RBAC/构建
37cbda3d7d3ef258a8f9704ff126e3b175cdce2d  C6b 路由联动
3f0b4c08ad510e35e116b9ecb36d7100c5af06f6  C6c oneoff 迁出
ce4a743a43678e0e7849919e4e8ad1a80a1925a5  C7 归档+文档
59fc4f7dd3aeac3b9a6a6ce081af3941e4d39158  C7b 文档修订
e7d5ec158418af103859d4ed3bed08a5879b3a45  T02 记录 v1
1ee2c4ad845c0e573b09cf90d7a6f1b240bcbaa1  T02 记录终稿
94649ed4d7e09ee48478059c4ff4afcd6c29a790  T02 收尾（tag governance/baseline-20260914 所指）
