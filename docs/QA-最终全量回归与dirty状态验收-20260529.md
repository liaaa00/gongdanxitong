# QA 最终全量回归与 dirty 状态验收（2026-05-29）

## 结论
通过（当前 integration worktree：`.spectrai-worktrees/integrations/e67c4d14-e561-4cf0-afcb-75c7573485d5`，HEAD `52b2093ae12b306432722d77d704bb27e0ce8049` + 当前未提交修复变更）。

本轮在发现残留问题后未停止在报告阶段，已补齐：
- 前端 Notifications 过期测试预期（当前产品口径为仅保留“处理”入口）。
- coverage 下 Admin Users 测试异步等待不稳。
- 后端 DispatchEngineService 构造参数相关老测试。
- onboarding-dispatch helper 兼容导出/签名与多处理人分配语义。
- 缺失的 `tests/zhejiang-qifu-onboarding-template.xlsx` 测试夹具。
- 默认 Playwright 与 dispatched-orders 500 专项用例隔离。

## 自动化回归结果

### 前端
1. `npm test`：PASS，30 files / 128 tests。
2. `npm run lint`：PASS，0 errors / 7 warnings（低于 max-warnings=10）。TeamDispatched `anchor-is-valid` error 已消失。
3. `npm run build`：PASS。当前构建产物包含入口 chunk `assets/index-DjollYbX.js`（本轮输出）。
4. `npm run coverage`：PASS，已使用 v8 provider。
   - Statements 43.28%
   - Branches 31.27%
   - Functions 41.04%
   - Lines 46.12%
5. `npm run e2e`：PASS，6 tests passed。
   - 说明：默认 Playwright 配置已排除 `dispatched-orders-500-regression.spec.ts`，该专项用例使用独立配置执行。
6. `npx playwright test e2e/dispatched-orders-500-regression.spec.ts --config=playwright.dispatched.config.ts`：PASS，1 test passed。
   - 使用 `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` 指向本机已有 `chromium_headless_shell-1217`。
   - 初次直接执行默认 Playwright 时失败原因为缺少当前依赖期望的 `chromium_headless_shell-1223`；`npx playwright install chromium` 曾超时。最终通过显式指定已有浏览器恢复执行。

### 后端
1. `npm run build`：PASS。
2. `npx tsc --noEmit`：PASS。
3. `npm run lint`：PASS，0 errors / 8 warnings（既有 unused warnings）。
4. `npm test`：PASS，40 suites passed / 1 skipped，280 tests passed / 14 skipped。
5. `npm run test:e2e`：PASS，3 suites / 20 tests。
   - bcrypt 原生 binding 阻断已不存在。
   - Jest 结束时仍有 worker force-exit/teardown 提示，但所有 e2e 断言通过。

## `/api/dispatched-orders` 500 回归验证

为避免误用根目录旧服务，启动 integration 后端到 3100 后验证：

- `data_entry`：200
- `social_insurance`：200
- `onboarding_contact`：200
- `contract`：200

请求格式：
`/api/dispatched-orders?current=1&pageSize=20&module_code=<module>&sort=dispatched_at&order=descend&page=1`

备注：本机 3000 端口存在旧 node 服务，四模块仍返回 500；该结果不代表当前 integration 修复。当前 integration 后端 3100 验证全部 200。

## `[GlobalError] unhandledrejection` 验证

专项 Playwright 用例 `frontend/e2e/dispatched-orders-500-regression.spec.ts` 通过：
- 对 `data_entry/social_insurance/onboarding_contact/contract` 四模块模拟 `/api/dispatched-orders` 500。
- 页面保持可见容器/正常兜底。
- 未捕获到 `[GlobalError] unhandledrejection`。

## 关键业务矩阵结论

覆盖并通过：
- 登录与鉴权基础流：未登录跳转登录、管理员登录、错误密码 401。
- 管理员用户页可访问。
- 新建工单页面可进入且表单渲染。
- Dashboard 新口径相关单测与覆盖率回归。
- MyDispatched / OnboardingModule / TeamDispatched / Notifications 等关键前端路径的 Vitest 回归。
- 后端 dashboard、dispatched order、work order submit/resubmit、onboarding split、dispatch engine、notifications、export template、excel parser、business closed loop e2e 等全量回归。

## dirty 状态验收

当前 integration 不是干净工作区。dirty 分类如下：

### 可接受/必要业务与测试变更
- 后端 dispatched-orders 500 修复相关：`backend/src/modules/dispatched-orders/dispatched-order.service.ts`、分页 DTO、dashboard 相关源码与测试。
- 后端 dispatch/onboarding 兼容修复：`backend/src/modules/dispatch-engine/dispatch-engine.service.ts`、`backend/src/modules/work-orders/onboarding-dispatch.helper.ts`、相关 Jest 测试。
- 后端缺失测试夹具：`tests/zhejiang-qifu-onboarding-template.xlsx`。
- 前端 lint/coverage/e2e 修复：`frontend/package.json`、`frontend/playwright.config.ts`、`frontend/playwright.dispatched.config.ts`、`frontend/e2e/dispatched-orders-500-regression.spec.ts`、Notifications/Admin Users 相关测试。
- 前端业务路径修复/回归测试：OnboardingModule、MyDispatched、HistoryWorkOrders、TeamDispatched、dashboard/dispatchedOrders 相关源码和测试。

### 需要收尾清理或明确保留的测试产物
- `frontend/coverage/`：coverage 运行产物，建议不提交或按仓库策略处理。
- `frontend/test-results/.last-run.json`、已删除/新增的 `frontend/test-results/...`：Playwright 运行产物，建议清理或不提交。
- `frontend/tsconfig.tsbuildinfo`：build 产物，建议不提交。
- `frontend/tmp_import_page.txt`：历史临时文件，建议确认是否删除。
- 旧 QA 报告文档若不需要纳入本次提交，应统一归档或清理。

### 特别说明
- `backend/src/modules/role-action-permissions/role-action-permission.service.ts` 仍处于 modified；本轮未专门复审其业务必要性，只记录为现有 dirty 源码项，建议代码审批专家确认是否属于可接受业务变更。

## 最终判断
功能与质量门禁已达到本任务验收目标：前端 test/build/lint/coverage/e2e 通过；后端 build/typecheck/lint/Jest/e2e 通过；四模块 `/api/dispatched-orders` 在当前 integration 后端验证为 200；专项页面兜底无 `[GlobalError] unhandledrejection`。

发布/合并前仍建议清理测试产物类 dirty，并由代码审批确认 `role-action-permission` 等非 QA 直接修复源码项是否纳入提交。
