# AI 修改记录

## 2026-09-06 工作台提醒模块调整

- **改动**：移除工作台的“本月提醒”卡片，并将常用操作区域调整为单列铺满；同时在运行时移除对应 DOM，避免页面继续显示该模块。
- **原因**：按客户要求取消“本月提醒”展示，保持工作台信息聚焦于服务操作和办理记录。
- **验证**：客户门户 `npm test` 通过，并确认 `5173` 返回页面不再显示该模块。

## 2026-09-04 客户门户会议口径落地

- **改动**：同步入职、离职、薪资三类门户流程；附件统一改为共享邮箱；新增离职标准 Excel 导入；离职证明改为后台配置；薪资页面保留两级分支、变化说明和附件；加入薪资 3/2/1 个工作日提醒文案；办结结果邮件范围扩展为三类业务。
- **原因**：落实 2026-09-04 两场需求评审会议的最终结论，明确数创系统与内部工单系统的边界。
- **验证**：在最新版客户门户工作树执行 `npm test`，21/21 通过。
- **备注**：真实 SMTP/API、数创系统同步、每日未核销客户清单及后台配置页面仍属于后续联调工作，不在本次启动验证中伪造完成。

## 2026-09-04 客户门户界面优化

- **改动**：移除工作台个性化问候语；重新优化入职办理、离职办理、薪资确认三页的页面头部、卡片、步骤条、表单控件、附件上传区、提醒信息和移动端布局。
- **原因**：解决客户门户页面层次弱、留白和操作区不协调、整体视觉简陋的问题，同时保持既有字段、接口和业务口径不变。
- **验证**：最新版客户门户工作树执行 `npm test`，21/21 通过；访问 `http://127.0.0.1:5173/` 返回 200，已确认不再包含个性化问候语并加载新版样式。

## 2026-09-06 客户门户乱码与模式切换修复

- **改动**：确认并排除旧版 `D:` 目录和旧 Electron 服务；重新启动实际客户门户工作树；强化入职、离职批量导入页的返回选择入口，并在返回时清理导入预览、映射和文件状态。
- **原因**：避免客户继续访问内部工单系统/旧门户，修复批量导入后无法回到单条录入的体验问题。
- **验证**：客户门户 `npm test` 21/21 通过；`5173` 返回企业服务客户门户最新版，未发现问候语、替换字符或字面量 `\\n`，入职和离职返回入口均存在。

## 2026-09-07 客户门户自适应与客户视角优化

- **改动**：修复 940px 以下页面仍保留固定侧栏、导致手机内容区被压缩的问题；入职和离职批量办理增加醒目的 Excel 模板下载入口；优化离职、薪资页面客户操作文案；移除薪资 3/2/1 提醒节奏、共享邮箱、后台配置和审核流程等内部说明；办理进度调整为近一年记录。
- **原因**：门户最终面向客户使用，需要在桌面和移动端都保持合理布局，并仅展示客户需要理解和操作的信息。
- **验证**：客户门户 `npm test` 21/21 通过；Playwright 在 390、620、768、1024、1440px 宽度核验无横向溢出，入职/离职模板下载区均完整可见。固定根目录回归脚本另发现内部工单前端既有测试失败并出现未退出情况，与本次客户门户页面改动无关。

## 2026-09-07 薪资确认两级选择与页面优化

- **改动**：将薪资确认页调整为“本月是否有变动”的一级单选；选择“本月有变动”后，再在“填写变化说明”和“上传薪资文件”中二选一；选择“与上月无变化”时直接确认提交。同步增加必填与附件格式/大小校验，并优化桌面端和移动端卡片布局。
- **原因**：落实会议确定的薪资门户简化口径，避免客户同时填写说明和上传文件，移除员工明细、金额核算、人数统计及内部提醒节奏等非客户操作内容。
- **验证**：客户门户 `npm test` 21/21 通过；HTTP 页面检查无乱码及内部运营文案；Playwright 在 1440px 与 390px 宽度验证两级切换、空内容拦截和页面无横向溢出。固定根目录回归已执行，仍因内部工单前端既有的 DataSyncMonitor mock 缺失及 MyDispatched 测试失败而未通过，与本次客户门户改动无关。
## 2026-09-07 客户规则本地配置闭环

- **改动**：新增离职默认项、薪资账单日与固定 3/2/1 工作日提醒、共享邮箱路由规则的后端字段、校验和迁移；内部客户规则页改为真实后端保存/读取；门户连接器按签名客户消费可信入职/离职/薪资/共享邮箱规则。
- **验证**：客户门户 22/22 通过；后端客户规则定向测试 12/12 通过；后端与内部前端构建通过。固定回归仍复现内部工单原有测试失败；本机数据库迁移因默认 `postgres` 和项目记录的 `ticket` 凭据均认证失败未执行，未连接服务器数据库。
