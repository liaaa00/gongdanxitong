# 工单系统权限配置文档

> **重要**：此文档记录了系统运行所需的所有权限配置，即使管理员权限失效后也可参考此文档恢复。

---

## 一、已配置的防火墙规则

### 1. 前端 Vite 服务（端口 5173）
```powershell
New-NetFirewallRule -DisplayName "工单系统-前端Vite" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow -Profile Private,Domain
```

### 2. 后端 API 服务（端口 3000）
```powershell
New-NetFirewallRule -DisplayName "工单系统-后端API" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow -Profile Private,Domain
```

### 3. PostgreSQL 数据库（端口 5432）
```powershell
New-NetFirewallRule -DisplayName "PostgreSQL数据库" -Direction Inbound -LocalPort 5432 -Protocol TCP -Action Allow -Profile Private,Domain
```

### 查看已配置的规则
```powershell
Get-NetFirewallRule -DisplayName "*工单系统*","*PostgreSQL*" | Select-Object DisplayName, Enabled, Direction, Action, Profile
```

### 删除规则（如需重新配置）
```powershell
Remove-NetFirewallRule -DisplayName "工单系统-前端Vite"
Remove-NetFirewallRule -DisplayName "工单系统-后端API"
Remove-NetFirewallRule -DisplayName "PostgreSQL数据库"
```

---

## 二、网络配置

### 当前网络类型
- **网络名称**: FSG-Public
- **网络类型**: Private（专用网络）
- **网卡**: 以太网

### 修改网络类型（需要管理员权限）
```powershell
# 改为专用网络（推荐）
Set-NetConnectionProfile -InterfaceAlias "以太网" -NetworkCategory Private

# 改为公用网络
Set-NetConnectionProfile -InterfaceAlias "以太网" -NetworkCategory Public
```

### 查看网络类型
```powershell
Get-NetConnectionProfile | Select-Object Name, NetworkCategory, InterfaceAlias
```

---

## 三、PowerShell 执行策略

### 当前策略
- **CurrentUser**: RemoteSigned（允许运行本地脚本和已签名的远程脚本）

### 修改执行策略（如果脚本无法运行）
```powershell
# 以管理员身份运行 PowerShell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

### 临时绕过执行策略（单次运行）
```powershell
powershell -ExecutionPolicy Bypass -File "启动系统.ps1"
```

---

## 四、服务端口占用

### 检查端口占用
```powershell
# 检查 5173 端口
Get-NetTCPConnection -LocalPort 5173 -State Listen

# 检查 3000 端口
Get-NetTCPConnection -LocalPort 3000 -State Listen

# 检查 5432 端口
Get-NetTCPConnection -LocalPort 5432 -State Listen
```

### 查找占用端口的进程
```powershell
# 查找占用 3000 端口的进程
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess
```

### 强制停止进程
```powershell
# 停止指定 PID 的进程
Stop-Process -Id <PID> -Force
```

---

## 五、目录权限

### 项目目录
- **路径**: `D:\AI\SpeceAppDate\工单系统`
- **当前用户**: DESKTOP-AF392BI\Asus
- **权限**: 完全控制（继承自父目录）

### PostgreSQL 数据目录
- **路径**: `D:\pgsql16portable\data`
- **权限**: 当前用户完全控制

### 检查目录权限
```powershell
$path = "D:\AI\SpeceAppDate\工单系统"
$acl = Get-Acl $path
$acl.Access | Select-Object IdentityReference, FileSystemRights, AccessControlType
```

---

## 六、环境变量

### Node.js 路径
- **路径**: `D:\AI\node-v20.20.2-win-x64`
- **版本**: v20.20.2

### 临时添加到 PATH（当前会话）
```powershell
$env:PATH = "D:\AI\node-v20.20.2-win-x64;$env:PATH"
```

### 永久添加到 PATH（需要管理员权限）
```powershell
# 用户级别
[Environment]::SetEnvironmentVariable("Path", $env:Path + ";D:\AI\node-v20.20.2-win-x64", "User")

# 系统级别（需要管理员）
[Environment]::SetEnvironmentVariable("Path", $env:Path + ";D:\AI\node-v20.20.2-win-x64", "Machine")
```

---

## 七、数据库配置

### PostgreSQL 连接信息
- **主机**: 127.0.0.1
- **端口**: 5432
- **数据库**: ticket_system
- **用户名**: ticket
- **密码**: ticket123

### 数据库文件位置
- **数据目录**: `D:\pgsql16portable\data`
- **日志文件**: `D:\pgsql16portable\pg.log`
- **可执行文件**: `D:\pgsql16portable\pgsql\bin\`

### 手动启动/停止 PostgreSQL
```powershell
# 启动
& "D:\pgsql16portable\pgsql\bin\pg_ctl.exe" start -D "D:\pgsql16portable\data" -l "D:\pgsql16portable\pg.log"

# 停止
& "D:\pgsql16portable\pgsql\bin\pg_ctl.exe" stop -D "D:\pgsql16portable\data" -m fast

# 重启
& "D:\pgsql16portable\pgsql\bin\pg_ctl.exe" restart -D "D:\pgsql16portable\data" -m fast

# 查看状态
& "D:\pgsql16portable\pgsql\bin\pg_ctl.exe" status -D "D:\pgsql16portable\data"
```

---

## 八、常见问题排查

### 1. 防火墙规则失效
**症状**: 同事无法访问 `http://192.168.26.66:5173`

**解决**:
```powershell
# 以管理员身份重新添加规则
New-NetFirewallRule -DisplayName "工单系统-前端Vite" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow -Profile Private,Domain,Public
New-NetFirewallRule -DisplayName "工单系统-后端API" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow -Profile Private,Domain,Public
```

### 2. 网络类型变回 Public
**症状**: 防火墙规则不生效

**解决**:
```powershell
# 方案 1: 改回 Private（需要管理员权限）
Set-NetConnectionProfile -InterfaceAlias "以太网" -NetworkCategory Private

# 方案 2: 为 Public 网络添加规则
New-NetFirewallRule -DisplayName "工单系统-前端Vite-Public" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow -Profile Public
New-NetFirewallRule -DisplayName "工单系统-后端API-Public" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow -Profile Public
```

### 3. 端口被占用
**症状**: 启动时提示 `EADDRINUSE`

**解决**:
```powershell
# 查找占用进程
Get-NetTCPConnection -LocalPort 3000 -State Listen
Get-Process -Id <PID>

# 停止进程
Stop-Process -Id <PID> -Force
```

### 4. PostgreSQL 无法启动
**症状**: 后端报错 `connect ECONNREFUSED 127.0.0.1:5432`

**解决**:
```powershell
# 检查 PostgreSQL 是否运行
Get-Process -Name postgres

# 手动启动
& "D:\pgsql16portable\pgsql\bin\pg_ctl.exe" start -D "D:\pgsql16portable\data" -l "D:\pgsql16portable\pg.log"

# 查看日志
Get-Content "D:\pgsql16portable\pg.log" -Tail 50
```

### 5. 脚本无法运行
**症状**: 双击 `.ps1` 文件报错

**解决**:
```powershell
# 方案 1: 修改执行策略
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned

# 方案 2: 右键 -> 使用 PowerShell 运行

# 方案 3: 临时绕过
powershell -ExecutionPolicy Bypass -File "启动系统.ps1"
```

---

## 九、备份和恢复

### 备份数据库
```powershell
# 备份到 SQL 文件
& "D:\pgsql16portable\pgsql\bin\pg_dump.exe" -h 127.0.0.1 -U ticket -d ticket_system -f "backup_$(Get-Date -Format 'yyyyMMdd_HHmmss').sql"
```

### 恢复数据库
```powershell
# 从 SQL 文件恢复
& "D:\pgsql16portable\pgsql\bin\psql.exe" -h 127.0.0.1 -U ticket -d ticket_system -f "backup_20260513_093000.sql"
```

---

## 十、联系信息

### 技术支持
- **项目路径**: `D:\AI\SpeceAppDate\工单系统`
- **文档路径**: `D:\AI\SpeceAppDate\工单系统\docs`
- **日志路径**: 
  - PostgreSQL: `D:\pgsql16portable\pg.log`
  - 后端: 控制台输出
  - 前端: 控制台输出

### 快速启动
1. 双击 `启动系统.ps1`（或右键 -> 使用 PowerShell 运行）
2. 等待所有服务启动完成
3. 访问 `http://localhost:5173` 或 `http://192.168.26.66:5173`

### 快速停止
1. 双击 `停止系统.ps1`（或右键 -> 使用 PowerShell 运行）
2. 所有服务将自动停止

---

**最后更新**: 2026-05-13  
**版本**: v1.0
