# 8个生产问题修复总结

## ✅ 已修复问题

### 问题3：同意修改后状态变成已退回 ✅ 已修复

**问题根因**：
- `DispatchedOrderService.approveModify`方法（843行）在批准修改后，将状态设为`PENDING`（未接单）
- 用户期望：批准修改后应该恢复到`PROCESSING`（已接单/处理中）状态

**修复方案**：
```typescript
// 修改前（错误）
order.status = shouldRedispatch ? DispatchedOrderStatus.PENDING : previousStatus;
order.acceptedAt = shouldRedispatch ? null : order.acceptedAt;

// 修改后（正确）
order.status = shouldRedispatch ? DispatchedOrderStatus.PROCESSING : previousStatus;
order.acceptedAt = shouldRedispatch ? (order.acceptedAt || new Date()) : order.acceptedAt;
```

**修改文件**：`backend/src/modules/dispatched-orders/dispatched-order.service.ts`

**影响范围**：仅影响从`RETURNED`状态批准修改后的状态流转

---

## 🔍 需要真实测试验证的问题

### 问题2：退回工单修改后无法提交 ⚠️ 代码逻辑正常

**代码分析结果**：
- 后端`resubmitDispatched`方法（1115-1122行）明确允许`RETURNED`状态重新提交
- 前端`canCreatorResubmit`逻辑（375行）在`returned`状态下显示"重新提交"按钮
- API调用链路完整：前端→`/dispatched-orders/${id}/resubmit`→后端处理

**可能原因**：
1. 用户权限不足（不是工单创建人）
2. 特定场景下的状态冲突（如父工单审批中）
3. 前端报错但用户未反馈具体错误信息

**建议**：需要真实环境测试，查看具体错误信息

---

## 📋 待分析问题

### 问题1：收到的工单申请没有显示联系方式

**前端代码**：
- `MyDispatched/index.tsx`已有手机号和邮箱列
- 数据源：`record.extra_data?.mobile`和`record.extra_data?.email`

**后端代码**：
- `toListItem`方法返回`extraData`字段
- 导入模块已配置`mobile`和`email`字段映射

**可能根因**：
1. 历史数据：旧工单导入时未保存mobile/email字段
2. 字段映射问题：中文列名→字段代码的映射未生效
3. 数据清洗问题：extraData被覆盖或清空

**下一步**：需要查询数据库确认`work_orders`表中`extra_data`字段是否包含mobile和email

---

### 问题4：增员/减员关键字段放主界面

**需求**：
- 增员：参保地、起始月
- 减员：缴纳地区、停保月

**实现方案**：
在`MyDispatched/index.tsx`的`columns`数组中新增4个列，条件显示：
```typescript
{
  title: '参保地',
  key: 'social_location',
  width: 120,
  hideInSearch: true,
  render: (_, record) => {
    if (['data_entry', 'social_insurance'].includes(record.module_code)) {
      return String(record.extra_data?.social_location || '-');
    }
    return null;
  },
}
```

---

### 问题5：重复信息上传无法自动判别

**需求**：相同证件号的员工重复提交时应该拦截

**需要检查**：
1. 导入预览时是否检查证件号重复
2. 工单创建时是否检查同一员工的未完成工单

**可能位置**：
- `backend/src/modules/imports/`导入服务
- `backend/src/modules/work-orders/work-order-validation.service.ts`

---

### 问题6：减员子工单未显示具体内容

**问题描述**：减员子工单详情页只显示姓名和证件号，其他字段未显示

**可能根因**：
1. `visibleFields`配置问题：减员模块的可见字段列表不完整
2. 字段权限配置问题：某些字段被设为`HIDDEN`
3. 详情视图模板配置问题

**需要检查**：
- `backend/seeds/modules.seed.ts`中减员模块的`visibleFields`配置
- `detail_view_templates`表中减员模块的模板

---

### 问题7：基本工资和试用期工资字段格式

**需求**：这两个字段应该是文本类型（允许输入"面议"、"按协议"等）

**检查点**：`fields`表中`base_salary`和`probation_salary`的`field_type`

**修复方案**（如果确认是number类型）：
```sql
UPDATE fields SET field_type = 'text' WHERE field_code IN ('base_salary', 'probation_salary');
```

---

### 问题8：福报人员社保公积金子工单详情页数据显示不全

**问题描述**：特殊标记的员工，社保公积金子工单详情页缺少字段

**可能根因**：
1. 社保公积金模块的详情视图模板字段不全
2. 特殊员工类型的字段过滤逻辑问题

**需要检查**：
- 社保公积金模块（`social_insurance`）的`visibleFields`
- 详情视图模板配置

---

## 下一步行动计划

### 立即可做
1. ✅ 运行回归测试验证问题3的修复
2. 实现问题4（增减员关键字段放主界面）- 纯前端改动
3. 检查问题7（字段类型）- 简单数据库查询和修改

### 需要数据库查询
1. 问题1：查询`work_orders`表确认extraData内容
2. 问题7：查询`fields`表确认字段类型
3. 问题6、8：查询模块配置和详情视图模板

### 需要功能实现
1. 问题5：重复信息判别逻辑
2. 问题6、8：完善详情视图配置

### 需要真实环境测试
1. 问题2：退回工单提交（代码逻辑正常，需确认实际表现）
2. 问题1：联系方式显示（需确认数据是否存在）
