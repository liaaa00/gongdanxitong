# 工单系统 - 局域网部署使用指南

## 📋 目录

1. [系统架构说明](#系统架构说明)
2. [服务器管理员操作指南](#服务器管理员操作指南)
3. [同事访问指南](#同事访问指南)
4. [常见问题解决](#常见问题解决)
5. [技术细节](#技术细节)

---

## 🏗️ 系统架构说明

### 部署模式：局域网单机服务器

```
┌─────────────────────────────────────────────────────────┐
│                    服务器电脑                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │  PostgreSQL 数据库 (127.0.0.1:5432)              │   │
│  │  - 所有工单数据                                   │   │
│  │  - 用户和权限数据                                 │   │
│  └──────────────────────────────────────────────────┘   │
│                         ↑                                │
│  ┌──────────────────────────────────────────────────┐   │
│  │  后端 API (0.0.0.0:3000)                         │   │
│  │  - 监听所有网络接口                               │   │
│  │  - 处理业务逻辑                                   │   │
│  └──────────────────────────────────────────────────┘   │
│                         ↑                                │
│  ┌──────────────────────────────────────────────────┐   │
│  │  前端页面 (0.0.0.0:5173)                         │   │
│  │  - 监听所有网络接口                               │   │
│  │  - 提供用户界面                                   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  服务器 IP: 192.168.26.66                               │
└─────────────────────────────────────────────────────────┘
                         ↑
                         │ 局域网访问
                         │
        ┌────────────────┼────────────────┐
        │                │                │
   ┌────────┐       ┌────────┐      ┌────────┐
   │ 同事A  │       │ 同事B  │      │ 同事C  │
   │ 电脑   │       │ 电脑   │      │ 电脑   │
   └────────┘       └────────┘      └────────┘
   访问: http://192.168.26.66:5173
```

### 关键特性

✅ **数据互通**：所有同事访问同一个数据库，数据实时同步  
✅ **单一服务器**：只有服务器电脑运行后端和数据库  
✅ **局域网访问**：同事通过服务器 IP 访问，无需安装任何软件  
✅ **权限隔离**：不同角色看到不同的数据和功能  

---

## 👨‍💼 服务器管理员操作指南

### 首次部署步骤

#### 1. 配置防火墙（仅需一次）

**以管理员身份运行**：
```powershell
右键 "配置防火墙.ps1" -> 以管理员身份运行
```

这将开放以下端口：
- **3000**：后端 API 端口
- **5173**：前端页面端口

#### 2. 初始化数据库（仅需一次）

```powershell
cd D:\AI\SpeceAppDate\工单系统\backend
$env:PATH="D:\AI\node-v20.20.2-win-x64;$env:PATH"
npm run seed
```

这将创建：
- 系统管理员账号（lizhanbo, wangzixi）
- 业务负责人账号（aolei, xuekun, yuqinxia）
- 各组长和组员账号
- 角色和权限配置

### 日常启动步骤

#### 方式一：使用局域网启动脚本（推荐）

```powershell
双击运行 "局域网启动.ps1"
```

脚本会自动：
1. ✅ 清理所有残留进程
2. ✅ 强制释放端口 3000 和 5173
3. ✅ 启动 PostgreSQL 数据库
4. ✅ 重新编译后端最新代码
5. ✅ 启动后端（监听 0.0.0.0:3000）
6. ✅ 启动前端（监听 0.0.0.0:5173）
7. ✅ 显示访问地址

#### 方式二：手动启动

```powershell
# 1. 启动 PostgreSQL
D:\pgsql16portable\pgsql\bin\pg_ctl.exe start -D D:\pgsql16portable\data

# 2. 启动后端
cd D:\AI\SpeceAppDate\工单系统\backend
$env:PATH="D:\AI\node-v20.20.2-win-x64;$env:PATH"
$env:HOST="0.0.0.0"
npm run build
node dist\main.js

# 3. 启动前端（新窗口）
cd D:\AI\SpeceAppDate\工单系统\frontend
$env:PATH="D:\AI\node-v20.20.2-win-x64;$env:PATH"
node_modules\.bin\vite.cmd --host 0.0.0.0 --port 5173
```

### 停止服务

```powershell
双击运行 "停止系统.ps1"
```

或手动停止：
```powershell
# 停止所有 Node 进程
Get-Process node | Stop-Process -Force

# 停止 PostgreSQL
D:\pgsql16portable\pgsql\bin\pg_ctl.exe stop -D D:\pgsql16portable\data
```

### 查看服务器 IP 地址

```powershell
ipconfig | Select-String "IPv4"
```

常见 IP 地址：
- `192.168.x.x`：局域网 IP（告诉同事这个）
- `127.0.0.1`：本机回环地址（仅服务器自己用）

### 更新代码后的操作

**重要**：每次修改代码后，必须重新启动才能生效！

```powershell
# 1. 停止服务
.\停止系统.ps1

# 2. 重新启动（会自动编译最新代码）
.\局域网启动.ps1
```

### 重置数据库（慎用）

```powershell
cd D:\AI\SpeceAppDate\工单系统\backend
$env:PATH="D:\AI\node-v20.20.2-win-x64;$env:PATH"

# 删除所有数据并重新初始化
npm run seed
```

⚠️ **警告**：这会删除所有工单数据！

---

## 👥 同事访问指南

### 访问地址

**前端页面**：
```
http://192.168.26.66:5173
```

⚠️ **重要**：
- ✅ 必须使用服务器的 IP 地址（192.168.26.66）
- ❌ 不能使用 `localhost` 或 `127.0.0.1`
- ✅ 确保与服务器在同一局域网

### 测试账号

所有演示/种子账号的默认密码：`123456`（`admin123` 是旧口径，会返回 401，不可用于演示）

| 账号 | 姓名 | 角色 | 权限 |
|------|------|------|------|
| lizhanbo | 李占博 | 系统管理员 | 所有权限 |
| wangzixi | 王梓曦 | 系统管理员 | 所有权限 |
| aolei | 敖蕾 | 业务负责人 | 管理业务部门 |
| xuekun | 薛锟 | 业务负责人 | 管理业务部门 |
| shenwenjun | 沈文君 | 业务组长 | 管理业务组 |
| yaoyiping | 姚怡萍 | 业务组员 | 创建工单 |

### 首次登录步骤

1. 打开浏览器（推荐 Chrome 或 Edge）
2. 输入地址：`http://192.168.26.66:5173`
3. 输入账号和密码
4. 点击"登录"

### 浏览器要求

- ✅ Chrome 90+
- ✅ Edge 90+
- ✅ Firefox 88+
- ❌ IE 不支持

### 网络要求

- ✅ 与服务器在同一局域网
- ✅ 能 ping 通服务器 IP：`ping 192.168.26.66`
- ✅ 防火墙允许访问端口 5173

---

## 🔧 常见问题解决

### 问题 1：同事无法访问

**症状**：浏览器显示"无法访问此网站"或"连接超时"

**排查步骤**：

1. **检查服务器是否启动**
   ```powershell
   # 在服务器电脑上运行
   netstat -ano | Select-String "5173"
   ```
   应该看到 `LISTENING` 状态

2. **检查网络连通性**
   ```powershell
   # 在同事电脑上运行
   ping 192.168.26.66
   ```
   应该有响应

3. **检查防火墙**
   ```powershell
   # 在服务器电脑上运行（管理员权限）
   Get-NetFirewallRule -DisplayName "工单系统*"
   ```
   应该看到两条规则，状态为 `Enabled`

4. **重新配置防火墙**
   ```powershell
   # 以管理员身份运行
   .\配置防火墙.ps1
   ```

### 问题 2：页面显示"Something went wrong"

**原因**：后端未启动或数据库连接失败

**解决方案**：

1. **检查后端是否运行**
   ```powershell
   # 在服务器电脑上运行
   Invoke-WebRequest -Uri "http://127.0.0.1:3000/api/health"
   ```
   应该返回 200 状态码

2. **检查 PostgreSQL 是否运行**
   ```powershell
   Get-Process postgres
   ```
   应该看到多个 postgres 进程

3. **重新启动服务**
   ```powershell
   .\停止系统.ps1
   .\局域网启动.ps1
   ```

### 问题 3：登录后显示旧数据

**原因**：浏览器缓存或后端运行旧版本

**解决方案**：

1. **清除浏览器缓存**
   - 按 `Ctrl + Shift + R` 强制刷新
   - 或按 `F12` 打开开发者工具 -> Network -> Disable cache

2. **重新启动服务器**
   ```powershell
   .\停止系统.ps1
   .\局域网启动.ps1
   ```

### 问题 4：端口被占用

**症状**：启动脚本显示"端口已被占用"

**解决方案**：

```powershell
# 查找占用端口的进程
netstat -ano | Select-String "3000"
netstat -ano | Select-String "5173"

# 强制停止所有 Node 进程
Get-Process node | Stop-Process -Force

# 重新启动
.\局域网启动.ps1
```

### 问题 5：数据库连接失败

**症状**：后端日志显示"database connection error"

**解决方案**：

1. **检查 PostgreSQL 是否运行**
   ```powershell
   Get-Process postgres
   ```

2. **手动启动 PostgreSQL**
   ```powershell
   D:\pgsql16portable\pgsql\bin\pg_ctl.exe start -D D:\pgsql16portable\data
   ```

3. **检查数据库配置**
   ```powershell
   # 查看后端配置
   cat D:\AI\SpeceAppDate\工单系统\backend\.env
   ```
   确保：
   - `DB_HOST=127.0.0.1`
   - `DB_PORT=5432`
   - `DB_USERNAME=ticket`
   - `DB_PASSWORD=ticket123`
   - `DB_DATABASE=ticket_system`

---

## 🔬 技术细节

### 网络监听配置

#### 后端（NestJS）

```typescript
// src/main.ts
await app.listen(port); // 默认监听 0.0.0.0
```

通过环境变量 `HOST=0.0.0.0` 确保监听所有网络接口。

#### 前端（Vite）

```bash
vite --host 0.0.0.0 --port 5173
```

`--host 0.0.0.0` 参数使 Vite 监听所有网络接口。

### API 代理配置

前端通过环境变量配置后端地址：

```env
# frontend/.env
VITE_API_BASE_URL=http://192.168.26.66:3000
```

请求流程：
```
同事浏览器 -> http://192.168.26.66:5173 (前端)
           -> http://192.168.26.66:3000/api (后端)
           -> PostgreSQL (127.0.0.1:5432)
```

### 数据库架构

```
PostgreSQL (127.0.0.1:5432)
├── Database: ticket_system
│   ├── users (用户表)
│   ├── roles (角色表)
│   ├── user_roles (用户角色关联)
│   ├── departments (部门表)
│   ├── work_orders (工单表)
│   ├── customers (客户表)
│   └── ... (其他业务表)
```

### 端口说明

| 端口 | 服务 | 监听地址 | 用途 |
|------|------|----------|------|
| 5432 | PostgreSQL | 127.0.0.1 | 数据库（仅本机） |
| 3000 | 后端 API | 0.0.0.0 | 业务逻辑（局域网） |
| 5173 | 前端页面 | 0.0.0.0 | 用户界面（局域网） |

### 防火墙规则

```powershell
# 查看规则
Get-NetFirewallRule -DisplayName "工单系统*" | Format-Table

# 规则详情
Name: 工单系统-后端API
Direction: Inbound
Protocol: TCP
LocalPort: 3000
Action: Allow
Profile: Domain, Private

Name: 工单系统-前端页面
Direction: Inbound
Protocol: TCP
LocalPort: 5173
Action: Allow
Profile: Domain, Private
```

### 进程管理

```powershell
# 查看所有相关进程
Get-Process | Where-Object {
    $_.ProcessName -eq 'node' -or
    $_.ProcessName -eq 'postgres'
}

# 查看端口占用
netstat -ano | Select-String "LISTENING" | Select-String "3000|5173|5432"
```

---

## 📞 技术支持

### 日志位置

- **PostgreSQL 日志**：`D:\pgsql16portable\pg.log`
- **后端日志**：后端 PowerShell 窗口
- **前端日志**：前端 PowerShell 窗口 + 浏览器控制台（F12）

### 健康检查

```powershell
# 检查后端健康状态
Invoke-WebRequest -Uri "http://127.0.0.1:3000/api/health"

# 检查前端是否可访问
Invoke-WebRequest -Uri "http://127.0.0.1:5173"

# 检查数据库连接
$env:PGPASSWORD='ticket123'
& 'D:\pgsql16portable\pgsql\bin\psql.exe' -h 127.0.0.1 -U ticket -d ticket_system -c "SELECT 1"
```

### 性能监控

```powershell
# 查看进程资源占用
Get-Process node, postgres | Select-Object Name, CPU, WorkingSet, Id

# 查看网络连接
netstat -ano | Select-String "3000|5173"
```

---

## 📝 更新日志

### v1.0 (2026-05-14)
- ✅ 初始版本
- ✅ 局域网单机服务器部署
- ✅ 自动清理残留进程
- ✅ 强制释放端口
- ✅ 自动编译最新代码
- ✅ 防火墙配置脚本
- ✅ 完整使用文档

---

## 🎯 最佳实践

### 服务器管理员

1. ✅ 每天第一个到公司时启动系统
2. ✅ 下班前停止系统（可选）
3. ✅ 定期备份数据库（建议每周）
4. ✅ 更新代码后必须重启服务
5. ✅ 保持服务器电脑开机且不休眠

### 同事使用

1. ✅ 使用服务器 IP 访问，不要用 localhost
2. ✅ 及时保存工作，避免数据丢失
3. ✅ 遇到问题先刷新页面（Ctrl+Shift+R）
4. ✅ 不要在浏览器中保存密码（安全考虑）
5. ✅ 下班前退出登录

### 安全建议

1. ✅ 定期修改密码
2. ✅ 不要在公共网络使用
3. ✅ 不要将服务器暴露到互联网
4. ✅ 定期检查访问日志
5. ✅ 备份重要数据

---

**文档版本**：v1.0  
**最后更新**：2026-05-14  
**维护者**：系统管理员
