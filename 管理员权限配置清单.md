# 工单系统 - 管理员权限配置清单

> **重要**：此文档记录了所有需要管理员权限才能配置的项目，已全部配置完成。

---

## ✅ 已完成的管理员权限配置

### 1. 系统环境变量（永久生效）

**配置内容**：
- Node.js 路径已添加到系统 PATH
- 路径：`D:\AI\node-v20.20.2-win-x64`

**验证命令**：
```powershell
[Environment]::GetEnvironmentVariable("Path", "Machine") -like "*node-v20.20.2*"
```

**效果**：
- ✅ 任何用户都可以直接使用 `node` 命令
- ✅ 不需要每次手动设置 PATH
- ✅ 重启后仍然有效

---

### 2. PowerShell 执行策略（系统级）

**配置内容**：
- LocalMachine 执行策略：`RemoteSigned`
- CurrentUser 执行策略：`RemoteSigned`

**验证命令**：
```powershell
Get-ExecutionPolicy -List
```

**效果**：
- ✅ 所有用户都可以运行本地 PowerShell 脚本
- ✅ 不需要每次 `-ExecutionPolicy Bypass`
- ✅ 双击 `.ps1` 文件可以直接运行

---

### 3. 防火墙规则（6 条，永久生效）

**配置内容**：

| 规则名称 | 端口 | 协议 | 方向 | 网络类型 |
|---------|------|------|------|----------|
| 工单系统-前端Vite | 5173 | TCP | 入站 | Private, Domain |
| 工单系统-后端API | 3000 | TCP | 入站 | Private, Domain |
| PostgreSQL数据库 | 5432 | TCP | 入站 | Private, Domain |
| 工单系统-前端Vite-Public | 5173 | TCP | 入站 | Public |
| 工单系统-后端API-Public | 3000 | TCP | 入站 | Public |
| PostgreSQL数据库-Public | 5432 | TCP | 入站 | Public |

**验证命令**：
```powershell
Get-NetFirewallRule -DisplayName "*工单系统*","*PostgreSQL*" | Select-Object DisplayName, Enabled, Profile
```

**效果**：
- ✅ 局域网访问始终可用（无论网络类型是 Private 还是 Public）
- ✅ 重启后仍然有效
- ✅ 即使网络类型变化也不影响

**手动添加命令**（如需重新配置）：
```powershell
# Private/Domain 网络
New-NetFirewallRule -DisplayName "工单系统-前端Vite" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow -Profile Private,Domain
New-NetFirewallRule -DisplayName "工单系统-后端API" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow -Profile Private,Domain
New-NetFirewallRule -DisplayName "PostgreSQL数据库" -Direction Inbound -LocalPort 5432 -Protocol TCP -Action Allow -Profile Private,Domain

# Public 网络
New-NetFirewallRule -DisplayName "工单系统-前端Vite-Public" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow -Profile Public
New-NetFirewallRule -DisplayName "工单系统-后端API-Public" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow -Profile Public
New-NetFirewallRule -DisplayName "PostgreSQL数据库-Public" -Direction Inbound -LocalPort 5432 -Protocol TCP -Action Allow -Profile Public
```

---

### 4. 开机自启动计划任务（永久生效）

**配置内容**：
- 任务名称：`工单系统自动启动`
- 触发条件：系统启动时
- 执行脚本：`D:\AI\SpeceAppDate\工单系统\启动系统.ps1`
- 运行权限：当前用户（最高权限）

**验证命令**：
```powershell
Get-ScheduledTask -TaskName "工单系统自动启动"
```

**效果**：
- ✅ 电脑开机后自动启动所有服务
- ✅ 不需要手动运行启动脚本
- ✅ 重启后自动恢复运行

**手动创建命令**（如需重新配置）：
```powershell
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-ExecutionPolicy Bypass -WindowStyle Hidden -File `"D:\AI\SpeceAppDate\工单系统\启动系统.ps1`""
$trigger = New-ScheduledTaskTrigger -AtStartup
$principal = New-ScheduledTaskPrincipal -UserId "$env:COMPUTERNAME\$env:USERNAME" -LogonType Interactive -RunLevel Highest
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable
Register-ScheduledTask -TaskName "工单系统自动启动" -Action $action -Trigger $trigger -Principal $principal -Settings $settings -Description "开机自动启动工单系统"
```

**禁用/启用自启动**：
```powershell
# 禁用
Disable-ScheduledTask -TaskName "工单系统自动启动"

# 启用
Enable-ScheduledTask -TaskName "工单系统自动启动"

# 删除
Unregister-ScheduledTask -TaskName "工单系统自动启动" -Confirm:$false
```

---

### 5. 网络配置（永久生效）

**配置内容**：
- 网络类型：`Private`（专用网络）
- 网络发现：已启用

**验证命令**：
```powershell
Get-NetConnectionProfile | Select-Object Name, NetworkCategory, InterfaceAlias
```

**效果**：
- ✅ 防火墙规则更容易生效
- ✅ 局域网访问更稳定
- ✅ 网络发现功能可用

**手动修改命令**（如需重新配置）：
```powershell
# 改为 Private
Set-NetConnectionProfile -InterfaceAlias "以太网" -NetworkCategory Private

# 改为 Public
Set-NetConnectionProfile -InterfaceAlias "以太网" -NetworkCategory Public

# 启用网络发现
netsh advfirewall firewall set rule group="网络发现" new enable=Yes
```

---

### 6. Windows Defender 排除项（可选）

**配置内容**：
- 排除目录：
  - `D:\AI\SpeceAppDate\工单系统`
  - `D:\pgsql16portable`
- 排除进程：
  - `node.exe`
  - `postgres.exe`

**验证命令**：
```powershell
Get-MpPreference | Select-Object ExclusionPath, ExclusionProcess
```

**效果**：
- ✅ 提升系统性能（减少实时扫描）
- ✅ 避免误报

**手动添加命令**（如需重新配置）：
```powershell
Add-MpPreference -ExclusionPath "D:\AI\SpeceAppDate\工单系统"
Add-MpPreference -ExclusionPath "D:\pgsql16portable"
Add-MpPreference -ExclusionProcess "node.exe"
Add-MpPreference -ExclusionProcess "postgres.exe"
```

---

## 📋 配置验证清单

运行以下命令验证所有配置：

```powershell
Write-Host "=== 配置验证 ===" -ForegroundColor Cyan

# 1. 系统 PATH
Write-Host "`n[1] 系统 PATH:" -ForegroundColor Yellow
if ([Environment]::GetEnvironmentVariable("Path", "Machine") -like "*node-v20.20.2*") {
    Write-Host "  ✓ Node.js 已在系统 PATH" -ForegroundColor Green
} else {
    Write-Host "  ✗ Node.js 不在系统 PATH" -ForegroundColor Red
}

# 2. 执行策略
Write-Host "`n[2] 执行策略:" -ForegroundColor Yellow
$policy = Get-ExecutionPolicy -Scope LocalMachine
Write-Host "  ✓ LocalMachine: $policy" -ForegroundColor Green

# 3. 防火墙规则
Write-Host "`n[3] 防火墙规则:" -ForegroundColor Yellow
$ruleCount = (Get-NetFirewallRule -DisplayName "*工单系统*","*PostgreSQL*").Count
Write-Host "  ✓ 已配置 $ruleCount 条规则" -ForegroundColor Green

# 4. 计划任务
Write-Host "`n[4] 计划任务:" -ForegroundColor Yellow
$task = Get-ScheduledTask -TaskName "工单系统自动启动" -ErrorAction SilentlyContinue
if ($task) {
    Write-Host "  ✓ 已配置（状态: $($task.State)）" -ForegroundColor Green
} else {
    Write-Host "  ✗ 未配置" -ForegroundColor Red
}

# 5. 网络类型
Write-Host "`n[5] 网络类型:" -ForegroundColor Yellow
$network = Get-NetConnectionProfile
Write-Host "  ✓ $($network.NetworkCategory)" -ForegroundColor Green

Write-Host ""
```

---

## 🔄 失去管理员权限后的影响

### ✅ 仍然可以做的事情

1. **运行系统**
   - ✅ 双击桌面快捷方式启动
   - ✅ 运行 `启动系统.ps1` 和 `停止系统.ps1`
   - ✅ 手动启动/停止服务

2. **局域网访问**
   - ✅ 同事可以访问 `http://192.168.26.66:5173`
   - ✅ 防火墙规则继续有效

3. **开机自启动**
   - ✅ 电脑重启后自动启动所有服务

4. **日常操作**
   - ✅ 查看日志
   - ✅ 修改代码
   - ✅ 重启服务

### ❌ 不能做的事情

1. **修改系统配置**
   - ❌ 修改系统 PATH
   - ❌ 修改执行策略
   - ❌ 添加/删除防火墙规则
   - ❌ 修改计划任务
   - ❌ 修改网络类型

2. **安装系统服务**
   - ❌ 注册 Windows 服务
   - ❌ 修改服务启动类型

### 🔧 如果需要重新配置

如果某些配置失效（如防火墙规则被删除），需要重新获取管理员权限，然后：

1. 打开 `权限配置文档.md`
2. 找到对应章节的"手动添加命令"
3. 以管理员身份运行 PowerShell
4. 复制粘贴命令执行

---

## 📞 常见问题

### Q1: 重启后防火墙规则失效？
**A**: 防火墙规则是永久的，不会失效。如果失效，可能是：
- 组策略覆盖了规则
- 防火墙被重置
- 规则被手动删除

**解决**：以管理员身份重新运行第 3 节的命令。

### Q2: 开机自启动不工作？
**A**: 检查计划任务状态：
```powershell
Get-ScheduledTask -TaskName "工单系统自动启动"
```
如果状态是 `Disabled`，启用它：
```powershell
Enable-ScheduledTask -TaskName "工单系统自动启动"
```

### Q3: 网络类型变回 Public 后无法访问？
**A**: 不用担心，我们已经为 Public 网络也添加了防火墙规则。如果还是不行，检查规则是否存在：
```powershell
Get-NetFirewallRule -DisplayName "*工单系统*Public*"
```

### Q4: 脚本无法运行？
**A**: 检查执行策略：
```powershell
Get-ExecutionPolicy -List
```
如果 `LocalMachine` 不是 `RemoteSigned`，以管理员身份运行：
```powershell
Set-ExecutionPolicy -Scope LocalMachine RemoteSigned -Force
```

---

## 📝 配置历史

| 日期 | 操作 | 操作人 |
|------|------|--------|
| 2026-05-13 | 初始配置（所有管理员权限项目） | Asus |

---

## 🎯 下一步建议

1. **测试开机自启动**：
   - 重启电脑
   - 等待 2-3 分钟
   - 访问 `http://localhost:5173` 验证

2. **测试局域网访问**：
   - 用手机连同一个 WiFi
   - 访问 `http://192.168.26.66:5173`
   - 测试登录和基本功能

3. **备份配置**：
   - 导出防火墙规则：
     ```powershell
     Get-NetFirewallRule -DisplayName "*工单系统*","*PostgreSQL*" | Export-Csv "防火墙规则备份.csv"
     ```
   - 导出计划任务：
     ```powershell
     Export-ScheduledTask -TaskName "工单系统自动启动" -TaskPath "\" | Out-File "计划任务备份.xml"
     ```

---

**最后更新**: 2026-05-13  
**版本**: v1.0  
**状态**: ✅ 所有配置已完成并验证
