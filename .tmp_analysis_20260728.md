# 工单系统核心逻辑分析报告
生成时间: 2026-07-28  
当前分支: main (生产基线)  
服务器: 192.168.26.195:22 (/data/apps/work-order-system)

---

## 一、权限体系分析

### 1.1 角色定义

系统共定义了 **9种标准角色**,分为3大体系:

#### A. 业务发起方(前台)
- **admin** (管理员): 全局权限
- **biz_manager / business_owner** (业务负责人): 团队管理范围权限
- **biz_leader / business_group_leader** (业务组长): 本人发起+团队查看权限
- **biz_member / business_group_member** (业务员): 本人发起权限

#### B. 后道处理方
- **data_entry_leader / data_entry_supervisor** (增员报岗主管): 模块主管权限
- **shared_team_owner** (共享团队负责人): 跨模块管理权限  
- **labor_contract_member** (劳动合同专员): 合同模块处理权限
- **onboarding_resignation_member** (入职离职专员): 入离职模块处理权限
- **social_insurance_specialist** (社保专员): 社保模块处理权限

#### C. 角色别名兼容
代码中存在多套历史别名映射,如:
- `manager` → `business_owner`
- `salesperson` → `business_group_member`
- `contract_supervisor` / `onboarding_supervisor` 等主管角色

**关键代码位置**:
- 后端: `backend/src/common/auth/role-permissions.ts`
- 前端: `frontend/src/constants/roles.ts`

### 1.2 权限控制机制

#### (1) 路由级权限
- 入口: `frontend/src/config/routeVisibility.ts`
- 机制: 路径白名单 + 角色数组映射
- 特点: **未列出的路径默认拒绝访问**

**关键示例**:
```typescript
// 业务员只能看到主工单列表和创建入口
'/work-orders': BUSINESS_ORDER_ROLES,  // admin + leader + member
'/work-orders/create': WORK_ORDER_CREATE_ROLES,

// "我的工单"入口在Phase1仅管理员可见
'/my-work': [ROLE.ADMIN],
```

#### (2) 后端接口权限
- 守卫: `RolesGuard` (backend/src/common/guards/roles.guard.ts)
- 装饰器:
  - `@Roles([...])`  基础角色判断
  - `@BusinessPermission('action_code')` 业务操作权限
- 机制: `role-action-permissions` 表动态配置

**权限判断顺序**:
1. JWT认证 (JwtAuthGuard)
2. 业务操作权限 (BusinessPermission优先)
3. 角色白名单 (Roles兜底)

#### (3) 字段级权限
- 表: `field_permissions`
- 服务: `FieldPermissionService`
- 拦截器: `FieldPermissionInterceptor`
- 维度: 角色 × 场景 × 字段 → 可见/只读/隐藏/脱敏

---

## 二、工单流转核心逻辑

### 2.1 工单实体关系

```
WorkOrder (主工单)
  ├─ orderType: ONBOARDING | RESIGNATION  
  ├─ status: DRAFT | PENDING | PROCESSING | COMPLETED | VOID | WITHDRAWN
  ├─ extraData: JSONB (63个入职字段)
  └─ dispatchedOrders: DispatchedOrder[]  (1对多)

DispatchedOrder (子工单)
  ├─ moduleCode: contract | onboarding_contact | data_entry | social_insurance | ...
  ├─ status: 9种状态 (见下文)
  ├─ handlerId: 派发给的后道处理人
  ├─ visibleFields: 该模块可见字段列表
  └─ parentOrder: WorkOrder
```

**核心表**:
- `work_orders` 主工单
- `dispatched_orders` 子工单
- `module_handlers` 模块处理人配置
- `dispatch_rules` 派发规则配置

### 2.2 子工单9种状态

```typescript
enum DispatchedOrderStatus {
  PENDING              = 'pending',              // 1. 未接单
  PROCESSING           = 'processing',           // 2. 已接单
  MODIFY_PENDING       = 'modify_pending',       // 3. 修改审批中
  WITHDRAW_PENDING     = 'withdraw_pending',     // 4. 撤回审批中
  VOID_PENDING         = 'void_pending',         // 5. 作废审批中
  COMPLETED            = 'completed',            // 6. 已完成
  VOID                 = 'void',                 // 7. 已作废
  WITHDRAWN            = 'withdrawn',            // 8. 已撤回
  RETURNED             = 'returned',             // 9. 已退回
}
```

**状态流转规则** (业务规则回归清单 § 10):

| 起始状态 | 业务员操作 | 审批要求 | 目标状态 |
|---------|----------|---------|---------|
| pending | 修改/撤回/作废 | 无需审批,仅通知后道 | 直接生效 |
| processing | 修改 | **必须审批** | modify_pending → processing/void |
| processing | 撤回 | **必须审批** | withdraw_pending → withdrawn |
| processing | 作废 | **必须审批** | void_pending → void |
| completed | 修改/撤回/作废 | **必须审批** | 对应审批状态 |
| completed | 退回 | 后道/主管/管理员操作 | returned |

### 2.3 派发引擎逻辑

**核心服务**: `DispatchEngineService` (backend/src/modules/dispatch-engine/)

**派发流程**:
```
1. 读取 dispatch_rules (按orderType + priority排序)
2. AST评估 triggerConditions (支持 AND/OR/EQ/NEQ/IN/CONTAINS)
3. 模块去重 (同一moduleCode选priority最高的规则)
4. 解析处理人:
   - 优先: module_handlers 配置
   - 策略: TEAM_CLAIM (团队认领) | ROUND_ROBIN (轮询) | DIRECT_ASSIGN (指定)
5. 应用 work_order_module_configs (SLA时限配置)
6. 入职特殊逻辑: ensureOnboardingSplitChildren (4个固定模块)
```

**入职4拆子工单** (onboarding-dispatch.helper.ts):
```typescript
// 固定生成4个模块:
const ONBOARDING_MODULES = [
  'contract',           // 劳动合同签订
  'onboarding_contact', // 入职联系
  'data_entry',         // 增员报岗录入
  'social_insurance',   // 社保公积金
];
```

**关键代码**:
- `backend/src/modules/dispatch-engine/dispatch-engine.service.ts`
- `backend/src/modules/dispatch-engine/ast-evaluator.ts` (条件引擎)
- `backend/src/modules/dispatch-engine/handler-picker.service.ts` (人员选择)

---

## 三、数据权限过滤逻辑

### 3.1 后端查询范围控制

**核心方法**: `applyUserScope` (各Service中)

**业务员/组长**: 只看本人发起的工单
```typescript
// dispatched-order.service.ts L800+
if (hasAnyRole(user.roles, [...BUSINESS_LEADER_ROLES, ...BUSINESS_MEMBER_ROLES])) {
  queryBuilder.andWhere('workOrder.created_by = :userId', { userId: user.sub });
}
```

**业务负责人**: 只看团队工单
```typescript
if (hasManagementScopeRole(user.roles)) {
  // 关联 user_roles 表,过滤到所管理部门的工单
  queryBuilder
    .innerJoin('user_roles', 'ur', 'ur.user_id = workOrder.created_by')
    .andWhere('ur.department_id IN (:...deptIds)', { deptIds });
}
```

**后道人员**: 按授权模块过滤
```typescript
// module_handlers 表: user_id + module_code + is_active
queryBuilder.andWhere('dispatched.module_code IN (:...moduleCodes)', {
  moduleCodes: userAuthorizedModules,
});
```

### 3.2 前端菜单显示逻辑

**入口**: `frontend/src/layouts/BasicLayout.tsx`

**菜单配置**: 动态生成,基于:
1. `routeVisibility.ts` 的角色白名单
2. `canAccessPath(path, userRoles)` 运行时判断
3. 业务规则硬编码 (如: 组长不显示"我的待办")

**特殊规则** (docs/业务规则回归清单.md § 4):
- 业务负责人: 隐藏 "我发起的/我的退回/我的待办/我的已办"
- 业务组长: 隐藏 "我的待办"
- 业务员: 隐藏 "我的待办/团队工单"
- 后道人员: 隐藏 "我发起的/我的退回"

---

## 四、月份统计口径

**核心规则** (业务规则回归清单 § 2):

> 工单流转统计按派发/创建月份,不按完成时间

**计算逻辑**:
```sql
orderMonth = COALESCE(
  DATE_TRUNC('month', dispatched_at),
  DATE_TRUNC('month', created_at)
)
```

**影响范围**:
- 我的已办: 按orderMonth筛选,不按completed_at
- 历史工单: 按orderMonth
- 仪表盘: 当月工单按orderMonth统计
- 入职/离职主工单列表: 默认当前月

**禁止事项**:
- ❌ 不得把"我的已办"改回按完成时间筛选
- ❌ 不得让5月派发、6月完成的工单归入6月统计

---

## 五、仪表盘统计口径

**核心规则** (业务规则回归清单 § 3):

```
当月工单总数 = 未办结 + 已完成 + 已作废

未办结 = pending | processing | modify_pending | 
         withdraw_pending | void_pending | 
         withdrawn | returned

已完成 = completed
已作废 = void
```

**业务组长仪表盘**: 可切换 "本人/团队" 视角
- 本人: 只统计 created_by = 当前用户
- 团队: 统计本部门全部成员发起的工单

**后道仪表盘五项名称** (必须与业务员一致):
1. 本月工单总数
2. 单月待处理 / 本月待处理
3. 总待处理
4. 本月已完成
5. 本月已作废

---

## 六、审批流程逻辑

### 6.1 审批触发条件

**规则矩阵**:

| 工单状态 | 业务员操作 | 审批流转 |
|---------|----------|---------|
| 未接单 | 修改字段 | ✅ 直接生效,通知后道 |
| 未接单 | 撤回工单 | ✅ 直接撤回,通知后道 |
| 未接单 | 作废工单 | ✅ 直接作废,通知后道 |
| 已接单 | 修改字段 | ❌ 进入modify_pending审批 |
| 已接单 | 撤回工单 | ❌ 进入withdraw_pending审批 |
| 已接单 | 作废工单 | ❌ 进入void_pending审批 |
| 已完成 | 任何变更 | ❌ 必须审批 |

### 6.2 审批权限

**审批人**: 当前子工单的后道处理人 (handlerId)

**管理员特权**: 
- 可直接修改已完成工单,无需审批 (WorkOrderService中判断)
- 可执行退回操作

**模块主管权限**:
- 可审批本模块的修改/撤回/作废请求
- 可退回已完成工单

### 6.3 审批后数据处理

**修改审批通过**:
```typescript
// dispatched-order.service.ts approveModification()
1. 应用业务员提交的字段修改 (从operation_log读取diff)
2. 子工单状态回到原状态 (completed → completed, processing → processing)
3. 已完成工单: 保留原 completed_at 时间
4. 写operation_log: approve_modification
5. 发通知给业务员
```

**修改审批拒绝**:
```typescript
1. 子工单状态回到原状态
2. 不应用字段修改
3. 写operation_log: reject_modification
4. 发通知给业务员
```

---

## 七、关键业务约束

### 7.1 月份唯一性约束

**规则**: 同一员工身份证号,同一订单类型,同一月份只能存在一个工单

**实现**:
```sql
-- work_orders 表
UNIQUE INDEX idx_unique_idcard_ordertype_month 
ON work_orders (
  (extra_data->>'id_card_no'),
  order_type,
  DATE_TRUNC('month', COALESCE(dispatched_at, created_at))
)
WHERE status NOT IN ('void', 'withdrawn');
```

**代码位置**: `work-order.service.ts` → `findDuplicateIdCardInMonth()`

### 7.2 合同期限条件必填

**规则** (业务规则回归清单 § 13.1):
- `contract_term_type = '无固定期限'` → contract_end_date **非必填**
- `contract_term_type = '固定期限' | '任务期限'` → contract_end_date **必填**

**实现**:
```typescript
// backend seed-fields.ts
{
  code: 'contract_end_date',
  required: false,
  defaultRequired: false,
  conditionalRequired: {
    op: 'NEQ',  // Not Equal
    field: 'contract_term_type',
    value: '无固定期限',
  }
}
```

**前端**: `DynamicForm` 组件支持 `notEquals` 条件必填运算符

### 7.3 导入预览结构列剔除

**问题**: 模板自带的"字段名"、"附件"列被误解析为数据表头

**修复** (AI修改记录 2026-07-07):
```typescript
// excel-parser.service.ts
const TEMPLATE_STRUCTURAL_HEADERS = {'字段名', '附件'};

// 命中结构列时替换为占位符
if (TEMPLATE_STRUCTURAL_HEADERS.has(cellValue)) {
  headers[colIndex] = `__col_${colIndex + 1}__`;
}
```

---

## 八、服务器部署状态分析

### 8.1 部署目标绑定

**固定绑定** (CLAUDE.md § 13错误模式记录):
- 主机: `192.168.26.195:22`
- 用户: `admin`
- SSH Profile: `work-order-local-ssh` (SpectrAI加密)
- 部署目录: `/data/apps/work-order-system`
- 运行容器: `ticket_backend`, `ticket_frontend`, `ticket_postgres`, `ticket_nginx`

**禁止事项**:
- ❌ 不得使用 `xiangxin-new` / `xiangxin-prod` (属于其他项目)
- ❌ 不得按profile名称猜测目标

### 8.2 生产环境特征

**运行分支**: `main` (不含在职/省外功能)

**关键差异**:
```diff
- 当前feature分支包含:
  + 在职单项业务模块 (IN_SERVICE_*)
  + 省外增减员模块 (OUT_OF_PROVINCE_*)
  + 27省映射逻辑 (PROVINCES_27)
  + 新增migration 211/212/213

- 生产main分支不含以上功能
```

**最近生产部署** (AI修改记录):
- 2026-07-28: 合同期限条件必填修复 (手动同步到生产)
- 2026-07-21: 认证会话安全 + 首次改密守卫 (commit可复现部署)
- 2026-07-23: 通知红点优化 (commit e110db4)

### 8.3 数据保护验证

**核心表行数基线** (最近部署后):
```
work_orders       = 15
dispatched_orders = 46  
users             = 31
customers         = 25
notifications     = 141
operation_logs    = 211+
user_roles        = 35
```

**部署前后哈希校验**: 
- 全行SHA-256哈希必须一致
- 允许差异: 部署窗口内的正常业务操作 (login_success日志等)

---

## 九、核心代码位置索引

### 9.1 后端关键文件

| 模块 | 文件 | 职责 |
|-----|------|-----|
| 工单 | work-order.service.ts | 主工单CRUD、提交、作废、撤回 |
| 子工单 | dispatched-order.service.ts | 子工单接单、完成、退回、转派、审批 |
| 派发引擎 | dispatch-engine.service.ts | 规则评估、模块生成、处理人分配 |
| 权限守卫 | roles.guard.ts | 角色权限拦截 |
| 认证 | auth.service.ts | 登录、JWT、改密、锁定 |
| 字段权限 | field-permission.service.ts | 字段级可见性控制 |

### 9.2 前端关键文件

| 模块 | 文件 | 职责 |
|-----|------|-----|
| 路由权限 | config/routeVisibility.ts | 路由×角色矩阵 |
| 角色归一化 | constants/roles.ts | 历史角色别名映射 |
| 主布局 | layouts/BasicLayout.tsx | 菜单动态生成、权限判断 |
| 动态表单 | components/DynamicForm/index.tsx | 字段必填、条件必填、联动 |
| API请求 | services/request.ts | 统一axios、鉴权、错误处理 |

### 9.3 核心实体

| 实体 | 表名 | 核心字段 |
|-----|------|---------|
| WorkOrder | work_orders | orderNo, orderType, status, extraData, created_by |
| DispatchedOrder | dispatched_orders | moduleCode, status, handlerId, parentOrderId |
| User | users | username, isActive, mustChangePassword, failedLoginAttempts |
| UserRole | user_roles | user_id, role_id, department_id (复合主键) |
| FieldPermission | field_permissions | roleCode, scenario, fieldCode, mode |
| ModuleHandler | module_handlers | moduleCode, userId, dispatchStrategy, weight |

---

## 十、已知待确认问题

### 10.1 服务器连接问题

**现象**: 使用密码 `Qwer123456` 无法通过标准ssh连接

**原因分析**:
1. 可能需要使用SpectrAI加密profile `work-order-local-ssh`
2. 可能服务器禁用了密码登录,仅允许密钥认证
3. 本地环境缺少 `sshpass` 工具

**建议方案**:
- 使用SpectrAI SSH profile功能
- 或配置SSH密钥对
- 或通过其他安全通道获取服务器访问权限

### 10.2 在职/省外模块状态

**当前状态**: feature分支编辑中,未合并到main

**包含内容**:
- 在职单项业务 (3级分类、状态机、Sheet4派单)
- 省外增减员 (双业务范围、Sheet5派单、福建双人配置)
- 新增实体: InServiceOrder, OutOfProvinceOrder
- 新增migration: 211/212/213

**部署风险**:
- ❌ 不能直接将feature分支代码同步到生产
- ❌ 生产服务器不包含在职/省外依赖代码,编译会失败

### 10.3 字段权限实时生效验证

**文档说明**: 后端有 `FieldPermissionInterceptor` 拦截器

**待确认**:
1. 拦截器是否全局注册?
2. 哪些接口响应会应用字段过滤?
3. 前端是否有兜底校验?

---

## 十一、分析结论

### 核心流程正确性

✅ **权限体系**: 角色定义清晰,前后端双重校验,字段级权限完整

✅ **工单流转**: 9种状态明确,审批规则严格,防误操作设计合理

✅ **派发引擎**: AST条件引擎灵活,模块去重逻辑正确,4拆入职固化

✅ **数据范围**: 业务员/组长/负责人/后道的数据隔离清晰

✅ **月份口径**: 按派发/创建月统计,避免完成月混乱

### 代码质量特征

**优点**:
- TypeScript严格模式,类型安全
- 业务规则文档化 (回归清单)
- 修改留痕机制 (operation_log + AI修改记录)
- 测试覆盖率较高 (backend 69 suites)

**注意事项**:
- 历史角色别名多,需要维护映射表
- 入职字段63个,校验逻辑复杂
- 生产环境不支持自动seed,需要手动同步字段定义

### 服务器部署策略

**当前模式**: 手动文件同步 + Docker容器替换

**保护措施**:
- 部署前SHA-256校验
- 数据库全量dump备份
- 核心表行数哈希对比
- 回滚镜像标签保留

**改进空间**:
- 建立可复现的Git tag部署流程
- 自动化部署脚本
- 蓝绿部署或金丝雀发布

---

**分析完成**. 如有具体疑问,请指出章节编号.
