# 阶段1派单引擎底座测试报告

- 任务：`180236d1-c899-4ba6-90e0-965b9ea8f91f`
- 角色：QA工程师
- 日期：2026-07-27
- 契约基线：integration commit `6f153550` 的 `docs/阶段1派单引擎扩展架构契约-20260727.md`

## 验收矩阵

| 编号 | 层级 | 场景 | 预期 |
|---|---|---|---|
| S1-U01 | 单元 | 正式枚举值 | `ModuleType`、`TeamRole`、三个新 `OrderType` 与架构契约一致 |
| S1-U02 | 单元 | Sheet4/Sheet5 普通省份 | 读取各自 `<moduleCode>__<province>`，同省不串表 |
| S1-U03 | 单元 | 双人省份 | `/` 前主办连续派单不轮询，后者仅转派备选 |
| S1-U04 | 单元 | 主办停用 | 不把转派备选当初始接单人，返回空 handler |
| S1-U05 | 单元 | 无映射/跨表缺省 | Sheet4 缺失不得读取 Sheet5，反之亦然 |
| S1-U06 | 单元 | 旧 picker 无上下文 | 原 FIXED/权重行为不变 |
| S1-I01 | 集成 | 创建在职工单 | 生成 `in_service_single_business` 子单并按 Sheet4 派单 |
| S1-I02 | 集成 | 创建省外增员/减员工单 | 两种订单均生成 `out_of_province_dispatch` 子单并按 Sheet5 派单 |
| S1-R01 | 回归 | 入职派单 | 原拆单与处理人选择不变 |
| S1-R02 | 回归 | 续签派单 | 仍派至 `renewal_contract` 处理人 |
| S1-R03 | 回归 | 离职派单 | 原联系/录入/社保模块派单不变 |
| S1-R04 | 回归 | assignee/fallback 死字段 | 保留字段但新旧派单均不读取 |

## 已完成

- 新增 `backend/test/province-handler-dispatch.spec.ts`，共 15 个正式契约用例。
- 测试使用现有 `module_handlers` 命名空间键，不引入或依赖 `ProvinceHandler` 第二张表。
- 既有派单基线已执行：3 suites passed；31 tests passed；1 historical test skipped；0 failed。
- `caf930e` 曾对未验收候选实现执行 14 个用例；该候选使用第二张 `ProvinceHandler` 表、错误枚举和错误模块码，与正式架构冲突，结果已作废，不作为验收证据。

## Integration 验证

- integration HEAD：`42324b043c078bc92afe4a8d48c38cb432477dcf`。
- 正式契约套件编译失败，0 tests executed：`TeamRole` 缺 `IN_SERVICE`/`OUT_OF_PROVINCE`，`OrderType` 缺 `OUT_OF_PROVINCE_INCREASE`/`OUT_OF_PROVINCE_DECREASE`，`HandlerPicker.pick()` 仍接受 `businessType` 而非 `mappingSource`（4 处 TS2353）。
- 实现新增 `ProvinceHandler` 表，且使用 `single_business`、`out_of_province_increase` 等候选模块码；均违反已合入的 `6f153550` 正式契约。
- 阶段1结论：P0 NO-GO。旧派单基线仍为 3 suites、31 passed、1 historical skipped；新底座必须返工后重新执行 15 个正式契约用例、旧派单联合回归及根目录 `回归测试.ps1 -BackendOnly` 或全局回归。
