# 范围合规报告：03fa8751 范围守门与变更控制审查

更新时间：2026-06-24 11:16（Asia/Shanghai）
角色：范围守门与变更控制专员
任务ID：03fa8751-4672-4912-9a71-58bdf0023b1f

## 1. 本轮允许改动范围

仅允许与已确认需求 1-9 直接相关的代码、配置、种子、迁移、测试和必要报告：

1. 入职导入模板与用户 Excel/截图对齐：字段顺序、字段名称、表头标黄、必填/非必填/条件必填说明、填写要求、示例值、下拉选项、隐藏 `__options` sheet、数据验证源等。
2. 身份证自动派生：性别、出生日期、年龄；需在系统数据、详情页、导出表可见。
3. 试用期终止年月/试用期结束日期带出修复；需详情页和导出表可见。
4. 入职主工单导入模板“是否电子签”下拉改为 `1.是 / 2.否`；劳动合同/电子签相关字段按前置关系做条件必填导入校验并给出明确错误。
5. 合同详情/导出：劳动合同详情页新增“劳动合同模板（标准模板/特殊模板）”“劳动合同主体”；速创导出末尾新增两个字段；E签宝导出新增劳动合同模板；速创“签订方式”默认 `1.新签`；必须考虑既有数据库模板更新/迁移策略。
6. 字段修改审批与同步机制：未接单直接修改并同步；已接单变更审批；已完成不允许线上修改；结构化记录变更字段、原值、新值、同步/审批/不通过/保留旧值清单；尽量基于当前 9 种状态补充，不重做状态机。
7. 社保公积金增/减员类子工单新增 6 个办理反馈字段；结果固定“已完成/未完成”；全部完成自动完成，任一未完成不自动完成。
8. 社保公积金从“批量完成”调整为“批量反馈办理结果”；导入时按三项办理结果判断状态；设计需可复用，避免写死。
9. 批量接单混选已接单/未接单时只处理未接单、跳过已接单并提示成功/跳过数量。

## 2. 明确禁止/高风险改动范围

除非 Leader/用户另行确认，否则禁止：

- 顺手重构、无关格式化、全文件格式化、大范围重命名。
- 无关权限、菜单、看板、账号、登录、组织、角色、路由可见性逻辑修改。
- 引入与 1-9 无关的新依赖；修改 `package.json` / `package-lock.json` 需说明对应需求和必要性。
- 修改历史已稳定逻辑但没有需求依据。
- 直接提交 `dist/`、`node_modules/`、运行日志、临时上传文件等生成物，除非明确作为交付物且有必要性说明。
- 仅改 seed 导致已有数据库模板不生效；需求 5 需要迁移/更新既有模板策略。

## 3. 团队提交时必须提供的审查清单

每位成员提交成果时必须列明：

| 改动文件 | 对应需求编号 | 必要性说明 | 验证证据 | 范围判定 |
| --- | --- | --- | --- | --- |
| 待成员提交 | 1-9 | 待补充 | 待补充 | 待审 |

## 4. 初始阶段性检查记录

### 4.1 Git/diff 状态

- 检查时间：2026-06-24 11:10-11:15（Asia/Shanghai）。
- 工作目录：`D:\ai\speceappdate\工单系统`。
- 结果：当前目录及 4 层内未发现 `.git` 文件/目录，`git status` 返回 `NO_GIT_REPO`，因此无法使用标准 `git diff` / `git diff --name-status` 判断改动。
- 风险：缺少 Git 元数据会降低范围审查精度。建议 Leader/集成负责人确认真实仓库根目录或恢复 Git 元数据；在此之前所有成员必须主动提交文件清单与必要性说明。

### 4.2 当前可见近期写入对象（需后续确认是否纳入交付）

- `backend/package-lock.json`：2026-06-24 08:32，疑似依赖安装导致；若未引入与 1-9 直接相关依赖，应避免作为需求改动提交。
- `frontend/package-lock.json`：2026-06-24 08:47，疑似依赖安装导致；若未引入与 1-9 直接相关依赖，应避免作为需求改动提交。
- `backend/dist/`：2026-06-24 09:24，构建产物；默认不应作为源代码交付改动。
- `backend/node_modules/`、`frontend/node_modules/`：依赖目录；不得纳入交付。
- `backend-run.out.log`、`frontend-run.out.log` 等运行日志：默认不应纳入交付。

### 4.3 初始目录观察

- 主要代码目录：`backend/src`、`frontend/src`。
- 测试目录：`backend/test`、`frontend/e2e`、前端 `*.test.tsx` 等。
- 配置/脚本：`backend/scripts`、`frontend/scripts`、`nginx`、根目录 docker/启动脚本。
- 当前尚未看到本轮成员提交的源代码变更清单；后续需按成员成果逐项审查。

## 5. 当前范围判定

- 允许改动：尚无已确认成员改动清单。
- 可疑改动：`package-lock.json`、`dist/`、`node_modules/`、运行日志的近期写入需要排除或说明；在无 Git diff 情况下暂不判定为业务越界，但不建议纳入提交。
- 建议回退/不提交：默认不提交 `dist/`、`node_modules/`、运行日志；`package-lock.json` 只有在确有需求相关依赖变化时才保留。
- 仍需用户/Leader确认边界：真实 Git 仓库根目录或可对比基线；成员提交清单；需求 5 的既有模板数据更新策略是否允许迁移/一次性修复脚本。

## 6. 已发出的范围控制要求

2026-06-24 11:15 已向团队广播：每位成员提交阶段必须列明“改动文件 -> 对应需求编号 -> 必要性 -> 验证证据”；禁止顺手重构、无关格式化、大范围重命名、无关权限/菜单/看板/账号逻辑修改、无关依赖引入；如需改 package-lock、dist、运行日志、权限/菜单/账号/看板相关文件，需先说明必要性并等待范围确认。

## 7. 后续持续审查计划

1. 等待各工程师提交改动文件清单与必要性说明。
2. 若 Leader 提供真实 Git 仓库位置或集成分支，优先用 `git diff --name-status` / `git diff --stat` 做文件级审查。
3. 对涉及权限、菜单、账号、看板、状态机重构、依赖、全局格式化的文件进行高风险复核。
4. 发现越界时立即上报 Leader，并建议最小回退或拆分为单独需求。

## 8. 阶段性复查记录（二）

检查时间：2026-06-24 11:20-11:23（Asia/Shanghai）

### 8.1 文件写入复查

- 仍未发现当前工作目录存在 Git 元数据，`git diff` 不可用。
- 以本报告创建时间（2026-06-24 11:17）为界，复查以下目录：`backend/src`、`backend/test`、`backend/scripts`、`frontend/src`、`frontend/e2e`、`frontend/scripts`、`nginx`。
- 结果：上述源代码、配置、测试目录在本报告创建后未出现新的写入文件。
- 新增/更新报告文件：`reports/438b8ec3-qa-regression.md`，属于 QA 报告类产出，暂不判定为代码越界。注意：PowerShell 控制台读取该报告时显示乱码，后续如引用内容需注意编码。

### 8.2 团队任务状态复查

- 实现类任务仍为 `in_progress`，尚未看到成员完成提交或正式文件清单：
  - `72d4b0a1...` 入职导入模板与后端字段派生修复。
  - `3384d7bd...` 合同详情与导出字段修复。
  - `056b50ab...` 字段修改审批与同步机制。
  - `a5f21a99...` 社保公积金反馈与批量接单混选优化。
- `46f9bda6...` 阻塞与冲突治理监控已完成，属于治理/报告类任务；如其产物包含代码或配置改动，仍需按同一清单审查。

### 8.3 当前结论

- 当前阶段未发现新的源代码越界改动证据。
- 当前最大审查风险仍是缺少 Git diff 基线；需要 Leader/集成负责人提供真实仓库根目录、统一 diff，或各成员在提交时提供完整文件清单。
- 若后续出现 `package-lock.json`、权限/菜单/账号/看板、全局状态机、大范围测试快照/格式化变动，必须逐项说明需求编号与必要性；否则建议回退或拆分。

## 9. 无 Git 根模式补充复查记录（三）

检查时间：2026-06-24 11:39 前后（Asia/Shanghai）

### 9.1 无 Git 根风险确认

- 已再次从 `D:\ai\speceappdate\工单系统` 向上检查至 `D:\`，均未发现 `.git`。
- Leader 已确认平台任务元数据也未返回仓库信息；因此本轮暂不能依赖统一 `git diff` / `git status`。
- 风险结论：范围审查只能临时基于成员最终提交清单、文件时间戳、报告证据和显式排除清单，结论精度低于 Git diff 模式；最终交付前仍需集成负责人提供统一交付文件清单。

### 9.2 当前可见近期写入复核

- 近期后端源文件写入：
  - `backend/src/modules/dispatched-orders/dispatched-order.service.ts`，最后写入 2026-06-24 11:24:08。
  - `backend/src/modules/dispatched-orders/handling-feedback.ts`，创建/写入 2026-06-24 11:20:56。
- 初步范围判断：上述文件均位于派单/子工单处理模块，关键词集中在 `handlingFeedback`、社保/公积金、批量接单/批量完成、子工单状态处理等，表面上与需求 6-8 相关；但仍需负责成员补齐“改动文件 -> 需求编号 -> 必要性 -> 验证证据”，否则不能作为最终范围合规依据。
- 未见 `backend/test`、`frontend/src`、`frontend/e2e`、`nginx` 在本次复查窗口出现新的源文件写入。

### 9.3 必须排除或单独说明的交付项

- `backend/dist/` 最近有构建产物写入，默认不得纳入源代码交付。
- `backend/node_modules/`、`frontend/node_modules/` 及其缓存/测试结果文件不得纳入交付。
- `backend-run.out.log`、`frontend-run.out.log`、`*.err.log` 等运行日志不得纳入交付。
- `backend/package-lock.json`、`frontend/package-lock.json` 有近期写入；只有在成员明确说明存在与需求 1-9 直接相关的依赖变化并给出验证证据时才允许保留，否则建议排除。

### 9.4 阶段性结论

- 在无 Git 根模式下，当前仅能给出阶段性结论：尚未发现明显跨模块扩散或无关业务源文件写入证据。
- 不能给出最终范围合规结论的原因：实现类任务仍显示进行中，尚未看到所有成员最终提交清单与验证证据。
- 交付前复核门槛：所有实现成员必须提交完整清单；范围守门需逐项确认需求编号、必要性、验证证据，并明确排除 `dist/`、`node_modules/`、运行日志及无依赖必要性的 package-lock 变更。

## 10. 最终清单复核任务阶段性结论

检查时间：2026-06-24 11:44-11:52（Asia/Shanghai）

- 已为任务 `15967df9-4478-4a41-b636-5356794bafba` 新建阶段性最终复核报告：`reports/15967df9-scope-final.md`。
- 当前不能给出“最终范围合规通过”结论：多个实现/QA 任务仍为 `in_progress`，尚未提交完整的“改动文件 -> 需求编号 -> 必要性 -> 验证证据”最终清单。
- 11:40 后仍观察到后端实体/迁移/种子/派单模块、前端派单服务和批量导入弹窗持续写入，说明交付范围尚未冻结。
- 高风险待复核文件包括：`backend/src/modules/dispatched-orders/dispatched-order.service.ts`、`backend/src/database/seeds/seed-fields.ts`、`backend/src/database/seeds/seed-field-permissions.ts`、`backend/src/app.module.ts`、`backend/src/database/data-source.ts`、新增字段同步实体/迁移、`frontend/src/services/dispatchedOrders.ts`、`frontend/src/components/DispatchedBatchImportModal.tsx`。
- 明确排除项保持不变：`dist/`、`node_modules/`、`frontend/tsconfig.tsbuildinfo`、运行日志不得纳入交付；`backend/package-lock.json`、`frontend/package-lock.json` 仅在存在需求相关依赖变化与验证证据时允许。
- 后续收到成员最终清单后，应以 `reports/15967df9-scope-final.md` 第 8 节表格逐项补齐范围判定。
