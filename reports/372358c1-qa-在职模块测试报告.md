# 阶段2 在职管理模块 QA 验收报告（任务 372358c1）

- 日期：2026-07-27
- 角色：QA 工程师
- 集成基线：integration HEAD `20ffcaa`（后端阶段2实现 `in-service-orders` 模块）
- 结论：**GO**

## 背景变更说明

首轮 QA 时后端阶段2实现尚未合入 integration，QA 曾以自建骨架 `in-service-business`
补测并判 NO-GO。本轮集成时后端已交付**权威实现** `in-service-orders` 模块
（提交 `20ffcaa` / cc492be），包含：枚举、Entity、迁移 `in_service_orders`、
DTO、Service、Controller、状态机、Sheet2 三级分类幂等种子、AppModule 注册、
dispatch-engine 接入，并自带单测 `backend/test/in-service-orders.spec.ts`。

据此，QA 自建的骨架模块与重复枚举已**全部废弃**（会与权威实现产生重复声明编译冲突），
本轮改为对权威实现做验收，不再维护骨架测试。

## 验收范围与结果

### 1. 在职模块单测（权威 spec：in-service-orders.spec.ts）—— 11/11 passed
- 三级分类契约：每级分类连通（一级→二级→三级父子映射完整）
- 状态机：定义了必经流程 + **补料往返循环（pending_info 多次触发）** —— 覆盖关键风险
- 状态机：非法转移抛 400 业务异常
- Service 派单：草稿审批经 **Sheet4** 解析接单人
- Service 派单：**双人省份**手动 fallback 不覆盖 Sheet4 主办 —— 覆盖关键风险
- Service 派单：仅当 Sheet4 无在岗主办时才用显式 fallback
- Service 派单：既无映射又无 fallback 时 handler 保持 null（转手动指派）
- 往返：支持多次 processing / pending-info 往返
- 完成并归档
- 创建时拒绝非法分类路径
- 拒绝非法服务态转移

### 2. 派单回归（dispatch + province-handler）—— 12 suites / 115 passed / 1 skipped
入职/续签/离职派单与 Sheet4/Sheet5 契约全部保持，未被在职模块改动影响。

### 3. 后端全量单测 —— 66 passed / 1 skipped suite / **1 failed suite**
- 498 passed / 16 skipped / **1 failed**（515 total）
- 唯一失败：`p1-split4-dirty-return.spec.ts:155`（社保批量反馈备注）
  - 判定：**基线预先存在失败**，与本任务无关。
  - 依据：该 spec 对 in-service 零引用；后端阶段2对 `dispatched-order.service.ts`
    为纯 `+38` 行新增（in_service 状态机分支），未触碰 returnOrder / split4 逻辑；
    该失败在阶段1集成链即已存在，另案跟踪。

### 4. tsc 全量类型检查 —— 通过（exit 0）

### 5. 前端关键业务回归 —— 48 files / 336 tests passed
角色菜单权限、后道人员分工、消息通知、数据看板、批量导入办理等既有能力不受影响。

## 关键风险验证结论
- 往返状态机 pending_info 多次触发：**已覆盖**（"multiple processing and pending-info round trips" 通过）
- 双人省份派单归属：**已覆盖**（fallback 不覆盖 Sheet4 主办 / 仅无主办时用 fallback 均通过）
- 省外北仑数据隔离：属阶段3范围，本阶段不涉及。

## 判定
阶段2在职管理模块权威实现测试全绿、类型检查通过、派单与前端回归无损，
唯一后端失败为基线预存缺陷。**结论：GO**。
